<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://ganhan999.github.io/</id>
    <title>Gridea</title>
    <updated>2021-02-21T09:57:21.938Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://ganhan999.github.io/"/>
    <link rel="self" href="https://ganhan999.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://ganhan999.github.io/images/avatar.png</logo>
    <icon>https://ganhan999.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Gridea</rights>
    <entry>
        <title type="html"><![CDATA[237、 删除链表中的节点]]></title>
        <id>https://ganhan999.github.io/post/237、 删除链表中的节点/</id>
        <link href="https://ganhan999.github.io/post/237、 删除链表中的节点/">
        </link>
        <updated>2021-02-21T10:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>请编写一个函数，使其可以删除某个链表中给定的（非末尾）节点。传入函数的唯一参数为 要被删除的节点 。<br>
现有一个链表 -- head = [4,5,1,9]，它可以表示为:</p>
<p>示例 1：<br>
输入：head = [4,5,1,9], node = 5<br>
输出：[4,1,9]<br>
解释：给定你链表中值为 5 的第二个节点，那么在调用了你的函数之后，该链表应变为 4 -&gt; 1 -&gt; 9.</p>
<p>示例 2：<br>
输入：head = [4,5,1,9], node = 1<br>
输出：[4,5,9]<br>
解释：给定你链表中值为 1 的第三个节点，那么在调用了你的函数之后，该链表应变为 4 -&gt; 5 -&gt; 9.</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>用下一个节点的值替代被删除节点的值，然后节点其实并未被删除，被删除的是node的下一个节点<br>
因为没有传头节点</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def deleteNode(self, node):
        node.val=node.next.val
        node.next=node.next.next
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等236. 二叉树的最近公共祖先]]></title>
        <id>https://ganhan999.github.io/post/236、二叉树的最近公共祖先/</id>
        <link href="https://ganhan999.github.io/post/236、二叉树的最近公共祖先/">
        </link>
        <updated>2021-02-20T12:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定一个二叉树, 找到该树中两个指定节点的最近公共祖先。</p>
<p>百度百科中最近公共祖先的定义为：“对于有根树 T 的两个节点 p、q，最近公共祖先表示为一个节点 x，<br>
满足 x 是 p、q 的祖先且 x 的深度尽可能大（一个节点也可以是它自己的祖先）。”</p>
<p>示例 1：<br>
输入：root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 1<br>
输出：3<br>
解释：节点 5 和节点 1 的最近公共祖先是节点 3 。</p>
<p>示例 2：<br>
输入：root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 4<br>
输出：5<br>
解释：节点 5 和节点 4 的最近公共祖先是节点 5 。因为根据定义最近公共祖先节点可以为节点本身。</p>
<p>示例 3：<br>
输入：root = [1,2], p = 1, q = 2<br>
输出：1</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>递归<br>
把符合条件的搬到父节点</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -&gt; 'TreeNode':
        if not root:#如果是空那么就返回空
            return None
        # 边界条件，如果匹配到left或right就直接返回停止递归
        if root.val == p.val or root.val == q.val:
            return root
        # 这两行代码可以无脑先写好！
        # 因为是DFS算法，这个模板可以无脑套用，写上之后可能你思路就清晰很多
        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)
        # 如果既在左子树找到，又在右子树找到，那么毫无疑问当前root就是公共节点
        if left and right:
            return root
        # 只有左子树有，那么直接返回左子树匹配到的第一个节点
        if left:
            return left
        if right:
            return right
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[235、二叉搜索树的最近公共祖先]]></title>
        <id>https://ganhan999.github.io/post/235、二叉搜索树的最近公共祖先/</id>
        <link href="https://ganhan999.github.io/post/235、二叉搜索树的最近公共祖先/">
        </link>
        <updated>2021-02-19T14:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定一个二叉搜索树, 找到该树中两个指定节点的最近公共祖先。<br>
百度百科中最近公共祖先的定义为：“对于有根树 T 的两个结点 p、q，最<br>
近公共祖先表示为一个结点 x，满足 x 是 p、q 的祖先且 x 的深度尽可能大（一个节点也可以是它自己的祖先）。”<br>
例如，给定如下二叉搜索树:  root = [6,2,8,0,4,7,9,null,null,3,5]</p>
<p>示例 1:<br>
输入: root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8<br>
输出: 6<br>
解释: 节点 2 和节点 8 的最近公共祖先是 6。</p>
<p>示例 2:<br>
输入: root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 4<br>
输出: 2<br>
解释: 节点 2 和节点 4 的最近公共祖先是 2, 因为根据定义最近公共祖先节点可以为节点本身。</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>两次遍历</p>
<p>找到两个节点<br>
分别记录他们的路径<br>
找到分岔点</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def lowestCommonAncestor(self, root: TreeNode, p: TreeNode, q: TreeNode) -&gt; TreeNode:
        def getPath(root: TreeNode, target: TreeNode) -&gt; List[TreeNode]:
            path = list()
            node = root
            while node != target:
                path.append(node)
                if target.val &lt; node.val:
                    node = node.left
                else:
                    node = node.right
            path.append(node)
            return path

        path_p = getPath(root, p)
        path_q = getPath(root, q)
        ancestor = None
        for u, v in zip(path_p, path_q):
            if u == v:
                ancestor = u
            else:
                break

        return ancestor
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>一次遍历</p>
<p>我们从根节点开始遍历；<br>
如果当前节点的值大于 p 和 q 的值，说明 p 和 q 应该在当前节点的左子树，因此将当前节点移动到它的左子节点；<br>
如果当前节点的值小于 p 和 q 的值，说明 p 和 q 应该在当前节点的右子树，因此将当前节点移动到它的右子节点；<br>
如果当前节点的值不满足上述两条要求，那么说明当前节点就是「分岔点」。此时，p 和 q 要么在当前节点的不同的子树中，要么其中一个就是当前节点。</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution:
    def lowestCommonAncestor(self, root: TreeNode, p: TreeNode, q: TreeNode) -&gt; TreeNode:
        ancestor = root
        while True:
            if p.val &lt; ancestor.val and q.val &lt; ancestor.val:
                ancestor = ancestor.left
            elif p.val &gt; ancestor.val and q.val &gt; ancestor.val:
                ancestor = ancestor.right
            else:
                break
        return ancestor
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[231、2的幂]]></title>
        <id>https://ganhan999.github.io/post/231、2的幂/</id>
        <link href="https://ganhan999.github.io/post/231、2的幂/">
        </link>
        <updated>2021-02-18T13:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定一个整数，编写一个函数来判断它是否是 2 的幂次方。</p>
<p>示例 1:<br>
输入: 1<br>
输出: true<br>
解释: 20 = 1</p>
<p>示例 2:<br>
输入: 16<br>
输出: true<br>
解释: 24 = 16</p>
<p>示例 3:<br>
输入: 218<br>
输出: false</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>一直除二，除到是奇数或者为1为止</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution(object):
    def isPowerOfTwo(self, n):
        if n == 0:
            return False
        while n % 2 == 0:
            n /= 2
        return n == 1
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>恒有 n &amp; (n - 1) == 0和n&gt;0的条件就是二的幂<br>
这里n二进制最高位为1，其他为0<br>
n-1则相反</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution:
    def isPowerOfTwo(self, n: int) -&gt; bool:
        return n &gt; 0 and n &amp; (n - 1) == 0
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>数1的个数</p>
<h1 id="大神做法3">大神做法3：</h1>
<pre><code class="language-python">class Solution:
    def isPowerOfTwo(self, n: int) -&gt; bool:
        return n&gt;=0 and bin(n).count('1') == 1
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等230. 二叉搜索树中第K小的元素]]></title>
        <id>https://ganhan999.github.io/post/230、二叉搜索树中第K小的元素/</id>
        <link href="https://ganhan999.github.io/post/230、二叉搜索树中第K小的元素/">
        </link>
        <updated>2021-02-15T02:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定一个二叉搜索树的根节点 root ，和一个整数 k ，请你设计一个算法查找其中第 k 个最小元素（从 1 开始计数）。</p>
<p>示例 1：<br>
输入：root = [3,1,4,null,2], k = 1<br>
输出：1</p>
<p>示例 2：<br>
输入：root = [5,3,6,2,4,null,null,1], k = 3<br>
输出：3</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>中序遍历后查找</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def kthSmallest(self, root, k):
        &quot;&quot;&quot;
        :type root: TreeNode
        :type k: int
        :rtype: int
        &quot;&quot;&quot;

        def inorder(r):
            return inorder(r.left) + [r.val] + inorder(r.right) if r else []

        return inorder(root)[k - 1]

</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>利用迭代就可以不用全部遍历二叉树</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution:
    def kthSmallest(self, root, k):
        &quot;&quot;&quot;
        :type root: TreeNode
        :type k: int
        :rtype: int
        &quot;&quot;&quot;
        stack = []

        while True:
            while root:
                stack.append(root)
                root = root.left
            root = stack.pop()
            k -= 1
            if not k:
                return root.val
            root = root.right

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[217、存在重复元素]]></title>
        <id>https://ganhan999.github.io/post/217、存在重复元素/</id>
        <link href="https://ganhan999.github.io/post/217、存在重复元素/">
        </link>
        <updated>2021-02-14T10:11:01.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定一个整数数组，判断是否存在重复元素。</p>
<p>如果存在一值在数组中出现至少两次，函数返回 true 。如果数组中每个元素都不相同，则返回 false 。</p>
<p>示例 1:<br>
输入: [1,2,3,1]<br>
输出: true</p>
<p>示例 2:<br>
输入: [1,2,3,4]<br>
输出: false</p>
<p>示例 3:<br>
输入: [1,1,1,3,3,4,3,2,4,2]<br>
输出: true</p>
<h1 id="本体思路">本体思路</h1>
<p>反证法，假设不存在有一个值是重复两次的，那么列表和集合的个数应该相同</p>
<h1 id="我的做法">#我的做法：</h1>
<pre><code class="language-python">class Solution:
    def containsDuplicate(self, nums: List[int]) -&gt; bool:
        len1=len(nums)
        set1=set(nums)
        len2=len(set1)
        return True if len1!=len2 else False
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="大神做法1">#大神做法1：</h1>
<p>排序后，然后判断前后元素是否相等</p>
<pre><code class="language-Python">class Solution:
    def containsDuplicate(self, nums: List[int]) -&gt; bool:
        # 先对数组排序
        nums.sort()
        # 遍历数组，比较前后元素值
        for i in range(len(nums)-1):
            if nums[i] == nums[i+1]:
                return True
        return False
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等215. 数组中的第K个最大元素]]></title>
        <id>https://ganhan999.github.io/post/215、数组中的第K个最大元素/</id>
        <link href="https://ganhan999.github.io/post/215、数组中的第K个最大元素/">
        </link>
        <updated>2021-02-13T04:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>在未排序的数组中找到第 k 个最大的元素。请注意，你需要找的是数组排序后的第 k 个最大的元素，而不是第 k 个不同的元素。</p>
<p>示例 1:<br>
输入: [3,2,1,5,6,4] 和 k = 2<br>
输出: 5</p>
<p>示例 2:<br>
输入: [3,2,3,1,2,4,5,5,6] 和 k = 4<br>
输出: 4</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>排序</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def findKthLargest(self, nums: List[int], k: int) -&gt; int:
        return sorted(nums, reverse = True)[k - 1]
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>堆排序，库函数</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution:
    def findKthLargest(self, nums: List[int], k: int) -&gt; int:
        return heapq.nlargest(k, nums)[-1]
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>堆排序，库函数</p>
<h1 id="大神做法3">大神做法3：</h1>
<pre><code class="language-python">class Solution:
    def findKthLargest(self, nums: List[int], k: int) -&gt; int:
        '''
        寻找第k个大的元素第一想法是构建小顶堆
        '''

        def shift_up(new_idx):
            new_val = minheap[new_idx]  # 临时存储需要上浮的元素
            # 向上寻找父结点进行比较，决定是否上浮
            while new_idx &gt; 0 and minheap[(new_idx - 1) // 2] &gt; new_val:
                minheap[new_idx] = minheap[(new_idx - 1) // 2]  # 父结点下沉
                new_idx = (new_idx - 1) // 2  # 子结点坐标上移
            minheap[new_idx] = new_val  # 完成结点上浮

        def shift_down(start, end):
            start_val = minheap[start]  # 临时存储头节点
            # 向下寻找双子结点进行比较，决定是否下沉
            while start * 2 + 1 &lt;= end:
                child = start * 2 + 1  # 设置左子节点
                # 比较左右子节点的大小，对于小顶堆来说，需要找出最小子结点
                if child + 1 &lt;= end and minheap[child] &gt; minheap[child + 1]:
                    child += 1
                if minheap[child] &lt; start_val:
                    minheap[start] = minheap[child]  # 子结点上浮
                    start = child
                else:
                    break
            minheap[start] = start_val  # 完成结点下沉

        # 上浮式建堆
        minheap = []
        for i in range(min(len(nums), k)):
            minheap.append(nums[i])
            shift_up(i)

        # 下沉式维护
        for num in nums[k:]:
            if num &gt; minheap[0]:
                minheap[0] = num
                shift_down(0, k - 1)

        return minheap[0]
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>快速排序＋选择</p>
<h1 id="大神做法4">大神做法4：</h1>
<pre><code class="language-python">class Solution:
    def findKthLargest(self, nums: List[int], k: int) -&gt; int:
        def partition( left, right):
            tmp = nums[left]
            while left &lt; right:
                while left &lt; right and nums[right] &lt;= tmp:#注意是找大的，所以要从大到小排序
                    right = right - 1
                nums[left] = nums[right]
                while left &lt; right and nums[left] &gt;= tmp:
                    left = left + 1
                nums[right] = nums[left]
            nums[left] = tmp
            mid = left
            return mid

        left = 0
        right = len(nums) - 1
        while 1:
            idx = partition(left, right)
            if idx == k - 1:
                return nums[idx]
            if idx &lt; k - 1:
                left = idx + 1
            if idx &gt; k - 1:
                right = idx - 1
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[206、反转链表]]></title>
        <id>https://ganhan999.github.io/post/206、反转链表/</id>
        <link href="https://ganhan999.github.io/post/206、反转链表/">
        </link>
        <updated>2021-02-12T03:11:01.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>反转一个单链表。</p>
<p>示例:<br>
输入: 1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;NULL<br>
输出: 5-&gt;4-&gt;3-&gt;2-&gt;1-&gt;NULL</p>
<p>进阶:<br>
你可以迭代或递归地反转链表。你能否用两种方法解决这道题？</p>
<h1 id="本体思路">本体思路</h1>
<p>迭代</p>
<h1 id="我的做法">#我的做法：</h1>
<pre><code class="language-python">class Solution:
    def reverseList(self, head: ListNode) -&gt; ListNode:
        pre = None
        cur = head
        while cur:
            temp = cur.next   # 先把原来cur.next位置存起来
            cur.next = pre
            pre = cur
            cur = temp
        return pre
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>递归</p>
<h1 id="大神做法1">#大神做法1：</h1>
<pre><code class="language-Python">class Solution:
    def reverseList(self, head: ListNode) -&gt; ListNode:
        if not head or head.next == None:
            return head
        res = self.reverseList(head.next)
        head.next.next = head
        head.next = None
        return res
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等148. 排序链表]]></title>
        <id>https://ganhan999.github.io/post/148、 排序链表/</id>
        <link href="https://ganhan999.github.io/post/148、 排序链表/">
        </link>
        <updated>2021-02-11T04:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给你链表的头结点 head ，请将其按 升序 排列并返回 排序后的链表 。</p>
<p>进阶：</p>
<p>你可以在 O(n log n) 时间复杂度和常数级空间复杂度下，对链表进行排序吗？</p>
<p>示例 1：<br>
输入：head = [4,2,1,3]<br>
输出：[1,2,3,4]</p>
<p>示例 2：<br>
输入：head = [-1,5,3,4,0]<br>
输出：[-1,0,3,4,5]</p>
<p>示例 3：<br>
输入：head = []<br>
输出：[]</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>合并排序，利用递归，自上而下</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def sortList(self, head: ListNode) -&gt; ListNode:
        def sortFunc(head: ListNode, tail: ListNode) -&gt; ListNode:#用快慢指针找到中点，分成两个链表
            if not head:
                return head
            if head.next == tail:
                head.next = None
                return head
            slow = fast = head
            while fast != tail:
                slow = slow.next
                fast = fast.next
                if fast != tail:
                    fast = fast.next
            mid = slow
            return merge(sortFunc(head, mid), sortFunc(mid, tail))

        def merge(head1: ListNode, head2: ListNode) -&gt; ListNode:
            dummyHead = ListNode(0)
            temp, temp1, temp2 = dummyHead, head1, head2
            while temp1 and temp2:
                if temp1.val &lt;= temp2.val:
                    temp.next = temp1
                    temp1 = temp1.next
                else:
                    temp.next = temp2
                    temp2 = temp2.next
                temp = temp.next
            if temp1:           
                temp.next = temp1
            elif temp2:
                temp.next = temp2
            return dummyHead.next
        return sortFunc(head, None)#一开始是以None作为fast的最后一个节点的判断条件
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>合并排序，自底向上，先1后2再4以此类推</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution:
    def sortList(self, head: ListNode) -&gt; ListNode:
        def merge(head1: ListNode, head2: ListNode) -&gt; ListNode:
            dummyHead = ListNode(0)
            temp, temp1, temp2 = dummyHead, head1, head2
            while temp1 and temp2:
                if temp1.val &lt;= temp2.val:
                    temp.next = temp1
                    temp1 = temp1.next
                else:
                    temp.next = temp2
                    temp2 = temp2.next
                temp = temp.next
            if temp1:
                temp.next = temp1
            elif temp2:
                temp.next = temp2
            return dummyHead.next

        if not head:
            return head

        length = 0
        node = head
        while node:
            length += 1
            node = node.next

        dummyHead = ListNode(0, head)
        subLength = 1
        while subLength &lt; length:
            prev, curr = dummyHead, dummyHead.next
            while curr:
                head1 = curr
                for i in range(1, subLength):
                    if curr.next:
                        curr = curr.next
                    else:
                        break
                head2 = curr.next
                curr.next = None
                curr = head2
                for i in range(1, subLength):
                    if curr and curr.next:
                        curr = curr.next
                    else:
                        break

                succ = None
                if curr:
                    succ = curr.next
                    curr.next = None

                merged = merge(head1, head2)
                prev.next = merged
                while prev.next:
                    prev = prev.next
                curr = succ
            subLength &lt;&lt;= 1
        return dummyHead.next
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等146. LRU 缓存机制]]></title>
        <id>https://ganhan999.github.io/post/146、LRU 缓存机制/</id>
        <link href="https://ganhan999.github.io/post/146、LRU 缓存机制/">
        </link>
        <updated>2021-02-10T04:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>运用你所掌握的数据结构，设计和实现一个  LRU (最近最少使用) 缓存机制 。<br>
实现 LRUCache 类：</p>
<p>LRUCache(int capacity) 以正整数作为容量 capacity 初始化 LRU 缓存<br>
int get(int key) 如果关键字 key 存在于缓存中，则返回关键字的值，否则返回 -1 。<br>
void put(int key, int value) 如果关键字已经存在，则变更其数据值；如果关键字不存在，<br>
则插入该组「关键字-值」。当缓存容量达到上限时，它应该在写入新数据之前删除最久未使用的数据值，从而为新的数据值留出空间。</p>
<p>进阶：你是否可以在 O(1) 时间复杂度内完成这两种操作？</p>
<p>示例：<br>
输入<br>
[&quot;LRUCache&quot;, &quot;put&quot;, &quot;put&quot;, &quot;get&quot;, &quot;put&quot;, &quot;get&quot;, &quot;put&quot;, &quot;get&quot;, &quot;get&quot;, &quot;get&quot;]<br>
[[2], [1, 1], [2, 2], [1], [3, 3], [2], [4, 4], [1], [3], [4]]</p>
<p>输出<br>
[null, null, null, 1, null, -1, null, -1, 3, 4]</p>
<p>解释<br>
LRUCache lRUCache = new LRUCache(2);<br>
lRUCache.put(1, 1); // 缓存是 {1=1}<br>
lRUCache.put(2, 2); // 缓存是 {1=1, 2=2}<br>
lRUCache.get(1);    // 返回 1<br>
lRUCache.put(3, 3); // 该操作会使得关键字 2 作废，缓存是 {1=1, 3=3}<br>
lRUCache.get(2);    // 返回 -1 (未找到)<br>
lRUCache.put(4, 4); // 该操作会使得关键字 1 作废，缓存是 {4=4, 3=3}<br>
lRUCache.get(1);    // 返回 -1 (未找到)<br>
lRUCache.get(3);    // 返回 3<br>
lRUCache.get(4);    // 返回 4</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>首先建立key：value键值对，把键值对放入hashmap中，也就是python的字典。<br>
然后再用双向链表的方式进行操作，链表中存储的是hashmap中的value</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class ListNode:
    def __init__(self, key=None, value=None):
        self.key = key
        self.value = value
        self.prev = None
        self.next = None


class LRUCache:
    def __init__(self, capacity: int):
        self.capacity = capacity
        self.hashmap = {}
        # 新建两个节点 head 和 tail
        self.head = ListNode()
        self.tail = ListNode()
        # 初始化链表为 head &lt;-&gt; tail
        self.head.next = self.tail
        self.tail.prev = self.head

    # 因为get与put操作都可能需要将双向链表中的某个节点移到末尾，所以定义一个方法
    def move_node_to_tail(self, key):
            # 先将哈希表key指向的节点拎出来，为了简洁起名node
            #      hashmap[key]                               hashmap[key]
            #           |                                          |
            #           V              --&gt;                         V
            # prev &lt;-&gt; node &lt;-&gt; next         pre &lt;-&gt; next   ...   node
            node = self.hashmap[key]
            node.prev.next = node.next
            node.next.prev = node.prev
            # 之后将node插入到尾节点前
            #                 hashmap[key]                 hashmap[key]
            #                      |                            |
            #                      V        --&gt;                 V
            # prev &lt;-&gt; tail  ...  node                prev &lt;-&gt; node &lt;-&gt; tail
            node.prev = self.tail.prev
            node.next = self.tail
            self.tail.prev.next = node
            self.tail.prev = node

    def get(self, key: int) -&gt; int:
        if key in self.hashmap:
            # 如果已经在链表中了久把它移到末尾（变成最新访问的）
            self.move_node_to_tail(key)
        res = self.hashmap.get(key, -1)
        if res == -1:
            return res
        else:
            return res.value

    def put(self, key: int, value: int) -&gt; None:
        if key in self.hashmap:
            # 如果key本身已经在哈希表中了就不需要在链表中加入新的节点
            # 但是需要更新字典该值对应节点的value
            self.hashmap[key].value = value
            # 之后将该节点移到末尾
            self.move_node_to_tail(key)
        else:
            if len(self.hashmap) == self.capacity:
                # 去掉哈希表对应项
                self.hashmap.pop(self.head.next.key)
                # 去掉最久没有被访问过的节点，即头节点之后的节点
                self.head.next = self.head.next.next
                self.head.next.prev = self.head
            # 如果不在的话就插入到尾节点前
            new = ListNode(key, value)
            self.hashmap[key] = new
            new.prev = self.tail.prev
            new.next = self.tail
            self.tail.prev.next = new
            self.tail.prev = new
</code></pre>
]]></content>
    </entry>
</feed>