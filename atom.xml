<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://ganhan999.github.io/</id>
    <title>Gridea</title>
    <updated>2021-01-24T11:14:12.052Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://ganhan999.github.io/"/>
    <link rel="self" href="https://ganhan999.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://ganhan999.github.io/images/avatar.png</logo>
    <icon>https://ganhan999.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Gridea</rights>
    <entry>
        <title type="html"><![CDATA[Java ElasticSearch]]></title>
        <id>https://ganhan999.github.io/post/Java ElasticSearch/</id>
        <link href="https://ganhan999.github.io/post/Java ElasticSearch/">
        </link>
        <updated>2021-01-23T14:18:25.000Z</updated>
        <content type="html"><![CDATA[<h1 id="elasticsearch">ElasticSearch</h1>
<h2 id="1-介绍">1. 介绍</h2>
<ul>
<li>本笔记参考狂神说，版本为7.6.X
<ul>
<li>https://www.bilibili.com/video/BV17a4y1x7zq?p=2</li>
</ul>
</li>
<li>Lucene是一套信息检索工具包（jar包），不含搜索引擎系统</li>
<li>ElasticSearch是基于Lucene做了一些封装和增强</li>
</ul>
<h2 id="2-入门操作">2. 入门操作</h2>
<ul>
<li>JDK1.8以上，客户端，界面工具</li>
<li>版本对应。</li>
</ul>
<h3 id="21-下载">2.1 下载</h3>
<blockquote>
<p>官网下载</p>
<p>windows下解压就可以使用</p>
</blockquote>
<p>目录：</p>
<pre><code class="language-bash">bin：启动文件
config：配置文件
	log4j2 日志文件
	jvm.options 虚拟机文件
	elasticsearch.yml 配置文件  比如默认9200端口
lib：相关jar包

modules：功能模块
plugins：插件：比如ik插件
</code></pre>
<p>启动，然后<strong>localhost:9200</strong>访问</p>
<h3 id="22-安装可视化界面head">2.2 安装可视化界面head</h3>
<ul>
<li>
<p>es head插件，github上面下载</p>
<ul>
<li>https://github.com/mobz/elasticsearch-head</li>
</ul>
</li>
<li>
<pre><code class="language-bash">npm install
npm run start #启动插件：localhost:9100
</code></pre>
</li>
<li>
<p>解决跨域问题：修改elasticsearch.yml文件</p>
<ul>
<li>
<pre><code class="language-yml">#解决跨域问题
http.cors.enabled: true
http.cors.allow-origin: &quot;*&quot;
</code></pre>
</li>
</ul>
</li>
</ul>
<h3 id="23-安装kibana">2.3 安装kibana</h3>
<ul>
<li>ELK：日志分析架构栈</li>
<li>注意：下载版本与es一致；可以在配置文件中汉化</li>
<li>默认端口 localhost:5601</li>
</ul>
<h2 id="3-es核心概念">3. ES核心概念</h2>
<ul>
<li>
<p>es是<strong>面向文档的</strong>，一切都是JSON</p>
</li>
<li>
<p>对比</p>
<ul>
<li>
<table>
<thead>
<tr>
<th>关系型数据库</th>
<th>Elasticsearch</th>
</tr>
</thead>
<tbody>
<tr>
<td>数据库database</td>
<td>索引 indices（数据库）</td>
</tr>
<tr>
<td>表tables</td>
<td>types （以后会被启用）</td>
</tr>
<tr>
<td>行rows</td>
<td>documents （文档）</td>
</tr>
<tr>
<td>字段columns</td>
<td>fields</td>
</tr>
</tbody>
</table>
</li>
</ul>
</li>
<li>
<p>物理设计</p>
<ul>
<li>在后台把每个<strong>索引划分为多个分片</strong>，每片可以再集群中的不同服务器间迁移；</li>
</ul>
</li>
<li>
<p>逻辑设计</p>
<ul>
<li>文档：索引和搜索数据的最小单位是文档；
<ul>
<li>自我包含：key：value</li>
<li>层次型：一个文档中包含文档（json对象）</li>
</ul>
</li>
<li>类型：文档的逻辑容器</li>
<li>索引：数据库</li>
</ul>
</li>
<li>
<p>倒排索引</p>
<ul>
<li>es使用倒排索引的结构，采用Lucene倒排索引作为底层。用于快速全文检索。</li>
</ul>
</li>
</ul>
<h2 id="4-ik分词器插件">4. IK分词器插件</h2>
<ul>
<li>什么是IK分词器：
<ul>
<li>把一句话分词</li>
<li>如果使用中文：推荐IK分词器</li>
<li>两个分词算法：ik_smart（最少切分），ik_max_word（最细粒度划分）</li>
</ul>
</li>
</ul>
<h3 id="41-下载安装">4.1 下载安装</h3>
<blockquote>
<p>下载地址：https://github.com/medcl/elasticsearch-analysis-ik/releases</p>
<p>然后解压，放到elasticsearch的plugins中，建立“ik”文件夹，然后放入；</p>
<p>重启观察es：发现加载ik插件了</p>
<p>[外链图片转存失败,源站可能有防盗链机制,建议将图片保存下来直接上传(img-ewUmG3or-1590485221083)(http://njpsz.xyz/images/ik.png)]</p>
</blockquote>
<h3 id="42-使用kibana测试">4.2 使用Kibana测试：</h3>
<p><strong>【ik_smart】测试：</strong></p>
<p>输入：</p>
<pre><code class="language-json">GET _analyze
{
  &quot;analyzer&quot;: &quot;ik_smart&quot;,
  &quot;text&quot;: &quot;我是社会主义接班人&quot;
}
</code></pre>
<p>输出：</p>
<pre><code class="language-json">{
  &quot;tokens&quot; : [
    {
      &quot;token&quot; : &quot;我&quot;,
      &quot;start_offset&quot; : 0,
      &quot;end_offset&quot; : 1,
      &quot;type&quot; : &quot;CN_CHAR&quot;,
      &quot;position&quot; : 0
    },
    {
      &quot;token&quot; : &quot;是&quot;,
      &quot;start_offset&quot; : 1,
      &quot;end_offset&quot; : 2,
      &quot;type&quot; : &quot;CN_CHAR&quot;,
      &quot;position&quot; : 1
    },
    {
      &quot;token&quot; : &quot;社会主义&quot;,
      &quot;start_offset&quot; : 2,
      &quot;end_offset&quot; : 6,
      &quot;type&quot; : &quot;CN_WORD&quot;,
      &quot;position&quot; : 2
    },
    {
      &quot;token&quot; : &quot;接班人&quot;,
      &quot;start_offset&quot; : 6,
      &quot;end_offset&quot; : 9,
      &quot;type&quot; : &quot;CN_WORD&quot;,
      &quot;position&quot; : 3
    }
  ]
}
</code></pre>
<p><strong>【ik_max_word】测试：</strong></p>
<p>输入：</p>
<pre><code class="language-json">GET _analyze
{
  &quot;analyzer&quot;: &quot;ik_max_word&quot;,
  &quot;text&quot;: &quot;我是社会主义接班人&quot;
}
</code></pre>
<p>输入：</p>
<pre><code class="language-json">{
  &quot;tokens&quot; : [
    {
      &quot;token&quot; : &quot;我&quot;,
      &quot;start_offset&quot; : 0,
      &quot;end_offset&quot; : 1,
      &quot;type&quot; : &quot;CN_CHAR&quot;,
      &quot;position&quot; : 0
    },
    {
      &quot;token&quot; : &quot;是&quot;,
      &quot;start_offset&quot; : 1,
      &quot;end_offset&quot; : 2,
      &quot;type&quot; : &quot;CN_CHAR&quot;,
      &quot;position&quot; : 1
    },
    {
      &quot;token&quot; : &quot;社会主义&quot;,
      &quot;start_offset&quot; : 2,
      &quot;end_offset&quot; : 6,
      &quot;type&quot; : &quot;CN_WORD&quot;,
      &quot;position&quot; : 2
    },
    {
      &quot;token&quot; : &quot;社会&quot;,
      &quot;start_offset&quot; : 2,
      &quot;end_offset&quot; : 4,
      &quot;type&quot; : &quot;CN_WORD&quot;,
      &quot;position&quot; : 3
    },
    {
      &quot;token&quot; : &quot;主义&quot;,
      &quot;start_offset&quot; : 4,
      &quot;end_offset&quot; : 6,
      &quot;type&quot; : &quot;CN_WORD&quot;,
      &quot;position&quot; : 4
    },
    {
      &quot;token&quot; : &quot;接班人&quot;,
      &quot;start_offset&quot; : 6,
      &quot;end_offset&quot; : 9,
      &quot;type&quot; : &quot;CN_WORD&quot;,
      &quot;position&quot; : 5
    },
    {
      &quot;token&quot; : &quot;接班&quot;,
      &quot;start_offset&quot; : 6,
      &quot;end_offset&quot; : 8,
      &quot;type&quot; : &quot;CN_WORD&quot;,
      &quot;position&quot; : 6
    },
    {
      &quot;token&quot; : &quot;人&quot;,
      &quot;start_offset&quot; : 8,
      &quot;end_offset&quot; : 9,
      &quot;type&quot; : &quot;CN_CHAR&quot;,
      &quot;position&quot; : 7
    }
  ]
}
</code></pre>
<h3 id="43-用户配置字典">4.3 用户配置字典</h3>
<p>当一些特殊词（比如姓名）不能被识别切分时候，用户可以自定义字典：</p>
<figure data-type="image" tabindex="1"><img src="https://imgconvert.csdnimg.cn/aHR0cDovL25qcHN6Lnh5ei9pbWFnZXMvaWsyLnBuZw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>重启es和kibana测试</p>
<p>[外链图片转存失败,源站可能有防盗链机制,建议将图片保存下来直接上传(img-dNxpUIif-1590485221097)(http://njpsz.xyz/images/20200417150218.png)]</p>
<h2 id="5-rest风格">5. Rest风格</h2>
<h3 id="51-简介">5.1 简介</h3>
<p>RESTful是一种架构的规范与约束、原则，符合这种规范的架构就是RESTful架构。</p>
<p>操作</p>
<table>
<thead>
<tr>
<th>method</th>
<th>url地址</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>PUT</td>
<td>localhost:9100/索引名称/类型名称/文档id</td>
<td>创建文档（指定id）</td>
</tr>
<tr>
<td>POST</td>
<td>localhost:9100/索引名称/类型名称</td>
<td>创建文档（随机id）</td>
</tr>
<tr>
<td>POST</td>
<td>localhost:9100/索引名称/文档类型/文档id/_update</td>
<td>修改文档</td>
</tr>
<tr>
<td>DELETE</td>
<td>localhost:9100/索引名称/文档类型/文档id</td>
<td>删除文档</td>
</tr>
<tr>
<td>GET</td>
<td>localhost:9100/索引名称/文档类型/文档id</td>
<td>查询文档通过文档id</td>
</tr>
<tr>
<td>POST</td>
<td>localhost:9100/索引名称/文档类型/_search</td>
<td>查询所有文档</td>
</tr>
</tbody>
</table>
<h3 id="52-测试">5.2 测试</h3>
<ul>
<li>1、创建一个索引<code>PUT /索引名/类型名/id</code></li>
<li>默认是_doc</li>
<li>[外链图片转存失败,源站可能有防盗链机制,建议将图片保存下来直接上传(img-DeUg5fjf-1590485221110)(http://njpsz.xyz/images/20200417153651.png)]</li>
</ul>
<figure data-type="image" tabindex="2"><img src="https://imgconvert.csdnimg.cn/aHR0cDovL25qcHN6Lnh5ei9pbWFnZXMvMjAyMDA0MTcxNTM4MzYucG5n?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<h3 id="53-数据类型">5.3 数据类型</h3>
<ol>
<li>基本数据类型</li>
</ol>
<ul>
<li>字符串 text, keyword</li>
<li>数据类型 long, integer,short,byte,double,float,half_float,scaled_float</li>
<li>日期 date</li>
<li>布尔 boolean</li>
<li>二进制 binary</li>
</ul>
<ol>
<li>制定数据类型</li>
</ol>
<p>输入：创建规则</p>
<pre><code class="language-bash">PUT /test2
{
  &quot;mappings&quot;: {
    
    &quot;properties&quot;: {
      &quot;name&quot;: {
        &quot;type&quot;: &quot;text&quot;
      },
      &quot;age&quot;: {
        &quot;type&quot;: &quot;long&quot;
      },
      &quot;birthday&quot;: {
        &quot;type&quot;: &quot;date&quot;
      }
    }
  }  
}
</code></pre>
<p>输出：</p>
<pre><code class="language-bash">{
  &quot;acknowledged&quot; : true,
  &quot;shards_acknowledged&quot; : true,
  &quot;index&quot; : &quot;test2&quot;
}
</code></pre>
<p><strong>如果不指定具体类型，es会默认配置类型</strong></p>
<h3 id="54-关于索引的基本操作">5.4 关于索引的基本操作</h3>
<ul>
<li>
<p>查看索引信息：</p>
<blockquote>
<p>GET test2</p>
</blockquote>
</li>
<li>
<p>查看es信息</p>
<blockquote>
<p>get _cat/</p>
</blockquote>
</li>
<li>
<p><strong>修改</strong></p>
<blockquote>
<ol>
<li>之前的办法：直接put</li>
<li>现在的办法：</li>
</ol>
<p>POST /test3/_doc/1/_update<br>
{<br>
“doc”: {<br>
“name”: “庞世宗”<br>
}<br>
}</p>
</blockquote>
</li>
<li>
<p>删除索引</p>
<blockquote>
<p>DELETE test1</p>
</blockquote>
</li>
</ul>
<h3 id="55-关于文档的基本操作重点">5.5 关于文档的基本操作（重点）</h3>
<h4 id="551-基本操作">5.5.1 基本操作</h4>
<p>1、添加数据</p>
<pre><code class="language-bash">PUT /psz/user/1
{
  &quot;name&quot;: &quot;psz&quot;,
  &quot;age&quot;: 22,
  &quot;desc&quot;: &quot;偶像派程序员&quot;,
  &quot;tags&quot;: [&quot;暖&quot;,&quot;帅&quot;]
}
</code></pre>
<p>2、获取数据</p>
<pre><code class="language-bash">GEt psz/user/1
===============输出===========
{
  &quot;_index&quot; : &quot;psz&quot;,
  &quot;_type&quot; : &quot;user&quot;,
  &quot;_id&quot; : &quot;1&quot;,
  &quot;_version&quot; : 1,
  &quot;_seq_no&quot; : 0,
  &quot;_primary_term&quot; : 1,
  &quot;found&quot; : true,
  &quot;_source&quot; : {
    &quot;name&quot; : &quot;psz&quot;,
    &quot;age&quot; : 22,
    &quot;desc&quot; : &quot;偶像派程序员&quot;,
    &quot;tags&quot; : [
      &quot;暖&quot;,
      &quot;帅&quot;
    ]
  }
}
</code></pre>
<p>3、 更新数据PUT</p>
<figure data-type="image" tabindex="3"><img src="https://imgconvert.csdnimg.cn/aHR0cDovL25qcHN6Lnh5ei9pbWFnZXMvMjAyMDA0MjAxMzAzMjYucG5n?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>4、更新数据，推荐POST _update</p>
<ul>
<li>不推荐</li>
</ul>
<pre><code class="language-bash">POST psz/user/1
{
  &quot;doc&quot;:{
    &quot;name&quot;: &quot;庞庞胖&quot;    #后面信息会没有
  }
}
</code></pre>
<ul>
<li>推荐！</li>
</ul>
<pre><code class="language-bash">POST psz/user/1/_update
{
  &quot;doc&quot;:{
    &quot;name&quot;: &quot;庞庞胖&quot;    #后面信息存在
  }
}
</code></pre>
<p>5、简单搜索 GET</p>
<pre><code class="language-bash">GET psz/user/1
</code></pre>
<p>简答的条件查询：根据默认映射规则产生基本的查询</p>
<pre><code class="language-bash">GET psz/user/_search?q=name:庞世宗
</code></pre>
<h4 id="552-复杂查询">5.5.2 复杂查询</h4>
<p>1、查询，参数使用JSON体</p>
<pre><code class="language-bash">GET psz/user/_search
{
  &quot;query&quot;: {
    &quot;match&quot;: {
      &quot;name&quot;: &quot;庞世宗&quot;   //根据name匹配
    }  
  },
    &quot;_source&quot;: [&quot;name&quot;,&quot;age&quot;],  //结果的过滤，只显示name和age
    &quot;sort&quot;: [
    {
      &quot;age&quot;: {
        &quot;order&quot;: &quot;desc&quot; //根据年龄降序
    }
    }
  ],
    
  &quot;from&quot;: 0, //分页：起始值，从0还是
  &quot;size&quot;: 1  //返回多少条数据
}
</code></pre>
<ul>
<li>之后只用java操作es时候，所有的对象和方法就是这里面的key</li>
<li>分页前端 /search/{current}/{pagesize}</li>
</ul>
<p>2 、布尔值查询</p>
<ul>
<li>must(对应mysql中的and) ,所有条件都要符合</li>
</ul>
<pre><code class="language-bash">GET psz/user/_search
{
  &quot;query&quot;: {
    &quot;bool&quot;: {
      &quot;must&quot;: [  //相当于and
        {
          &quot;match&quot;: {
            &quot;name&quot;: &quot;庞世宗&quot;
          }
          
        },
        {
          &quot;match&quot;: {
            &quot;age&quot;: 22
          }
        }
          
      ]
    }
  }
}
</code></pre>
<ul>
<li>shoule(对应mysql中的or)</li>
</ul>
<pre><code class="language-bash">GET psz/user/_search
{
  &quot;query&quot;: {
    &quot;bool&quot;: {
      &quot;should&quot;: [ //should相当于or
        {
          &quot;match&quot;: {
            &quot;name&quot;: &quot;庞世宗&quot;
          }
          
        },
        {
          &quot;match&quot;: {
            &quot;age&quot;: 22
          }
        }
          
      ]
    }
  }
}
</code></pre>
<ul>
<li>must_not (对应mysql中的not)</li>
<li><strong>过滤器</strong></li>
</ul>
<pre><code class="language-bash">GET psz/user/_search
{
  &quot;query&quot;: {
    &quot;bool&quot;: {
      &quot;should&quot;: [
        {
          &quot;match&quot;: {
            &quot;name&quot;: &quot;庞世宗&quot;
          }
          
        }
      ],
      &quot;filter&quot;: [
        {
          &quot;range&quot;: {
            &quot;age&quot;: {
              &quot;gt&quot;: 20   //过滤年龄大于20的
            }
          }
        }
      ]
    }
  }
}
</code></pre>
<p>3、精确查询</p>
<ul>
<li>trem查询是直接通过倒排索引指定的词条进行精确的查找的。</li>
</ul>
<p>关于分词：</p>
<p>trem，直接查询精确地</p>
<p>match，会使用<strong>分词器解析</strong></p>
<p>关于类型：</p>
<p>text: 分词器会解析</p>
<p>keywords: 不会被拆分</p>
<p>4、高亮查询</p>
<pre><code class="language-bash">GET psz/user/_search
{
  &quot;query&quot;: {
    &quot;match&quot;: {
      &quot;name&quot;: &quot;庞世宗&quot;
    }
  },
  &quot;_source&quot;: [&quot;name&quot;,&quot;age&quot;],
  &quot;sort&quot;: [
    {
      &quot;age&quot;: {
        &quot;order&quot;: &quot;desc&quot;
      }
    }
  ],
  &quot;highlight&quot;: //高亮
  {
    &quot;pre_tags&quot;: &quot;&lt;P&gt;&quot;,   //自定义高亮
    &quot;post_tags&quot;: &quot;&lt;/P&gt;&quot;, 
    &quot;fields&quot;: {
      &quot;name&quot;:{}  //自定义高亮区域
    }  
  }
}
</code></pre>
<h2 id="6-集成springboot">6. 集成Springboot</h2>
<h3 id="61-集成springboot">6.1 集成Springboot</h3>
<p>官方文档：https://www.elastic.co/guide/en/elasticsearch/client/java-rest/current/index.html</p>
<p>1、找到原生的依赖</p>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;org.elasticsearch.client&lt;/groupId&gt;
    &lt;artifactId&gt;elasticsearch-rest-high-level-client&lt;/artifactId&gt;
    &lt;version&gt;7.6.2&lt;/version&gt;
&lt;/dependency&gt;

    &lt;properties&gt;
        &lt;java.version&gt;1.8&lt;/java.version&gt;
        &lt;elasticsearch.version&gt;7.6.1&lt;/elasticsearch.version&gt;
    &lt;/properties&gt;
12345678910
</code></pre>
<p>2、找对象</p>
<p><strong>Initialization</strong></p>
<p>A <code>RestHighLevelClient</code> instance needs a <a href="https://www.elastic.co/guide/en/elasticsearch/client/java-rest/current/java-rest-low-usage-initialization.html">REST low-level client builder</a> to be built as follows:</p>
<pre><code class="language-java">RestHighLevelClient client = new RestHighLevelClient(
        RestClient.builder(
                new HttpHost(&quot;localhost&quot;, 9200, &quot;http&quot;),
                new HttpHost(&quot;localhost&quot;, 9201, &quot;http&quot;)));
</code></pre>
<p>The high-level client will internally create the low-level client used to perform requests based on the provided builder. That low-level client maintains a pool of connections and starts some threads so you should close the high-level client when you are well and truly done with it and it will in turn close the internal low-level client to free those resources. This can be done through the <code>close</code>:</p>
<pre><code class="language-java">client.close();
</code></pre>
<p>In the rest of this documentation about the Java High Level Client, the <code>RestHighLevelClient</code> instance will be referenced as <code>client</code>.</p>
<p>3、分析类中的方法</p>
<blockquote>
<p>一定要版本一致！默认es是6.8.1，要改成与本地一致的。</p>
</blockquote>
<pre><code class="language-xml">	&lt;properties&gt;
		&lt;java.version&gt;1.8&lt;/java.version&gt;
		&lt;elasticsearch.version&gt;7.6.1&lt;/elasticsearch.version&gt;
	&lt;/properties&gt;
1234
</code></pre>
<p>Java配置类</p>
<pre><code class="language-java">@Configuration  //xml
public class EsConfig {

    @Bean
    public RestHighLevelClient restHighLevelClient(){
        RestHighLevelClient client = new RestHighLevelClient(
                RestClient.builder(
                        new HttpHost(&quot;localhost&quot;, 9200, &quot;http&quot;))); //妈的被这个端口搞了
        return client;
    }
}
</code></pre>
<h3 id="62-索引api操作">6.2 索引API操作</h3>
<p>1、创建索引</p>
<pre><code class="language-java">@SpringBootTest
class EsApplicationTests {
	
	@Autowired
	@Qualifier(&quot;restHighLevelClient&quot;)
	private RestHighLevelClient restHighLevelClient;

	//创建索引的创建 Request
	@Test
	void testCreateIndex() throws IOException {
		//1.创建索引请求
		CreateIndexRequest request = new CreateIndexRequest(&quot;索引名&quot;);
		//2.执行创建请求 indices 请求后获得响应
		CreateIndexResponse createIndexResponse = restHighLevelClient.indices().create(request, RequestOptions.DEFAULT);

		System.out.println(createIndexResponse);
	}

}
</code></pre>
<p>2、获取索引</p>
<pre><code class="language-java">	@Test
	void testExistIndex() throws IOException {
		GetIndexRequest request = new GetIndexRequest(&quot;索引名&quot;);
		boolean exist =restHighLevelClient.indices().exists(request,RequestOptions.DEFAULT);
		System.out.println(exist);

	}
</code></pre>
<p>3、删除索引</p>
<pre><code class="language-java">	@Test
	void deleteIndex() throws IOException{
		DeleteIndexRequest requset = new DeleteIndexRequest(&quot;索引名&quot;);
		AcknowledgedResponse delete = restHighLevelClient.indices().delete(requset, RequestOptions.DEFAULT);
		System.out.println(delete.isAcknowledged());
	}
</code></pre>
<h3 id="63-文档api操作">6.3 文档API操作</h3>
<p>1、测试添加文档</p>
<pre><code class="language-java">	//测试添加文档
	@Test
	void testAddDocument() throws IOException {
		//创建对象
		User user = new User(&quot;psz&quot;, 22);
		IndexRequest request = new IndexRequest(&quot;ppp&quot;);
		//规则 PUT /ppp/_doc/1
		request.id(&quot;1&quot;);
		request.timeout(timeValueSeconds(1));
		//数据放入请求
		IndexRequest source = request.source(JSON.toJSONString(user), XContentType.JSON);

		//客户端发送请求,获取响应结果
		IndexResponse indexResponse = restHighLevelClient.index(request, RequestOptions.DEFAULT);
		System.out.println(indexResponse.toString());
		System.out.println(indexResponse.status());
	}
</code></pre>
<p>2、获取文档</p>
<pre><code class="language-java">	//获取文档，判断是否存在 GET /index/doc/1
	@Test
	void testIsExists() throws IOException {

		GetRequest getRequest = new GetRequest(&quot;ppp&quot;, &quot;1&quot;);
		//过滤，不放回_source上下文
		getRequest.fetchSourceContext(new FetchSourceContext(false));
		getRequest.storedFields(&quot;_none_&quot;);
		boolean exists = restHighLevelClient.exists(getRequest, RequestOptions.DEFAULT);
		System.out.println(exists);
	}
</code></pre>
<p>3、获取文档信息</p>
<pre><code class="language-java">	//获取文档信息
	@Test
	void getDocument() throws IOException {
		GetRequest getRequest = new GetRequest(&quot;ppp&quot;, &quot;1&quot;);
		GetResponse getResponse = restHighLevelClient.get(getRequest, RequestOptions.DEFAULT);
		System.out.println(getResponse.getSourceAsString());
		System.out.println(getResponse);
	}
==============输出==========================
{&quot;age&quot;:22,&quot;name&quot;:&quot;psz&quot;}
{&quot;_index&quot;:&quot;ppp&quot;,&quot;_type&quot;:&quot;_doc&quot;,&quot;_id&quot;:&quot;1&quot;,&quot;_version&quot;:2,&quot;_seq_no&quot;:1,&quot;_primary_term&quot;:1,&quot;found&quot;:true,&quot;_source&quot;:{&quot;age&quot;:22,&quot;name&quot;:&quot;psz&quot;}}
</code></pre>
<p>4、更新文档信息</p>
<pre><code class="language-java">	//更新文档信息
	@Test
	void updateDocument() throws IOException {

		UpdateRequest updateRequest = new UpdateRequest(&quot;ppp&quot;,&quot;1&quot;);
		updateRequest.timeout(&quot;1s&quot;);

		//json格式传入对象
		User user=new User(&quot;新名字&quot;,21);
		updateRequest.doc(JSON.toJSONString(user),XContentType.JSON);
		//请求，得到响应
		UpdateResponse updateResponse = restHighLevelClient.update(updateRequest, RequestOptions.DEFAULT);
		System.out.println(updateResponse);
	}
</code></pre>
<p>5、删除文档信息</p>
<pre><code>//删除文档信息
@Test
void deleteDocument() throws IOException {

   DeleteRequest deleteRequest = new DeleteRequest(&quot;ppp&quot;,&quot;1&quot;);
   deleteRequest.timeout(&quot;1s&quot;);
   DeleteResponse deleteResponse = restHighLevelClient.delete(deleteRequest, RequestOptions.DEFAULT);
   System.out.println(deleteResponse);
}
</code></pre>
<h3 id="64-批量操作bulk">6.4 批量操作Bulk</h3>
<ul>
<li>真实项目中，肯定用到大批量查询</li>
</ul>
<pre><code class="language-java">	@Test
	void testBulkRequest() throws IOException{
		BulkRequest bulkRequest = new BulkRequest();
		bulkRequest.timeout(&quot;10s&quot;);//数据量大的时候，秒数可以增加

		ArrayList&lt;User&gt; userList = new ArrayList&lt;&gt;();
		userList.add(new User(&quot;psz&quot;,11));
		userList.add(new User(&quot;psz2&quot;,12));
		userList.add(new User(&quot;psz3&quot;,13));
		userList.add(new User(&quot;psz4&quot;,14));
		userList.add(new User(&quot;psz5&quot;,15));

		for (int i = 0; i &lt; userList.size(); i++) {
			bulkRequest.add(
					new IndexRequest(&quot;ppp&quot;)
					.id(&quot;&quot;+(i+1))
					.source(JSON.toJSONString(userList.get(i)),XContentType.JSON));
		}
		//请求+获得响应
		BulkResponse bulkResponse = restHighLevelClient.bulk(bulkRequest, RequestOptions.DEFAULT);
		System.out.println(bulkResponse.hasFailures());//返回false：成功
	}
</code></pre>
<h3 id="65-搜索">6.5 搜索</h3>
<pre><code class="language-java">	/*
		查询:
		搜索请求：SearchRequest
		条件构造：SearchSourceBuilder
	 */
	@Test
	void testSearch() throws IOException {
		SearchRequest searchRequest = new SearchRequest(&quot;ppp&quot;);
		//构建搜索条件
		SearchSourceBuilder searchSourceBuilderBuilder = new SearchSourceBuilder();
		// 查询条件QueryBuilders工具
		// ：比如：精确查询
		TermQueryBuilder termQueryBuilder = QueryBuilders.termQuery(&quot;name&quot;, &quot;psz&quot;);
		searchSourceBuilderBuilder.query(termQueryBuilder);
		//设置查询时间
		searchSourceBuilderBuilder.timeout(new TimeValue(60, TimeUnit.SECONDS));
		//设置高亮
		//searchSourceBuilderBuilder.highlighter()

		searchRequest.source(searchSourceBuilderBuilder);
		SearchResponse searchResponse = restHighLevelClient.search(searchRequest, RequestOptions.DEFAULT);
		System.out.println(JSON.toJSONString(searchResponse.getHits()));
	}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java Redis]]></title>
        <id>https://ganhan999.github.io/post/Java Redis/</id>
        <link href="https://ganhan999.github.io/post/Java Redis/">
        </link>
        <updated>2021-01-22T15:18:25.000Z</updated>
        <content type="html"><![CDATA[<h1 id="nosql">nosql</h1>
<h2 id="为什么要用nosql">为什么要用nosql</h2>
<p>2020年，大数据时代；</p>
<blockquote>
<p>单机MySQL</p>
</blockquote>
<p>大数据，一般的数据库无法进行处理了！2006，Hadoop发布</p>
<ol>
<li>数据量太大，一个机器放不下</li>
<li>数据的索引（B+ Tree）,一个机器内存也放不下</li>
<li>访问量，一个服务器承受不了</li>
</ol>
<blockquote>
<p>2.Memcached（缓存）+MySQL+垂直拆分（读写分离）</p>
</blockquote>
<p>发展过程：</p>
<ol>
<li>优化数据结构和索引–》文件缓存（IO）–》Memcached（当时最热门的技术）</li>
</ol>
<figure data-type="image" tabindex="1"><img src="https://img-blog.csdnimg.cn/img_convert/b3b274b6a5b00a2df0cf95c6cd7023ee.png" alt="image-20201015114333233" loading="lazy"></figure>
<blockquote>
<p>分库分表+水平差分+Mysql集群</p>
</blockquote>
<p>本质：数据库（读，写）</p>
<p>MySQL，早些年MyISAM：表锁（100w，张三–密码），十分影响效率，高并发下就会出现严重的问题</p>
<p>Innodb：行锁，每次查询只锁一行</p>
<p>每个不同的业务使用不同的数据库进行单独管理</p>
<p>慢慢的就开始使用分库分表解决写的压力！MySQL在那个年代推出了表分区！这个并没有多少公司使用</p>
<p>MYSQL的集群，很好满足了那个年代的所有需求<img src="https://img-blog.csdnimg.cn/img_convert/c1763c1e27df326ad5068e402dc3ea4d.png" alt="image-20201015120050036" loading="lazy"></p>
<blockquote>
<p>最近的年代</p>
</blockquote>
<p>技术爆炸</p>
<p>MySQL等关系型数据库就不够用了，数据量大，变化快！</p>
<p>如果有一种数据库专门处理大文件数据，mysql压力就会变小</p>
<p>研究如何处理这些问题，大数据IO压力下，表基本上无法更改 1亿</p>
<blockquote>
<p>目前一个基本的互联网项目</p>
</blockquote>
<p>用户的个人信息，社交网络，地理位置，用户自己产生的数据，用户日志等等爆发性增长</p>
<p>这时候我们就需要用到nosql。nosql可以很好的处理以上的情况</p>
<figure data-type="image" tabindex="2"><img src="https://img-blog.csdnimg.cn/img_convert/deebe6a3d5c2a36a336863479b863696.png" alt="image-20201015122940653" loading="lazy"></figure>
<h2 id="什么是nosql">什么是NOSQL</h2>
<p>NoSQL： not only sql</p>
<p>泛指非关系型数据库，随着web2.0互联网的诞生；传统的关系型数据库很难对付web2.0时代！尤其是大规模高并发的社区！暴露出来难以克服的问题，NoSQL在当今大数据环境下发展十分迅速，Redis是发展最快的，当下要掌握的东西</p>
<p>关系型数据库：表格，行，列（POI技术），</p>
<p>很多的数据类型用户的个人信息，社交网络，地理位置，这些数据类型的存储不需要一个固定格式！不需要多余的操作就可以横向扩展！Map&lt;String,Object&gt;,使用键值对来控制</p>
<h2 id="nosql特点">NoSQL特点：</h2>
<p>解耦</p>
<ol>
<li>
<p>方便扩展（数据之间没有关系，很好扩展）</p>
</li>
<li>
<p>大数据量高性能（Redis一秒可以写八万次，读取11w次，NoSQL的缓存记录级，是一种细粒度的缓存，性能比较高）</p>
</li>
<li>
<p>数据类型是多样型的（不需要事先设计数据库，随取随用！如果是数据库量十分大的表，无法设计）</p>
</li>
<li>
<p>传统的RDBMS和NoSQL</p>
<pre><code>传统的RDBMS
- 结构化组织
- SQL
- 数据和关系都存在单独的表中 row col
- 数据操作，数据定义语言
- 严格的一致性
- 。。。。
1234567
</code></pre>
<pre><code>NoSQL
- 不仅仅是数据
- 没有固定的查询语言
- 键值对存储，列存储，文档存储，图形数据库（社交关系）
- 最终一致性
- CAP定理和BASE（异地多活） 初级架构师
- 高性能，高可用，高可扩展
- 。。。。
12345678
</code></pre>
</li>
</ol>
<h2 id="了解3v3高">了解：3V+3高</h2>
<p>大数据的3V：主要是描述问题的</p>
<ol>
<li>海量Volume</li>
<li>多样Variety</li>
<li>实时Velocity</li>
</ol>
<p>三高：对程序的要求</p>
<ol>
<li>高并发</li>
<li>高可扩（随时水平拆分，机器不够了，可以扩展机器来解决）</li>
<li>高性能（保证用户体验和性能）</li>
</ol>
<p>真正在公司中的实践，一定是nosql + RDBMS，一起使用才是最强的</p>
<p>技术没有高低之分，就看你如何使用。（内功，思维的提高）</p>
<h1 id="alibaba架构演进">alibaba架构演进</h1>
<p>思考问题：这么多的问题都是在一个数据库中的吗？</p>
<p>技术不能急，慢慢学才能扎实。</p>
<p>敏捷开发，极限编程</p>
<p>开源才是技术的王道</p>
<p>如果未来想当一个架构师，没有什么是加一层解决不了的</p>
<pre><code>1.商品的基本信息
名称，价格，商家信息
关系型数据库就可以解决了，MySQL/Oracle
淘宝内部的mysql不是我们用的mysql
2.商品的描述。评论（文字比较多）
文档型数据库中，MongoDB
3，图片
分布式文件系统，FastDFS
- 淘宝自己的 TFS
- Google的 GFS
- Hadoop的HDFS
- 阿里云的 oss
4.商品关键字
- 搜索引擎 solr elasticsearch
- ISearch：多隆
5. 商品热门的波段信息
- 内存数据库
- Redis，Tair，Memacache
6。商品的交易，外部的支付接口
- 三方应用
1234567891011121314151617181920
</code></pre>
<p>大型互联网应用问题：</p>
<ol>
<li>数据类型太多了</li>
<li>数据源太多了，经常重构</li>
<li>数据要改造需要大面积改动</li>
</ol>
<p>解决问题：</p>
<h1 id="nosql数据类型">nosql数据类型</h1>
<h1 id="nosql四大分类">nosql四大分类</h1>
<h2 id="kv键值对">KV键值对：</h2>
<ul>
<li>新浪：Redis</li>
<li>美团：Redis + Tair</li>
<li>阿里，百度：Redis + Memacache</li>
</ul>
<h3 id="文档型数据库bson格式和json一样">文档型数据库（bson格式，和json一样）：</h3>
<ul>
<li>MongoDB（一般必须要掌握）
<ul>
<li>MongoDB是一个基于分布式文件存储的数据库，C++编写，主要用来处理大量的文档</li>
<li>MongoDB是一个介于关系型数据库和非关系型数据库中间的产品！MongoDB是非关系型数据库中功能最丰富，最像关系型数据库的</li>
</ul>
</li>
<li>ConthDB</li>
</ul>
<p>列存储数据库</p>
<ul>
<li>HBase</li>
<li>分布式文件系统</li>
</ul>
<p>图关系数据库</p>
<ul>
<li>不是存图形的，放的是关系</li>
<li>Neo4j，InfoGrid</li>
</ul>
<figure data-type="image" tabindex="3"><img src="https://img-blog.csdnimg.cn/img_convert/94831069fe316c7af32f84feae62e7ec.png" alt="img" loading="lazy"></figure>
<h1 id="cap">CAP</h1>
<h1 id="base">BASE</h1>
<h1 id="redis入门">Redis入门</h1>
<p>Redis（Remote Dictionary Server )，即远程字典服务，是一个开源的使用ANSI <a href="https://baike.baidu.com/item/C%E8%AF%AD%E8%A8%80">C语言</a>编写、支持网络、可基于内存亦可持久化的日志型、Key-Value<a href="https://baike.baidu.com/item/%E6%95%B0%E6%8D%AE%E5%BA%93/103728">数据库</a>，并提供多种语言的API。从2010年3月15日起，Redis的开发工作由VMware主持。从2013年5月开始，Redis的开发由<a href="https://baike.baidu.com/item/Pivotal">Pivotal</a>赞助。</p>
<p>免费和开源！是当下最热门的nosql技术</p>
<p>区别的是redis会周期性的把更新的数据写入磁盘或者把修改操作写入追加的记录文件，并且在此基础上实现了master-slave(主从)同步。</p>
<h2 id="redis能干嘛">Redis能干嘛：</h2>
<ol>
<li>内存存储，持久化，内存中是断电即失，所以说持久化很重要，（rdb，aof）</li>
<li>效率高，可以用于高速缓存</li>
<li>发布订阅系统</li>
<li>地图信息分析</li>
<li>计时器，计数器（浏览量）</li>
</ol>
<h2 id="特性">特性：</h2>
<ol>
<li>多样的数据类型</li>
<li>持久化</li>
<li>集群</li>
<li>事务</li>
</ol>
<h2 id="学习中需要用到的东西">学习中需要用到的东西：</h2>
<ol>
<li>狂神的公众号</li>
<li>官网：https://redis.io/</li>
<li>中文网 ：http://www.redis.cn/</li>
<li>下载地址，通过官网下载即可</li>
</ol>
<p>注意：windows版本的在github上下载（停更很久了）</p>
<p>Redis推荐都是在linux上搭建的</p>
<h1 id="redis安装">Redis安装</h1>
<h2 id="windows下安装">Windows下安装</h2>
<ol>
<li>
<p>下载安装包https://github.com/dmajkic/redis/releases</p>
</li>
<li>
<p>下载完毕得到压缩包<img src="https://img-blog.csdnimg.cn/img_convert/6a6ea6c70217b185322b00c7ae1dd821.png" alt="image-20201015181310051" loading="lazy"></p>
</li>
<li>
<p>解压到自己电脑下的环境目录即可，Redis十分的小<img src="https://img-blog.csdnimg.cn/img_convert/4d21aefd32119c61fa11530b1b7d690b.png" alt="image-20201015181503909" loading="lazy"></p>
</li>
<li>
<p>开启Redis，双击运行服务即可，默认端口号：6379<img src="https://img-blog.csdnimg.cn/img_convert/05e3b5ac7de329c051e1e21f67f432af.png" alt="image-20201015183108341" loading="lazy"></p>
</li>
<li>
<p>使用redis客户端来连接redis</p>
</li>
<li>
<figure data-type="image" tabindex="4"><img src="https://img-blog.csdnimg.cn/img_convert/a82b0e48e00247e01ac2f872b9c40a7e.png" alt="image-20201015183413390" loading="lazy"></figure>
<pre><code class="language-redis">ping 测试连接
set name yangjian key-value键值对

123
</code></pre>
<p>记住一句话，Windows下使用确实简单，但是Redis推荐我们使用linux去开发使用<img src="https://img-blog.csdnimg.cn/img_convert/6e93fcf46ce3cd8a9658eecf3017e87b.png" alt="image-20201015183844238" loading="lazy"></p>
</li>
</ol>
<h2 id="linux下安装">Linux下安装</h2>
<ol>
<li>
<p>下载安装包，官网下载 http://www.redis.cn/<code>redis-6.0.6.tar.gz</code></p>
</li>
<li>
<p>解压redis安装包，程序/opt</p>
<pre><code class="language-linux">tar -zxvf redis-6.0.6.tar.gz
1
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://img-blog.csdnimg.cn/img_convert/879fd1fd07ebb9ed8c8eaee5b4766fe0.png" alt="image-20201015191253901" loading="lazy"></figure>
</li>
<li>
<p>进入解压后的文件，可以看到redis的配置文件</p>
</li>
</ol>
<figure data-type="image" tabindex="6"><img src="https://img-blog.csdnimg.cn/img_convert/7081b0d4827623d8cf42052151eb1bfa.png" alt="image-20201015191419342" loading="lazy"></figure>
<p>https://blog.csdn.net/qq_39135287/article/details/83474865?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522160427974219724813256379%2522%252C%2522scm%2522%253A%252220140713.130102334.pc%255Fall.%2522%257D&amp;request_id=160427974219724813256379&amp;biz_id=0&amp;utm_medium=distribute.pc_search_result.none-task-blog-2allfirst_rank_v2~rank_v28-1-83474865.pc_search_result_cache&amp;utm_term=linux%E4%B8%8B%E5%AE%89%E8%A3%85redis&amp;spm=1018.2118.3001.4449</p>
<p>注意：新版本需要升级gcc</p>
<h3 id="通过指定的配置文件启动服务">通过指定的配置文件启动服务</h3>
<figure data-type="image" tabindex="7"><img src="https://img-blog.csdnimg.cn/img_convert/02b40f2527208ed95e05032212fda96d.png" alt="image-20201102101058193" loading="lazy"></figure>
<h3 id="使用redis-cli客户端连接">使用redis-cli客户端连接</h3>
<figure data-type="image" tabindex="8"><img src="https://img-blog.csdnimg.cn/img_convert/9caac0d2f3bbba242609021f1bddf9ab.png" alt="image-20201102101741551" loading="lazy"></figure>
<h3 id="查看redis进程是否开启">查看redis进程是否开启</h3>
<figure data-type="image" tabindex="9"><img src="https://img-blog.csdnimg.cn/img_convert/7adc9fc365d7eef857fdae850f3b0046.png" alt="image-20201102102014348" loading="lazy"></figure>
<h3 id="如何关闭redis服务呢shutdown">如何关闭redis服务呢？shutdown</h3>
<figure data-type="image" tabindex="10"><img src="https://img-blog.csdnimg.cn/img_convert/ccc74f1c1cb917a268f7ee9e1026261a.png" alt="image-20201102102124065" loading="lazy"></figure>
<p>后面我们会使用单机多redis集群进行测试</p>
<h2 id="测试性能">测试性能</h2>
<p>redis-benchmark是一个压力测试工具</p>
<p>官方自带的性能测试工具</p>
<h3 id="命令参数">命令参数</h3>
<figure data-type="image" tabindex="11"><img src="https://img-blog.csdnimg.cn/img_convert/fea09ddf2bd6acecbdb7db2ddc420faa.png" alt="image-20201102100512051" loading="lazy"></figure>
<p><strong>自己来测试一下</strong></p>
<pre><code class="language-bash"># 测试100个并发连接，100000请求
redis-benchmark -h localhost -p 6379 -c 100 -n 100000
12
</code></pre>
<h3 id="如何查看分析">如何查看分析</h3>
<figure data-type="image" tabindex="12"><img src="https://img-blog.csdnimg.cn/img_convert/2c22a9f8a1cc97ab1e9aa9d6cc426808.png" alt="image-20201102102737332" loading="lazy"></figure>
<figure data-type="image" tabindex="13"><img src="https://img-blog.csdnimg.cn/img_convert/479a2863740a879c3f62a786d3a53eee.png" alt="image-20201102102938949" loading="lazy"></figure>
<h2 id="基础知识">基础知识</h2>
<p>redis默认有16个数据库</p>
<p>默认使用的是第0个</p>
<p>可以使用select 进行切换数据库</p>
<pre><code class="language-bash">127.0.0.1:6379&gt; select 3  # 切换数据库
OK
127.0.0.1:6379[3]&gt; dbsize  # 查看db大小
(integer) 0

keys * # 查看当前数据库所有的key

flushdb  # 清空当前库

flushall # 清空所有数据库
12345678910
</code></pre>
<p>为什么redis的端口号为6379（了解即可，一个女人的姓名（手机</p>
<blockquote>
<p>redis是单线程的</p>
</blockquote>
<p>明白redis是很快的，官方表示，redis是基于内存操作的，cpu不是redis的性能瓶颈，redis的瓶颈是根据机器的内存和网络带宽，既然可以使用单线程来实现，那就使用了单线程了！所以就使用了单线程，</p>
<p>Redis是由c语言写的，官方提供的数据为 100000+的QPS，说明这个完全不比memecache差！</p>
<p>Redis 为什么单线程还更快？</p>
<ol>
<li>误区：高性能的服务器一定是多线程的？</li>
<li>误区：多线程一定比单线程效率高（上下文切换）</li>
</ol>
<p>CPU&gt;内存&gt;硬盘的速度要有所了解</p>
<p>核心：redis是将所有的数据全部放在内存里面的，所以说使用单线程操作效率就是最高的（CPU上下文切换是一个耗时的操作）</p>
<p>对于内存系统来说，如果没有上下文切换，效率就是最高的！多次读写都是在一个cpu上的，在内存情况下，这个就是最佳的方案</p>
<figure data-type="image" tabindex="14"><img src="https://img-blog.csdnimg.cn/img_convert/0a2aabfa7475eb7d3427a5d3b57abb5d.png" alt="image-20201102103753647" loading="lazy"></figure>
<h1 id="五大基本数据类型">五大基本数据类型</h1>
<blockquote>
<p>官方文档</p>
<p>Redis 是一个开源（BSD许可）的，内存中的数据结构存储系统，它可以用作数据库、缓存和消息中间件。 它支持多种类型的数据结构，如 <a href="http://www.redis.cn/topics/data-types-intro.html#strings">字符串（strings）</a>， <a href="http://www.redis.cn/topics/data-types-intro.html#hashes">散列（hashes）</a>， <a href="http://www.redis.cn/topics/data-types-intro.html#lists">列表（lists）</a>， <a href="http://www.redis.cn/topics/data-types-intro.html#sets">集合（sets）</a>， <a href="http://www.redis.cn/topics/data-types-intro.html#sorted-sets">有序集合（sorted sets）</a> 与范围查询， <a href="http://www.redis.cn/topics/data-types-intro.html#bitmaps">bitmaps</a>， <a href="http://www.redis.cn/topics/data-types-intro.html#hyperloglogs">hyperloglogs</a> 和 <a href="http://www.redis.cn/commands/geoadd.html">地理空间（geospatial）</a> 索引半径查询。 Redis 内置了 <a href="http://www.redis.cn/topics/replication.html">复制（replication）</a>，<a href="http://www.redis.cn/commands/eval.html">LUA脚本（Lua scripting）</a>， <a href="http://www.redis.cn/topics/lru-cache.html">LRU驱动事件（LRU eviction）</a>，<a href="http://www.redis.cn/topics/transactions.html">事务（transactions）</a> 和不同级别的 <a href="http://www.redis.cn/topics/persistence.html">磁盘持久化（persistence）</a>， 并通过 <a href="http://www.redis.cn/topics/sentinel.html">Redis哨兵（Sentinel）</a>和自动 <a href="http://www.redis.cn/topics/cluster-tutorial.html">分区（Cluster）</a>提供高可用性（high availability）</p>
</blockquote>
<blockquote>
<p>现在的命令一定要全部记住，后面使用springboot，jredis，所有的都是基于此</p>
</blockquote>
<h2 id="redis-key">Redis-Key</h2>
<h3 id="基本命令">基本命令</h3>
<pre><code class="language-bash">set key value # set值

keys * #查看当前db的所有key

get key # 得到key的value

exists key # 是否存在key（键）

move key db # 移动键到另外一个数据库，共有16个数据库，默认为0

expire key 秒数 # 设置过期时间，单位是秒

ttl key # 查看还有多少时间过期

type key # 查看key的具体类型
123456789101112131415
</code></pre>
<p>不会的命令去官网查http://www.redis.cn/commands.html</p>
<h2 id="string">String</h2>
<p>九成的java程序员使用redis只会一个String类型！</p>
<pre><code class="language-bash">append key &quot;字符串&quot; # 向key后追加一个字符串，如果当前key不存在，就相当于set

strlen key # 获取字符串长度

incr key # 加一

decr key # 自减1

incrby value #增加value，可以设置步长，指定增量

decrby value # 减少value

############################################################
字符串范围 range 

getrange key start end # 下标从0开始，start-end
getrange key 0 -1 # 查看整个字符串,获取全部字符串，和get key是一样的

# 替换
# 替换指定位置开始的字符串
setrange key offset string # offset：偏移量，string：要替换的字符串

###########################################################
# setex(set with expire) # 设置过期时间

# setnx(set if not exist) # 不存在设置，在分布式锁中会经常使用，保证当前这个值存在
127.0.0.1:6379&gt; setex key3 30 hello  # 设置key3的值30s后过期
OK
127.0.0.1:6379&gt; ttl key3
(integer) 27
127.0.0.1:6379&gt; get key3
&quot;hello&quot;
127.0.0.1:6379&gt; setnx mykey &quot;redis&quot; # 如果mykey不存在，创建mykey
(integer) 1
127.0.0.1:6379&gt; keys *
1) &quot;mykey&quot;
2) &quot;key2&quot;
3) &quot;key1&quot;
127.0.0.1:6379&gt; ttl key3
(integer) -2
127.0.0.1:6379&gt; setnx mykey &quot;MongoDB&quot; # 如果mykey存在，则创建失败
(integer) 0
127.0.0.1:6379&gt; get mykey
&quot;redis&quot;
########################################################
# 批量set，get
mset k1 v1 k2 v2 ... # 同时设置多个值

mget k1 k2 ... # 同时获取多个值

msetnx k1 v1 k2 v2 ... # msetnx是一个原子性的操作，要么一起成功，要么一起失败

# 对象
set user:1 {name:zhangsan,age:3} # 设置一个user:1对象，值为json字符串来保存一个对象

mset user:1:name zhangsan user:1:age 20 # 这里的key是一个巧妙的设计，user:{id}:{field}，如此设计在redis中是完全可以的

getset # 先get后se
getset key value # 如果不存在值则返回nil，如果存在值则获取原来的值，然后set
1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859
</code></pre>
<p>数据结构是相通的</p>
<p>String类似的使用场景：value除了是我们的字符串还可以是数字</p>
<ul>
<li>计数器</li>
<li>统计多单位的数量 uid</li>
<li>粉丝数</li>
<li>对象缓存存储</li>
</ul>
<h2 id="list">List</h2>
<p>基本的数据类型，列表</p>
<p>在redis里面，我们可以把list玩成栈，队列</p>
<p>所有的list命令都是用<code>l</code>来开头的，不区分大小写命令</p>
<pre><code class="language-bash">127.0.0.1:6379&gt; lpush list one # 将一个值或多个值插入到列表的头部
(integer) 1
127.0.0.1:6379&gt; lpush list two
(integer) 2
127.0.0.1:6379&gt; lpush list three
(integer) 3
127.0.0.1:6379&gt; lrange list 0 -1 # 获取所有元素
1) &quot;three&quot;
2) &quot;two&quot;
3) &quot;one&quot;
127.0.0.1:6379&gt; lrange list 0 1 # 获取0-1，通过区间获取具体的值
1) &quot;three&quot;
2) &quot;two&quot;
127.0.0.1:6379&gt; rpush list right # 将一个值放在链表的尾部
(integer) 4
127.0.0.1:6379&gt; lrange list  0  -1
1) &quot;three&quot;
2) &quot;two&quot;
3) &quot;one&quot;
4) &quot;right&quot;

##############################################################

lpop key # 移除列表的第一个元素
rpop key # 移除列表的最后一个元素

lindex key index # 获取key的index下标处的值，index从0开始

llen key # 获取list的长度

# 移除指定的值
lrme key count value # 删除count个value，从前往后删除，精确匹配

#############################################################
trim 修剪操作：list截断
ltrim key start stop # 截取list的start到stop之间的所有元素，左右都闭合，这个list就已经被改变了，只剩下截取的元素

rpoplpush source destination # 移除列表的最后一个元素并添加到目的列表的第一个

exists key # 判断里面有没有值

lset key index element # 设置key的index下标位置的值为element ，只有存在这个index下标才可以使用

linsert key before|after pivot element # 在pivot之前或之后插入值
1234567891011121314151617181920212223242526272829303132333435363738394041424344
</code></pre>
<p>小结：</p>
<ul>
<li>list实际上一个链表，before Node after，左右都可以插入</li>
<li>如果key不存在，创建新的链表</li>
<li>如果存在，新增内容</li>
<li>如果移除了所有值，空链表，不存在</li>
<li>在两边改动效率最高，中间元素效率会低一点</li>
</ul>
<p>消息排队！消息队列，</p>
<h2 id="set">Set</h2>
<p>set中的值是不能重复的</p>
<pre><code class="language-bash">sadd key value # 向set集合中添加

smembers key # 查看key里面的所有值

scard key # 获取set的元素个数

sismember key value # 判断value值是否在key中

srem key value # 移除value元素

set # 无序不重复集合，抽随机

srandmember myset count # 随机抽出指定个数元素

spop key count # 随机删除一些set集合中的元素

smove source destination # 将一个集合中的元素移动到另一个集合中

数字集合类：
 - 差集 sdiff key1 key2
 - 交集 sinter key1 key2 # 共同好友就可以这样实现
 - 并集 sunion key1 key2 
12345678910111213141516171819202122
</code></pre>
<p>共同关注，共同爱好，二度好友</p>
<h2 id="hash哈希">Hash（哈希）</h2>
<p>Map集合，key-,key-map,这时候这个值是一个map集合，本质和string没有太大区别</p>
<pre><code class="language-bash">hset key filed value [field value] # 存放hash As of Redis 4.0.0, HSET is variadic and allows for multiple field/value pairs.
# As per Redis 4.0.0, HMSET is considered deprecated. Please use HSET in new code.

hget key field # 获取key中filed键的值

hgetall key # 获取所有的键值对

hdel key filed # 删除hash指定的字段，对应的value也就没有了

hlen key # 获取hash的内容长度

hexists key field # 判断hash中的某个filed是否存在

hkeys key # 获取hash的所有键（key）

hvals key # 获取hash的所有值

hincrby key filed count # 自增count

hdecrby key field count  # 自减
1234567891011121314151617181920
</code></pre>
<h2 id="zset有序集合">Zset(有序集合)</h2>
<p>在set的基础上，增加了一个值, zset k1 score1 v1</p>
<pre><code class="language-bash">zadd key score value # 添加值，score代表优先级，可以一次添加多个

zrange key start end # 获取start-end的值，0 -1代表获取所有值

# 排序如何实现
zrangebyscore key startscore endscore # 对集合通过score排序， 默认升序

zrangebyscore key -inf inf withscores # 显示score

zrevrange salary 0 -1 [withscores] # 降序排列所有值

zrem key member [member] # 移除元素

zcard key # 获取有序集合中的个数

zcount key start end # 获取start-end之间的个数
12345678910111213141516
</code></pre>
<p>多查官方文档</p>
<p>案例思路：set 排序 存储班级成绩表，工资表排序</p>
<p>普通消息：1.重要消息 2.带权重进行判断</p>
<p>排行榜应用实现，取top n测试</p>
<h1 id="三种特殊数据类型">三种特殊数据类型</h1>
<h2 id="geospatial-地理位置">geospatial 地理位置</h2>
<p>朋友的定位，附近的人，打车距离计算。</p>
<p>两地之间的距离，方圆几里的人</p>
<p>可以查询一些测试数据</p>
<p>只有六个命令</p>
<figure data-type="image" tabindex="15"><img src="https://img-blog.csdnimg.cn/img_convert/579ecfb5f79518c8509056805825f924.png" alt="image-20201103190205816" loading="lazy"></figure>
<blockquote>
<p>geoadd，添加地理位置的经度纬度</p>
</blockquote>
<pre><code class="language-bash"># 规则：两级无法直接添加，我们一般会下载城市数据，直接通过java程序一次性导入
# 
geoadd key 纬度 经度  名称 # 添加地理位置信息
127.0.0.1:6379&gt; geoadd China:city 116.23128 40.22077 beijing
(integer) 1
127.0.0.1:6379&gt; geoadd China:city 121.48941 31.40527 shanghai
(integer) 1
127.0.0.1:6379&gt; geoadd China:city 106.54041 29.40268 chongqing
12345678
</code></pre>
<blockquote>
<p>geopos，获取指定的经度纬度</p>
</blockquote>
<pre><code class="language-bash">geopos key position # 获取position的经度纬度
1
</code></pre>
<blockquote>
<p>geodist：返回两个位置的绝对距离</p>
</blockquote>
<p>返回两个给定位置之间的距离。</p>
<p>如果两个位置之间的其中一个不存在， 那么命令返回空值。</p>
<p>指定单位的参数 unit 必须是以下单位的其中一个：</p>
<ul>
<li><strong>m</strong> 表示单位为米。</li>
<li><strong>km</strong> 表示单位为千米。</li>
<li><strong>mi</strong> 表示单位为英里。</li>
<li><strong>ft</strong> 表示单位为英尺。</li>
</ul>
<pre><code class="language-bash">geodist key member1 member2 [m|km|ft|mi] # 查看member1到member2的直线距离
1
</code></pre>
<blockquote>
<p>georadius</p>
</blockquote>
<p>以给定的经纬度为中心， 返回键包含的位置元素当中， 与中心的距离不超过给定最大距离的所有位置元素。</p>
<p>范围可以使用以下其中一个单位：</p>
<ul>
<li><strong>m</strong> 表示单位为米。</li>
<li><strong>km</strong> 表示单位为千米。</li>
<li><strong>mi</strong> 表示单位为英里。</li>
<li><strong>ft</strong> 表示单位为英尺。</li>
</ul>
<p>我附近的人？（获得附近所有人的地址，定位！）通过半径来查询</p>
<pre><code class="language-bash">georadius key longitude latitude radius [m|km|ft|mi] [withcoord] [withdist] # 查询坐标半径的所有元素,后面可以跟很多参数，具体参数查看官方文档
1
</code></pre>
<blockquote>
<p>georadiusbymember</p>
</blockquote>
<p>这个命令和 <a href="http://www.redis.cn/commands/georadius.html">GEORADIUS</a> 命令一样， 都可以找出位于指定范围内的元素， 但是 <code>GEORADIUSBYMEMBER</code> 的中心点是由给定的位置元素决定的， 而不是像 <a href="http://www.redis.cn/commands/georadius.html">GEORADIUS</a> 那样， 使用输入的经度和纬度来决定中心点</p>
<p>指定成员的位置被用作查询的中心。</p>
<pre><code class="language-bash"># 找出指定元素周围的其他元素
georadiusbymember key member radius [m|km|ft|mi]
12
</code></pre>
<blockquote>
<p>geohash</p>
</blockquote>
<p>该命令将返回11个字符的Geohash字符串，所以没有精度Geohash，损失相比，使用内部52位表示。返回的geohashes具有以下特性：</p>
<ol>
<li>他们可以缩短从右边的字符。它将失去精度，但仍将指向同一地区。</li>
<li>它可以在 <code>geohash.org</code> 网站使用，网址 <code>http://geohash.org/&lt;geohash-string&gt;</code>。查询例子：http://geohash.org/sqdtr74hyu0.</li>
<li>与类似的前缀字符串是附近，但相反的是不正确的，这是可能的，用不同的前缀字符串附近。</li>
</ol>
<pre><code class="language-bash">geohash key member [member] # 获取指定元素坐标的hash表示,如果两个字符串越接近，离的越近
1
</code></pre>
<blockquote>
<p>geo底层的实现原理其实就是Zset！我们可以使用Zset命令操作geo</p>
</blockquote>
<pre><code class="language-bash">zrange key 0 -1 #查看所有元素

zrem key member # 删除指定元素
123
</code></pre>
<h2 id="hyperloglog">hyperloglog</h2>
<blockquote>
<p>什么是基数</p>
</blockquote>
<p>不重复的元素</p>
<p>Redis2.8.9版本就更新了Hyperloglog数据结构</p>
<p>Redis Hyperloglog 基数统计的算法</p>
<p>网页的 UA（一个访问一个网站多次，但是还是算做一个人）</p>
<p>传统的方式，set保存用户的id，然后就可以统计set中的元素数量作为标准判断，存在误差</p>
<p>这个方式如果保存大量的用户id，就会比较麻烦！我们的目的是计数，而不是保存用户id</p>
<p>优点：占用的内存是固定的，2^64不同元素，只需要废12kb的内存，如果要从内存角度来比较的话，Hyperloglog是首选</p>
<p>0.81%的错误率！统计UA的任务，是可以忽略不计的</p>
<pre><code class="language-bash">pfadd key member [member] # 添加元素

pfcount key # 查看个数

pfmerge destkey sourcekey [sourcekey] # 合并多个sourcekey

# 注意：不会有重复数据
1234567
</code></pre>
<h2 id="bitmap">bitmap</h2>
<blockquote>
<p>位存储</p>
</blockquote>
<p>统计疫情感染人数：0 1 0 1，统计用户信息，活跃，不活跃，打开，365打卡，两个状态的，都可以使用bitmaps来存储</p>
<p>bitmap 位图，也是一种数据结构！都是操作二进制位来进行记录，都只有 0 1两种状态</p>
<p>365天 = 365 bit， 1字节 = 8bit， 46个字节左右！</p>
<blockquote>
<p>测试</p>
</blockquote>
<figure data-type="image" tabindex="16"><img src="https://img-blog.csdnimg.cn/img_convert/bcf88e67b570869a0f08649b9693b441.png" alt="image-20201103211923600" loading="lazy"></figure>
<p>使用bitmaps记录周一到周日的打卡，查看某一天</p>
<pre><code class="language-bash">setbit key offset value # 设置

getbit key offset # 得到结果

# 统计操作，打卡的天数
bitcount key [start end] # 统计打卡天数
123456
</code></pre>
<h1 id="事务">事务</h1>
<p>MYSQL：ACID！</p>
<p>要么同时成功，要么同时失败：原子性，redis没有原子性</p>
<p>Redis单条命令保证原子性，但是事务不保证原子性</p>
<p>Redis事务本质：一组命令的集合,一个事务中的所有命令都会被序列化，在事务执行过程中，会按照顺序执行</p>
<p>一次性，顺序性，排他性！执行一系列的命令</p>
<p>Redis事务没有隔离级别的概念</p>
<p>所有的命令在事务中，并没有被直接被执行，只有发起执行命令的时候才会被执行！Exec</p>
<p>redis的事务</p>
<ul>
<li>开启事务(multi)</li>
<li>命令入队(……)</li>
<li>执行事务(exec)</li>
</ul>
<p>锁：redis可以实现乐观锁，watch</p>
<blockquote>
<p>正常执行事务</p>
</blockquote>
<pre><code class="language-bash">127.0.0.1:6379&gt; multi   # 开启事务
OK
127.0.0.1:6379&gt; set k1 v1
QUEUED
127.0.0.1:6379&gt; set k2 v2
QUEUED
127.0.0.1:6379&gt; get k2
QUEUED
127.0.0.1:6379&gt; set k3 v3
QUEUED
127.0.0.1:6379&gt; exec  # 执行事务
1) OK
2) OK
3) &quot;v2&quot;
4) OK
123456789101112131415
</code></pre>
<blockquote>
<p>放弃事务</p>
</blockquote>
<pre><code class="language-bash">127.0.0.1:6379&gt; multi     # 开启事务
OK
127.0.0.1:6379&gt; set k1 v1 
QUEUED
127.0.0.1:6379&gt; set k2 v2
QUEUED
127.0.0.1:6379&gt; set k4 v4
QUEUED
127.0.0.1:6379&gt; discard 	# 放弃事务	
OK
127.0.0.1:6379&gt; get k4	# 事务中的命令都不会被执行
(nil)

12345678910111213
</code></pre>
<blockquote>
<p>编译型异常（代码有问题，命令有错 ），事务中所有的命令都不会执行</p>
</blockquote>
<pre><code class="language-bash">127.0.0.1:6379&gt; multi
OK
127.0.0.1:6379&gt; set k1 v2 
QUEUED
127.0.0.1:6379&gt; set k2 v2
QUEUED
127.0.0.1:6379&gt; set k3 v3
QUEUED
127.0.0.1:6379&gt; getset k3     # 错误的命令
(error) ERR wrong number of arguments for 'getset' command
127.0.0.1:6379&gt; set k4 v4
QUEUED
127.0.0.1:6379&gt; set k5 v5
QUEUED
127.0.0.1:6379&gt; exec	# 执行事务报错
(error) EXECABORT Transaction discarded because of previous errors.
127.0.0.1:6379&gt; get k5	# 所有的命令都不会被执行
(nil)
123456789101112131415161718
</code></pre>
<blockquote>
<p>运行时异常（1/0），如果事务队列中存在语法性错误，那么执行命令的时候，其他命令是可以正常执行的，错误命令抛出异常</p>
</blockquote>
<pre><code class="language-bash">127.0.0.1:6379&gt; multi
OK
127.0.0.1:6379&gt; set k1 &quot;v1&quot;
QUEUED
127.0.0.1:6379&gt; incr k1
QUEUED
127.0.0.1:6379&gt; set k2 v2
QUEUED
127.0.0.1:6379&gt; set k3 v3
QUEUED
127.0.0.1:6379&gt; get k3
QUEUED
127.0.0.1:6379&gt; exec
1) OK
2) (error) ERR value is not an integer or out of range  # 虽然这条命令报错，但是依旧执行成功
3) OK
4) OK
5) &quot;v3&quot;

12345678910111213141516171819
</code></pre>
<blockquote>
<p>监控！Watch ：（ ，面试常问</p>
</blockquote>
<p>悲观锁：</p>
<ul>
<li>很悲观，认为什么时候都会出问题，无论做什么都会加锁</li>
</ul>
<p>乐观锁：</p>
<ul>
<li>很乐观，认为什么时候都不会出现问题，所以不会加锁！更新数据的时候去判断一下，在此期间是否有人修改过这个数据</li>
<li>获取version</li>
<li>更新的时候比较version</li>
</ul>
<blockquote>
<p>Redis测试监视测试</p>
</blockquote>
<p><strong>正常执行成功</strong></p>
<pre><code class="language-bash">watch key # 监视值

unwatch # 放弃监视

exec	# 执行事务

multi	# 开启事务


127.0.0.1:6379&gt; set money 100
OK
127.0.0.1:6379&gt; set out 0
OK
127.0.0.1:6379&gt; watch money 	# 监视money对象
OK
127.0.0.1:6379&gt; multi 			# 事务正常结束，数据期间没有发生变动，这个时候就正常执行成功
OK
127.0.0.1:6379&gt; decrby money 20
QUEUED
127.0.0.1:6379&gt; incrby out 20
QUEUED
127.0.0.1:6379&gt; exec
1) (integer) 80
2) (integer) 20
123456789101112131415161718192021222324
</code></pre>
<p><strong>执行失败</strong>，使用watch可以当redis的乐观锁操作</p>
<pre><code class="language-bash"># 主线程
127.0.0.1:6379&gt; watch money
OK
127.0.0.1:6379&gt; multi
OK
127.0.0.1:6379&gt; decrby money 10
QUEUED
127.0.0.1:6379&gt; incrby out 10
QUEUED
127.0.0.1:6379&gt; exec	# 执行之前，另外一个线程修改了值，就会导致执行失败
(nil)

# 线程2
127.0.0.1:6379&gt; get money
&quot;80&quot;
127.0.0.1:6379&gt; set money 1000
OK
1234567891011121314151617
</code></pre>
<figure data-type="image" tabindex="17"><img src="https://img-blog.csdnimg.cn/img_convert/f05dbe700a8856a77ea5a2c340250b7d.png" alt="image-20201104090306929" loading="lazy"></figure>
<p>如果修改失败，获取最新的值就好</p>
<h1 id="jedis">Jedis</h1>
<p>我们要用java来操作Redis</p>
<blockquote>
<p>什么是Jedis 是 Redis官方推荐的java连接开发工具！使用java操作redis中间件！如果要使用java操作redis，那么一定要对jedis十分的熟悉</p>
</blockquote>
<p>知其然并知其所以然</p>
<ol>
<li>代入Jedis的包</li>
</ol>
<pre><code class="language-xml">&lt;!-- https://mvnrepository.com/artifact/redis.clients/jedis --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;redis.clients&lt;/groupId&gt;
    &lt;artifactId&gt;jedis&lt;/artifactId&gt;
    &lt;version&gt;3.3.0&lt;/version&gt;
&lt;/dependency&gt;

&lt;!-- fastjson --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;com.alibaba&lt;/groupId&gt;
    &lt;artifactId&gt;fastjson&lt;/artifactId&gt;
    &lt;version&gt;1.2.74&lt;/version&gt;
&lt;/dependency&gt;
12345678910111213
</code></pre>
<ol>
<li>
<p>编码测试</p>
<ul>
<li>
<p>连接数据库</p>
<pre><code class="language-java">public class TestPing {
    public static void main(String[] args) {
//        1.new Jedis对象
        Jedis jedis = new Jedis(&quot;127.0.0.1&quot;,6379);
        // jedis所有的命令就是之前学习的所有指令,所以之间的指令很重要
        System.out.println(jedis.ping());
    }
}

// 输出，pong
12345678910
</code></pre>
</li>
<li>
<p>操作命令</p>
</li>
<li>
<p>断开连接</p>
</li>
</ul>
</li>
</ol>
<h2 id="常用api">常用API</h2>
<p>String</p>
<p>List</p>
<p>Set</p>
<p>Hash</p>
<p>Zset</p>
<blockquote>
<p>所有的API命令就是上面对应的指令，一个都没有变化</p>
</blockquote>
<h2 id="事务-2">事务</h2>
<pre><code class="language-java">public class TestTX {


    public static void main(String[] args) {
        Jedis jedis = new Jedis(&quot;127.0.0.1&quot;, 6379);
        JSONObject jsonObject = new JSONObject();
        jsonObject.put(&quot;hello&quot;, &quot;world&quot;);
        jsonObject.put(&quot;name&quot;, &quot;杨剑&quot;);
        jedis.flushDB();
        //开启事务
        Transaction multi = jedis.multi();
        String s = jsonObject.toJSONString();
        try {
            multi.set(&quot;user1&quot;, s);
            multi.set(&quot;user2&quot;, s);
            int i = 1/0;    //代码抛出异常，事务执行失败
            multi.exec();//执行事务
        } catch (Exception e) {
            multi.discard(); //放弃事务
            e.printStackTrace();
        } finally {
            System.out.println(jedis.get(&quot;user1&quot;));
            System.out.println(jedis.get(&quot;user2&quot;));
            jedis.close();
        }

    }
}
12345678910111213141516171819202122232425262728
</code></pre>
<h1 id="springboot整合">SpringBoot整合</h1>
<p>Springboot操作数据：spring-data jpa jdbc mongodb redis</p>
<p>SpringData也是和SpringBoot齐名的项目</p>
<p>说明：在springboot2.x之后，原来使用的jedis被替换成了lettuce？</p>
<p>jedis：采用的是直连，多个线程操作是不安全的，如果想要避免，使用jedis pool连接池！BIO</p>
<p>lettuce：采用netty，实例可以在多个线程中共享，不存在线程不安全的情况！可以减少线程数据了，更像NIO模式</p>
<blockquote>
<p>整合测试一下</p>
</blockquote>
<pre><code class="language-java">/*
 * Copyright 2012-2019 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the &quot;License&quot;);
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an &quot;AS IS&quot; BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package org.springframework.boot.autoconfigure.data.redis;

import java.net.UnknownHostException;

import org.springframework.boot.autoconfigure.EnableAutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Import;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.core.RedisOperations;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.core.StringRedisTemplate;

/**
 * {@link EnableAutoConfiguration Auto-configuration} for Spring Data's Redis support.
 *
 * @author Dave Syer
 * @author Andy Wilkinson
 * @author Christian Dupuis
 * @author Christoph Strobl
 * @author Phillip Webb
 * @author Eddú Meléndez
 * @author Stephane Nicoll
 * @author Marco Aust
 * @author Mark Paluch
 * @since 1.0.0
 */
@Configuration(proxyBeanMethods = false)
@ConditionalOnClass(RedisOperations.class)
@EnableConfigurationProperties(RedisProperties.class)
@Import({ LettuceConnectionConfiguration.class, JedisConnectionConfiguration.class })
public class RedisAutoConfiguration {

   @Bean
   @ConditionalOnMissingBean(name = &quot;redisTemplate&quot;)   //我们可以自己定义一个RedisTemplate来替换默认的
    // 默认的template没有过多的设置，redis对象都是需要序列化的
    //两个泛型都是object类型，我们后面使用需要强制转换&lt;String,Object&gt;
   public RedisTemplate&lt;Object, Object&gt; redisTemplate(RedisConnectionFactory redisConnectionFactory)
         throws UnknownHostException {
      RedisTemplate&lt;Object, Object&gt; template = new RedisTemplate&lt;&gt;();
      template.setConnectionFactory(redisConnectionFactory);
      return template;
   }

   @Bean
   @ConditionalOnMissingBean	//由于String类型是Redis中最常使用的类型，所以单独提出来了一个bean
   public StringRedisTemplate stringRedisTemplate(RedisConnectionFactory redisConnectionFactory)
         throws UnknownHostException {
      StringRedisTemplate template = new StringRedisTemplate();
      template.setConnectionFactory(redisConnectionFactory);
      return template;
   }

}
12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273
</code></pre>
<ol>
<li>
<p>导入连接</p>
<pre><code class="language-xml">&lt;dependency&gt;
   &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;65	
   &lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;
&lt;/dependency&gt;
1234
</code></pre>
</li>
<li>
<p>配置连接</p>
<pre><code class="language-properties"># springboot所有的配置类，都有一个自动配置类 RedisAutoConfiguration
# 自动配置类都会绑定一个properties配置文件 RedisProperties

# 配置redis
spring.redis.host=localhost
spring.redis.port=6379
123456
</code></pre>
</li>
<li>
<p>测试！</p>
<pre><code class="language-java">@SpringBootTest
class Redis02SpringbootApplicationTests {
   @Autowired
   private RedisTemplate redisTemplate;
   @Test
   void contextLoads() {
      //redisTemplate 操作不同的数据类型，api和我们的指令是一样的
      // 操作字符串，类似String类型
//    redisTemplate.opsForValue();
//    // 操作List
//    redisTemplate.opsForList();
//    //opsforset
//    redisTemplate.opsForSet();
//    //opsforhash
//    redisTemplate.opsForHash();
//    //地图
//    redisTemplate.opsForGeo();
//    //Zset
//    redisTemplate.opsForZSet();
//    //基数：hyperloglog
//    redisTemplate.opsForHyperLogLog();

      //除了基本的操作，常用的方法都可以直接使用,事务和基本的crud

      //获取redis连接对象
//    RedisConnection connection = redisTemplate.getConnectionFactory().getConnection();

      redisTemplate.opsForValue().set(&quot;name&quot;,&quot;yangjian&quot;);
      System.out.println(redisTemplate.opsForValue().get(&quot;name&quot;));
   }

}
1234567891011121314151617181920212223242526272829303132
</code></pre>
</li>
</ol>
<h2 id="序列化配置">序列化配置</h2>
<figure data-type="image" tabindex="18"><img src="https://img-blog.csdnimg.cn/img_convert/5564164fe50d39b6911766a0ea4b8069.png" alt="image-20201104153459661" loading="lazy"></figure>
<h2 id="自定义redistemplate拿过来可以直接用">自定义RedisTemplate(拿过来可以直接用)</h2>
<pre><code class="language-java">package com.yang.config;

import com.fasterxml.jackson.annotation.JsonAutoDetect;
import com.fasterxml.jackson.annotation.PropertyAccessor;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.serializer.Jackson2JsonRedisSerializer;
import org.springframework.data.redis.serializer.StringRedisSerializer;

import java.net.UnknownHostException;

@Configuration
public class RedisConfig {

    //编写自己的RedisTemplate
    //自己定义了一个RedisTemplate

    //这是一个固定模板，拿去可以直接使用
    @Bean
    @ConditionalOnMissingBean(name = &quot;redisTemplate&quot;)
    public RedisTemplate&lt;String, Object&gt; redisTemplate(RedisConnectionFactory redisConnectionFactory)
            throws UnknownHostException {
        //为了我们自己开发方便，一般直接使用&lt;String,Object&gt;
        RedisTemplate&lt;String, Object&gt; template = new RedisTemplate&lt;&gt;();
        template.setConnectionFactory(redisConnectionFactory);
        Jackson2JsonRedisSerializer&lt;Object&gt; objectJackson2JsonRedisSerializer = new Jackson2JsonRedisSerializer&lt;Object&gt;(Object.class);
        //json序列化配置
        ObjectMapper om = new ObjectMapper();
        om.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);
        om.enableDefaultTyping(ObjectMapper.DefaultTyping.NON_FINAL);
        objectJackson2JsonRedisSerializer.setObjectMapper(om);
        //String的序列化
        StringRedisSerializer stringRedisSerializer = new StringRedisSerializer();
        //key采用String序列化方式
        template.setKeySerializer(stringRedisSerializer);
        //hash的key也采用String的序列化方式
        template.setHashKeySerializer(stringRedisSerializer);
        //value的序列化方式采用jackson
        template.setValueSerializer(objectJackson2JsonRedisSerializer);
        //hash的value序列化方式采用jackson
        template.setHashValueSerializer(objectJackson2JsonRedisSerializer);
        template.afterPropertiesSet();

        return template;
    }
}
1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950
</code></pre>
<h2 id="redisutil工具类">RedisUtil工具类</h2>
<pre><code class="language-java">package com.yang.utils;

import org.springframework.data.redis.connection.DataType;
import org.springframework.data.redis.core.Cursor;
import org.springframework.data.redis.core.ScanOptions;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.data.redis.core.ZSetOperations.TypedTuple;
import org.springframework.stereotype.Component;

import java.util.Collection;
import java.util.Date;
import java.util.List;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Set;
import java.util.concurrent.TimeUnit;

/**
 * Redis工具类
 *
 * @author WangFan
 * @date 2018-02-24 下午03:09:50
 * @version 1.1 (GitHub文档: https://github.com/whvcse/RedisUtil )
 */
@Component
public class RedisUtil {
    private StringRedisTemplate redisTemplate;

    public void setRedisTemplate(StringRedisTemplate redisTemplate) {
        this.redisTemplate = redisTemplate;
    }

    public StringRedisTemplate getRedisTemplate() {
        return this.redisTemplate;
    }

    /** -------------------key相关操作--------------------- */

    /**
     * 删除key
     *
     * @param key
     */
    public void delete(String key) {
        redisTemplate.delete(key);
    }

    /**
     * 批量删除key
     *
     * @param keys
     */
    public void delete(Collection&lt;String&gt; keys) {
        redisTemplate.delete(keys);
    }

    /**
     * 序列化key
     *
     * @param key
     * @return
     */
    public byte[] dump(String key) {
        return redisTemplate.dump(key);
    }

    /**
     * 是否存在key
     *
     * @param key
     * @return
     */
    public Boolean hasKey(String key) {
        return redisTemplate.hasKey(key);
    }

    /**
     * 设置过期时间
     *
     * @param key
     * @param timeout
     * @param unit
     * @return
     */
    public Boolean expire(String key, long timeout, TimeUnit unit) {
        return redisTemplate.expire(key, timeout, unit);
    }

    /**
     * 设置过期时间
     *
     * @param key
     * @param date
     * @return
     */
    public Boolean expireAt(String key, Date date) {
        return redisTemplate.expireAt(key, date);
    }

    /**
     * 查找匹配的key
     *
     * @param pattern
     * @return
     */
    public Set&lt;String&gt; keys(String pattern) {
        return redisTemplate.keys(pattern);
    }

    /**
     * 将当前数据库的 key 移动到给定的数据库 db 当中
     *
     * @param key
     * @param dbIndex
     * @return
     */
    public Boolean move(String key, int dbIndex) {
        return redisTemplate.move(key, dbIndex);
    }

    /**
     * 移除 key 的过期时间，key 将持久保持
     *
     * @param key
     * @return
     */
    public Boolean persist(String key) {
        return redisTemplate.persist(key);
    }

    /**
     * 返回 key 的剩余的过期时间
     *
     * @param key
     * @param unit
     * @return
     */
    public Long getExpire(String key, TimeUnit unit) {
        return redisTemplate.getExpire(key, unit);
    }

    /**
     * 返回 key 的剩余的过期时间
     *
     * @param key
     * @return
     */
    public Long getExpire(String key) {
        return redisTemplate.getExpire(key);
    }

    /**
     * 从当前数据库中随机返回一个 key
     *
     * @return
     */
    public String randomKey() {
        return redisTemplate.randomKey();
    }

    /**
     * 修改 key 的名称
     *
     * @param oldKey
     * @param newKey
     */
    public void rename(String oldKey, String newKey) {
        redisTemplate.rename(oldKey, newKey);
    }

    /**
     * 仅当 newkey 不存在时，将 oldKey 改名为 newkey
     *
     * @param oldKey
     * @param newKey
     * @return
     */
    public Boolean renameIfAbsent(String oldKey, String newKey) {
        return redisTemplate.renameIfAbsent(oldKey, newKey);
    }

    /**
     * 返回 key 所储存的值的类型
     *
     * @param key
     * @return
     */
    public DataType type(String key) {
        return redisTemplate.type(key);
    }

    /** -------------------string相关操作--------------------- */

    /**
     * 设置指定 key 的值
     * @param key
     * @param value
     */
    public void set(String key, String value) {
        redisTemplate.opsForValue().set(key, value);
    }

    /**
     * 获取指定 key 的值
     * @param key
     * @return
     */
    public String get(String key) {
        return redisTemplate.opsForValue().get(key);
    }

    /**
     * 返回 key 中字符串值的子字符
     * @param key
     * @param start
     * @param end
     * @return
     */
    public String getRange(String key, long start, long end) {
        return redisTemplate.opsForValue().get(key, start, end);
    }

    /**
     * 将给定 key 的值设为 value ，并返回 key 的旧值(old value)
     *
     * @param key
     * @param value
     * @return
     */
    public String getAndSet(String key, String value) {
        return redisTemplate.opsForValue().getAndSet(key, value);
    }

    /**
     * 对 key 所储存的字符串值，获取指定偏移量上的位(bit)
     *
     * @param key
     * @param offset
     * @return
     */
    public Boolean getBit(String key, long offset) {
        return redisTemplate.opsForValue().getBit(key, offset);
    }

    /**
     * 批量获取
     *
     * @param keys
     * @return
     */
    public List&lt;String&gt; multiGet(Collection&lt;String&gt; keys) {
        return redisTemplate.opsForValue().multiGet(keys);
    }

    /**
     * 设置ASCII码, 字符串'a'的ASCII码是97, 转为二进制是'01100001', 此方法是将二进制第offset位值变为value
     *
     * @param key
     * @param postion
     *            位置
     * @param value
     *            值,true为1, false为0
     * @return
     */
    public boolean setBit(String key, long offset, boolean value) {
        return redisTemplate.opsForValue().setBit(key, offset, value);
    }

    /**
     * 将值 value 关联到 key ，并将 key 的过期时间设为 timeout
     *
     * @param key
     * @param value
     * @param timeout
     *            过期时间
     * @param unit
     *            时间单位, 天:TimeUnit.DAYS 小时:TimeUnit.HOURS 分钟:TimeUnit.MINUTES
     *            秒:TimeUnit.SECONDS 毫秒:TimeUnit.MILLISECONDS
     */
    public void setEx(String key, String value, long timeout, TimeUnit unit) {
        redisTemplate.opsForValue().set(key, value, timeout, unit);
    }

    /**
     * 只有在 key 不存在时设置 key 的值
     *
     * @param key
     * @param value
     * @return 之前已经存在返回false,不存在返回true
     */
    public boolean setIfAbsent(String key, String value) {
        return redisTemplate.opsForValue().setIfAbsent(key, value);
    }

    /**
     * 用 value 参数覆写给定 key 所储存的字符串值，从偏移量 offset 开始
     *
     * @param key
     * @param value
     * @param offset
     *            从指定位置开始覆写
     */
    public void setRange(String key, String value, long offset) {
        redisTemplate.opsForValue().set(key, value, offset);
    }

    /**
     * 获取字符串的长度
     *
     * @param key
     * @return
     */
    public Long size(String key) {
        return redisTemplate.opsForValue().size(key);
    }

    /**
     * 批量添加
     *
     * @param maps
     */
    public void multiSet(Map&lt;String, String&gt; maps) {
        redisTemplate.opsForValue().multiSet(maps);
    }

    /**
     * 同时设置一个或多个 key-value 对，当且仅当所有给定 key 都不存在
     *
     * @param maps
     * @return 之前已经存在返回false,不存在返回true
     */
    public boolean multiSetIfAbsent(Map&lt;String, String&gt; maps) {
        return redisTemplate.opsForValue().multiSetIfAbsent(maps);
    }

    /**
     * 增加(自增长), 负数则为自减
     *
     * @param key
     * @param value
     * @return
     */
    public Long incrBy(String key, long increment) {
        return redisTemplate.opsForValue().increment(key, increment);
    }

    /**
     *
     * @param key
     * @param value
     * @return
     */
    public Double incrByFloat(String key, double increment) {
        return redisTemplate.opsForValue().increment(key, increment);
    }

    /**
     * 追加到末尾
     *
     * @param key
     * @param value
     * @return
     */
    public Integer append(String key, String value) {
        return redisTemplate.opsForValue().append(key, value);
    }

    /** -------------------hash相关操作------------------------- */

    /**
     * 获取存储在哈希表中指定字段的值
     *
     * @param key
     * @param field
     * @return
     */
    public Object hGet(String key, String field) {
        return redisTemplate.opsForHash().get(key, field);
    }

    /**
     * 获取所有给定字段的值
     *
     * @param key
     * @return
     */
    public Map&lt;Object, Object&gt; hGetAll(String key) {
        return redisTemplate.opsForHash().entries(key);
    }

    /**
     * 获取所有给定字段的值
     *
     * @param key
     * @param fields
     * @return
     */
    public List&lt;Object&gt; hMultiGet(String key, Collection&lt;Object&gt; fields) {
        return redisTemplate.opsForHash().multiGet(key, fields);
    }

    public void hPut(String key, String hashKey, String value) {
        redisTemplate.opsForHash().put(key, hashKey, value);
    }

    public void hPutAll(String key, Map&lt;String, String&gt; maps) {
        redisTemplate.opsForHash().putAll(key, maps);
    }

    /**
     * 仅当hashKey不存在时才设置
     *
     * @param key
     * @param hashKey
     * @param value
     * @return
     */
    public Boolean hPutIfAbsent(String key, String hashKey, String value) {
        return redisTemplate.opsForHash().putIfAbsent(key, hashKey, value);
    }

    /**
     * 删除一个或多个哈希表字段
     *
     * @param key
     * @param fields
     * @return
     */
    public Long hDelete(String key, Object... fields) {
        return redisTemplate.opsForHash().delete(key, fields);
    }

    /**
     * 查看哈希表 key 中，指定的字段是否存在
     *
     * @param key
     * @param field
     * @return
     */
    public boolean hExists(String key, String field) {
        return redisTemplate.opsForHash().hasKey(key, field);
    }

    /**
     * 为哈希表 key 中的指定字段的整数值加上增量 increment
     *
     * @param key
     * @param field
     * @param increment
     * @return
     */
    public Long hIncrBy(String key, Object field, long increment) {
        return redisTemplate.opsForHash().increment(key, field, increment);
    }

    /**
     * 为哈希表 key 中的指定字段的整数值加上增量 increment
     *
     * @param key
     * @param field
     * @param delta
     * @return
     */
    public Double hIncrByFloat(String key, Object field, double delta) {
        return redisTemplate.opsForHash().increment(key, field, delta);
    }

    /**
     * 获取所有哈希表中的字段
     *
     * @param key
     * @return
     */
    public Set&lt;Object&gt; hKeys(String key) {
        return redisTemplate.opsForHash().keys(key);
    }

    /**
     * 获取哈希表中字段的数量
     *
     * @param key
     * @return
     */
    public Long hSize(String key) {
        return redisTemplate.opsForHash().size(key);
    }

    /**
     * 获取哈希表中所有值
     *
     * @param key
     * @return
     */
    public List&lt;Object&gt; hValues(String key) {
        return redisTemplate.opsForHash().values(key);
    }

    /**
     * 迭代哈希表中的键值对
     *
     * @param key
     * @param options
     * @return
     */
    public Cursor&lt;Entry&lt;Object, Object&gt;&gt; hScan(String key, ScanOptions options) {
        return redisTemplate.opsForHash().scan(key, options);
    }

    /** ------------------------list相关操作---------------------------- */

    /**
     * 通过索引获取列表中的元素
     *
     * @param key
     * @param index
     * @return
     */
    public String lIndex(String key, long index) {
        return redisTemplate.opsForList().index(key, index);
    }

    /**
     * 获取列表指定范围内的元素
     *
     * @param key
     * @param start
     *            开始位置, 0是开始位置
     * @param end
     *            结束位置, -1返回所有
     * @return
     */
    public List&lt;String&gt; lRange(String key, long start, long end) {
        return redisTemplate.opsForList().range(key, start, end);
    }

    /**
     * 存储在list头部
     *
     * @param key
     * @param value
     * @return
     */
    public Long lLeftPush(String key, String value) {
        return redisTemplate.opsForList().leftPush(key, value);
    }

    /**
     *
     * @param key
     * @param value
     * @return
     */
    public Long lLeftPushAll(String key, String... value) {
        return redisTemplate.opsForList().leftPushAll(key, value);
    }

    /**
     *
     * @param key
     * @param value
     * @return
     */
    public Long lLeftPushAll(String key, Collection&lt;String&gt; value) {
        return redisTemplate.opsForList().leftPushAll(key, value);
    }

    /**
     * 当list存在的时候才加入
     *
     * @param key
     * @param value
     * @return
     */
    public Long lLeftPushIfPresent(String key, String value) {
        return redisTemplate.opsForList().leftPushIfPresent(key, value);
    }

    /**
     * 如果pivot存在,再pivot前面添加
     *
     * @param key
     * @param pivot
     * @param value
     * @return
     */
    public Long lLeftPush(String key, String pivot, String value) {
        return redisTemplate.opsForList().leftPush(key, pivot, value);
    }

    /**
     *
     * @param key
     * @param value
     * @return
     */
    public Long lRightPush(String key, String value) {
        return redisTemplate.opsForList().rightPush(key, value);
    }

    /**
     *
     * @param key
     * @param value
     * @return
     */
    public Long lRightPushAll(String key, String... value) {
        return redisTemplate.opsForList().rightPushAll(key, value);
    }

    /**
     *
     * @param key
     * @param value
     * @return
     */
    public Long lRightPushAll(String key, Collection&lt;String&gt; value) {
        return redisTemplate.opsForList().rightPushAll(key, value);
    }

    /**
     * 为已存在的列表添加值
     *
     * @param key
     * @param value
     * @return
     */
    public Long lRightPushIfPresent(String key, String value) {
        return redisTemplate.opsForList().rightPushIfPresent(key, value);
    }

    /**
     * 在pivot元素的右边添加值
     *
     * @param key
     * @param pivot
     * @param value
     * @return
     */
    public Long lRightPush(String key, String pivot, String value) {
        return redisTemplate.opsForList().rightPush(key, pivot, value);
    }

    /**
     * 通过索引设置列表元素的值
     *
     * @param key
     * @param index
     *            位置
     * @param value
     */
    public void lSet(String key, long index, String value) {
        redisTemplate.opsForList().set(key, index, value);
    }

    /**
     * 移出并获取列表的第一个元素
     *
     * @param key
     * @return 删除的元素
     */
    public String lLeftPop(String key) {
        return redisTemplate.opsForList().leftPop(key);
    }

    /**
     * 移出并获取列表的第一个元素， 如果列表没有元素会阻塞列表直到等待超时或发现可弹出元素为止
     *
     * @param key
     * @param timeout
     *            等待时间
     * @param unit
     *            时间单位
     * @return
     */
    public String lBLeftPop(String key, long timeout, TimeUnit unit) {
        return redisTemplate.opsForList().leftPop(key, timeout, unit);
    }

    /**
     * 移除并获取列表最后一个元素
     *
     * @param key
     * @return 删除的元素
     */
    public String lRightPop(String key) {
        return redisTemplate.opsForList().rightPop(key);
    }

    /**
     * 移出并获取列表的最后一个元素， 如果列表没有元素会阻塞列表直到等待超时或发现可弹出元素为止
     *
     * @param key
     * @param timeout
     *            等待时间
     * @param unit
     *            时间单位
     * @return
     */
    public String lBRightPop(String key, long timeout, TimeUnit unit) {
        return redisTemplate.opsForList().rightPop(key, timeout, unit);
    }

    /**
     * 移除列表的最后一个元素，并将该元素添加到另一个列表并返回
     *
     * @param sourceKey
     * @param destinationKey
     * @return
     */
    public String lRightPopAndLeftPush(String sourceKey, String destinationKey) {
        return redisTemplate.opsForList().rightPopAndLeftPush(sourceKey,
                destinationKey);
    }

    /**
     * 从列表中弹出一个值，将弹出的元素插入到另外一个列表中并返回它； 如果列表没有元素会阻塞列表直到等待超时或发现可弹出元素为止
     *
     * @param sourceKey
     * @param destinationKey
     * @param timeout
     * @param unit
     * @return
     */
    public String lBRightPopAndLeftPush(String sourceKey, String destinationKey,
                                        long timeout, TimeUnit unit) {
        return redisTemplate.opsForList().rightPopAndLeftPush(sourceKey,
                destinationKey, timeout, unit);
    }

    /**
     * 删除集合中值等于value得元素
     *
     * @param key
     * @param index
     *            index=0, 删除所有值等于value的元素; index&gt;0, 从头部开始删除第一个值等于value的元素;
     *            index&lt;0, 从尾部开始删除第一个值等于value的元素;
     * @param value
     * @return
     */
    public Long lRemove(String key, long index, String value) {
        return redisTemplate.opsForList().remove(key, index, value);
    }

    /**
     * 裁剪list
     *
     * @param key
     * @param start
     * @param end
     */
    public void lTrim(String key, long start, long end) {
        redisTemplate.opsForList().trim(key, start, end);
    }

    /**
     * 获取列表长度
     *
     * @param key
     * @return
     */
    public Long lLen(String key) {
        return redisTemplate.opsForList().size(key);
    }

    /** --------------------set相关操作-------------------------- */

    /**
     * set添加元素
     *
     * @param key
     * @param values
     * @return
     */
    public Long sAdd(String key, String... values) {
        return redisTemplate.opsForSet().add(key, values);
    }

    /**
     * set移除元素
     *
     * @param key
     * @param values
     * @return
     */
    public Long sRemove(String key, Object... values) {
        return redisTemplate.opsForSet().remove(key, values);
    }

    /**
     * 移除并返回集合的一个随机元素
     *
     * @param key
     * @return
     */
    public String sPop(String key) {
        return redisTemplate.opsForSet().pop(key);
    }

    /**
     * 将元素value从一个集合移到另一个集合
     *
     * @param key
     * @param value
     * @param destKey
     * @return
     */
    public Boolean sMove(String key, String value, String destKey) {
        return redisTemplate.opsForSet().move(key, value, destKey);
    }

    /**
     * 获取集合的大小
     *
     * @param key
     * @return
     */
    public Long sSize(String key) {
        return redisTemplate.opsForSet().size(key);
    }

    /**
     * 判断集合是否包含value
     *
     * @param key
     * @param value
     * @return
     */
    public Boolean sIsMember(String key, Object value) {
        return redisTemplate.opsForSet().isMember(key, value);
    }

    /**
     * 获取两个集合的交集
     *
     * @param key
     * @param otherKey
     * @return
     */
    public Set&lt;String&gt; sIntersect(String key, String otherKey) {
        return redisTemplate.opsForSet().intersect(key, otherKey);
    }

    /**
     * 获取key集合与多个集合的交集
     *
     * @param key
     * @param otherKeys
     * @return
     */
    public Set&lt;String&gt; sIntersect(String key, Collection&lt;String&gt; otherKeys) {
        return redisTemplate.opsForSet().intersect(key, otherKeys);
    }

    /**
     * key集合与otherKey集合的交集存储到destKey集合中
     *
     * @param key
     * @param otherKey
     * @param destKey
     * @return
     */
    public Long sIntersectAndStore(String key, String otherKey, String destKey) {
        return redisTemplate.opsForSet().intersectAndStore(key, otherKey,
                destKey);
    }

    /**
     * key集合与多个集合的交集存储到destKey集合中
     *
     * @param key
     * @param otherKeys
     * @param destKey
     * @return
     */
    public Long sIntersectAndStore(String key, Collection&lt;String&gt; otherKeys,
                                   String destKey) {
        return redisTemplate.opsForSet().intersectAndStore(key, otherKeys,
                destKey);
    }

    /**
     * 获取两个集合的并集
     *
     * @param key
     * @param otherKeys
     * @return
     */
    public Set&lt;String&gt; sUnion(String key, String otherKeys) {
        return redisTemplate.opsForSet().union(key, otherKeys);
    }

    /**
     * 获取key集合与多个集合的并集
     *
     * @param key
     * @param otherKeys
     * @return
     */
    public Set&lt;String&gt; sUnion(String key, Collection&lt;String&gt; otherKeys) {
        return redisTemplate.opsForSet().union(key, otherKeys);
    }

    /**
     * key集合与otherKey集合的并集存储到destKey中
     *
     * @param key
     * @param otherKey
     * @param destKey
     * @return
     */
    public Long sUnionAndStore(String key, String otherKey, String destKey) {
        return redisTemplate.opsForSet().unionAndStore(key, otherKey, destKey);
    }

    /**
     * key集合与多个集合的并集存储到destKey中
     *
     * @param key
     * @param otherKeys
     * @param destKey
     * @return
     */
    public Long sUnionAndStore(String key, Collection&lt;String&gt; otherKeys,
                               String destKey) {
        return redisTemplate.opsForSet().unionAndStore(key, otherKeys, destKey);
    }

    /**
     * 获取两个集合的差集
     *
     * @param key
     * @param otherKey
     * @return
     */
    public Set&lt;String&gt; sDifference(String key, String otherKey) {
        return redisTemplate.opsForSet().difference(key, otherKey);
    }

    /**
     * 获取key集合与多个集合的差集
     *
     * @param key
     * @param otherKeys
     * @return
     */
    public Set&lt;String&gt; sDifference(String key, Collection&lt;String&gt; otherKeys) {
        return redisTemplate.opsForSet().difference(key, otherKeys);
    }

    /**
     * key集合与otherKey集合的差集存储到destKey中
     *
     * @param key
     * @param otherKey
     * @param destKey
     * @return
     */
    public Long sDifference(String key, String otherKey, String destKey) {
        return redisTemplate.opsForSet().differenceAndStore(key, otherKey,
                destKey);
    }

    /**
     * key集合与多个集合的差集存储到destKey中
     *
     * @param key
     * @param otherKeys
     * @param destKey
     * @return
     */
    public Long sDifference(String key, Collection&lt;String&gt; otherKeys,
                            String destKey) {
        return redisTemplate.opsForSet().differenceAndStore(key, otherKeys,
                destKey);
    }

    /**
     * 获取集合所有元素
     *
     * @param key
     * @param otherKeys
     * @param destKey
     * @return
     */
    public Set&lt;String&gt; setMembers(String key) {
        return redisTemplate.opsForSet().members(key);
    }

    /**
     * 随机获取集合中的一个元素
     *
     * @param key
     * @return
     */
    public String sRandomMember(String key) {
        return redisTemplate.opsForSet().randomMember(key);
    }

    /**
     * 随机获取集合中count个元素
     *
     * @param key
     * @param count
     * @return
     */
    public List&lt;String&gt; sRandomMembers(String key, long count) {
        return redisTemplate.opsForSet().randomMembers(key, count);
    }

    /**
     * 随机获取集合中count个元素并且去除重复的
     *
     * @param key
     * @param count
     * @return
     */
    public Set&lt;String&gt; sDistinctRandomMembers(String key, long count) {
        return redisTemplate.opsForSet().distinctRandomMembers(key, count);
    }

    /**
     *
     * @param key
     * @param options
     * @return
     */
    public Cursor&lt;String&gt; sScan(String key, ScanOptions options) {
        return redisTemplate.opsForSet().scan(key, options);
    }

    /**------------------zSet相关操作--------------------------------*/

    /**
     * 添加元素,有序集合是按照元素的score值由小到大排列
     *
     * @param key
     * @param value
     * @param score
     * @return
     */
    public Boolean zAdd(String key, String value, double score) {
        return redisTemplate.opsForZSet().add(key, value, score);
    }

    /**
     *
     * @param key
     * @param values
     * @return
     */
    public Long zAdd(String key, Set&lt;TypedTuple&lt;String&gt;&gt; values) {
        return redisTemplate.opsForZSet().add(key, values);
    }

    /**
     *
     * @param key
     * @param values
     * @return
     */
    public Long zRemove(String key, Object... values) {
        return redisTemplate.opsForZSet().remove(key, values);
    }

    /**
     * 增加元素的score值，并返回增加后的值
     *
     * @param key
     * @param value
     * @param delta
     * @return
     */
    public Double zIncrementScore(String key, String value, double delta) {
        return redisTemplate.opsForZSet().incrementScore(key, value, delta);
    }

    /**
     * 返回元素在集合的排名,有序集合是按照元素的score值由小到大排列
     *
     * @param key
     * @param value
     * @return 0表示第一位
     */
    public Long zRank(String key, Object value) {
        return redisTemplate.opsForZSet().rank(key, value);
    }

    /**
     * 返回元素在集合的排名,按元素的score值由大到小排列
     *
     * @param key
     * @param value
     * @return
     */
    public Long zReverseRank(String key, Object value) {
        return redisTemplate.opsForZSet().reverseRank(key, value);
    }

    /**
     * 获取集合的元素, 从小到大排序
     *
     * @param key
     * @param start
     *            开始位置
     * @param end
     *            结束位置, -1查询所有
     * @return
     */
    public Set&lt;String&gt; zRange(String key, long start, long end) {
        return redisTemplate.opsForZSet().range(key, start, end);
    }

    /**
     * 获取集合元素, 并且把score值也获取
     *
     * @param key
     * @param start
     * @param end
     * @return
     */
    public Set&lt;TypedTuple&lt;String&gt;&gt; zRangeWithScores(String key, long start,
                                                    long end) {
        return redisTemplate.opsForZSet().rangeWithScores(key, start, end);
    }

    /**
     * 根据Score值查询集合元素
     *
     * @param key
     * @param min
     *            最小值
     * @param max
     *            最大值
     * @return
     */
    public Set&lt;String&gt; zRangeByScore(String key, double min, double max) {
        return redisTemplate.opsForZSet().rangeByScore(key, min, max);
    }

    /**
     * 根据Score值查询集合元素, 从小到大排序
     *
     * @param key
     * @param min
     *            最小值
     * @param max
     *            最大值
     * @return
     */
    public Set&lt;TypedTuple&lt;String&gt;&gt; zRangeByScoreWithScores(String key,
                                                           double min, double max) {
        return redisTemplate.opsForZSet().rangeByScoreWithScores(key, min, max);
    }

    /**
     *
     * @param key
     * @param min
     * @param max
     * @param start
     * @param end
     * @return
     */
    public Set&lt;TypedTuple&lt;String&gt;&gt; zRangeByScoreWithScores(String key,
                                                           double min, double max, long start, long end) {
        return redisTemplate.opsForZSet().rangeByScoreWithScores(key, min, max,
                start, end);
    }

    /**
     * 获取集合的元素, 从大到小排序
     *
     * @param key
     * @param start
     * @param end
     * @return
     */
    public Set&lt;String&gt; zReverseRange(String key, long start, long end) {
        return redisTemplate.opsForZSet().reverseRange(key, start, end);
    }

    /**
     * 获取集合的元素, 从大到小排序, 并返回score值
     *
     * @param key
     * @param start
     * @param end
     * @return
     */
    public Set&lt;TypedTuple&lt;String&gt;&gt; zReverseRangeWithScores(String key,
                                                           long start, long end) {
        return redisTemplate.opsForZSet().reverseRangeWithScores(key, start,
                end);
    }

    /**
     * 根据Score值查询集合元素, 从大到小排序
     *
     * @param key
     * @param min
     * @param max
     * @return
     */
    public Set&lt;String&gt; zReverseRangeByScore(String key, double min,
                                            double max) {
        return redisTemplate.opsForZSet().reverseRangeByScore(key, min, max);
    }

    /**
     * 根据Score值查询集合元素, 从大到小排序
     *
     * @param key
     * @param min
     * @param max
     * @return
     */
    public Set&lt;TypedTuple&lt;String&gt;&gt; zReverseRangeByScoreWithScores(
            String key, double min, double max) {
        return redisTemplate.opsForZSet().reverseRangeByScoreWithScores(key,
                min, max);
    }

    /**
     *
     * @param key
     * @param min
     * @param max
     * @param start
     * @param end
     * @return
     */
    public Set&lt;String&gt; zReverseRangeByScore(String key, double min,
                                            double max, long start, long end) {
        return redisTemplate.opsForZSet().reverseRangeByScore(key, min, max,
                start, end);
    }

    /**
     * 根据score值获取集合元素数量
     *
     * @param key
     * @param min
     * @param max
     * @return
     */
    public Long zCount(String key, double min, double max) {
        return redisTemplate.opsForZSet().count(key, min, max);
    }

    /**
     * 获取集合大小
     *
     * @param key
     * @return
     */
    public Long zSize(String key) {
        return redisTemplate.opsForZSet().size(key);
    }

    /**
     * 获取集合大小
     *
     * @param key
     * @return
     */
    public Long zZCard(String key) {
        return redisTemplate.opsForZSet().zCard(key);
    }

    /**
     * 获取集合中value元素的score值
     *
     * @param key
     * @param value
     * @return
     */
    public Double zScore(String key, Object value) {
        return redisTemplate.opsForZSet().score(key, value);
    }

    /**
     * 移除指定索引位置的成员
     *
     * @param key
     * @param start
     * @param end
     * @return
     */
    public Long zRemoveRange(String key, long start, long end) {
        return redisTemplate.opsForZSet().removeRange(key, start, end);
    }

    /**
     * 根据指定的score值的范围来移除成员
     *
     * @param key
     * @param min
     * @param max
     * @return
     */
    public Long zRemoveRangeByScore(String key, double min, double max) {
        return redisTemplate.opsForZSet().removeRangeByScore(key, min, max);
    }

    /**
     * 获取key和otherKey的并集并存储在destKey中
     *
     * @param key
     * @param otherKey
     * @param destKey
     * @return
     */
    public Long zUnionAndStore(String key, String otherKey, String destKey) {
        return redisTemplate.opsForZSet().unionAndStore(key, otherKey, destKey);
    }

    /**
     *
     * @param key
     * @param otherKeys
     * @param destKey
     * @return
     */
    public Long zUnionAndStore(String key, Collection&lt;String&gt; otherKeys,
                               String destKey) {
        return redisTemplate.opsForZSet()
                .unionAndStore(key, otherKeys, destKey);
    }

    /**
     * 交集
     *
     * @param key
     * @param otherKey
     * @param destKey
     * @return
     */
    public Long zIntersectAndStore(String key, String otherKey,
                                   String destKey) {
        return redisTemplate.opsForZSet().intersectAndStore(key, otherKey,
                destKey);
    }

    /**
     * 交集
     *
     * @param key
     * @param otherKeys
     * @param destKey
     * @return
     */
    public Long zIntersectAndStore(String key, Collection&lt;String&gt; otherKeys,
                                   String destKey) {
        return redisTemplate.opsForZSet().intersectAndStore(key, otherKeys,
                destKey);
    }

    /**
     *
     * @param key
     * @param options
     * @return
     */
    public Cursor&lt;TypedTuple&lt;String&gt;&gt; zScan(String key, ScanOptions options) {
        return redisTemplate.opsForZSet().scan(key, options);
    }
}

</code></pre>
<p>所有的Redis操作，其实对于java开发人员来说十分的简单，更重要的是理解思想和应用场景</p>
<h1 id="redisconf详解">Redis.conf详解</h1>
<p>启动的时候就是通过配置文件启动的</p>
<h2 id="对大小写不敏感">对大小写不敏感</h2>
<h2 id="包含">包含</h2>
<p>好比学习spring的时候，import，include<img src="https://img-blog.csdnimg.cn/img_convert/8f8c0d69b1adf80a6eb73e482e1a85d2.png" alt="img" loading="lazy"></p>
<h2 id="网络重要">网络：重要</h2>
<pre><code class="language-bash">bind 127.0.0.1   # 绑定的ip
protected-mode yes # 保护模式
port 6379		# 端口设置
123
</code></pre>
<figure data-type="image" tabindex="19"><img src="https://img-blog.csdnimg.cn/img_convert/5634c8d64d5dada672d5a3b5c7727c8f.png" alt="image-20201104162912798" loading="lazy"></figure>
<h2 id="通用">通用</h2>
<pre><code class="language-bash">daemonize yes	# 以守护进程的方式运行，默认是no，需要自己开启为yes

pidfile /var/run/redis_6379.pid # 如果以后台方式运行，我们需要指定一个pid文件

# 日志
loglevel notice
logfile &quot;&quot;  # 日志的文件位置名
databases 16 #数据库的数量，默认为16个
always-show-logo yes # 是否显示logo
123456789
</code></pre>
<h2 id="快照">快照</h2>
<p>持久化，在规定的时间内，执行了多少次操作，则会持久化到文件 .rdb .aof</p>
<p>redis是内存数据库，如果没有持久化，那么数据断电即失去</p>
<pre><code class="language-bash"># 如果900秒内至少有一个key进行了修改，我们就进行持久化操作
save 900 1
# 如果300秒内至少有10key进行了修改，我们就进行持久化操作
save 300 10
# 如果60秒内至少有10000key进行了修改，我们就进行持久化操作
save 60 10000
# 我们之后学习持久化，会自己定义这个配置

stop-writes-on-bgsave-error yes # 持久化出错后是否继续工作

rdbcompression yes # 是否压缩rdb文件，需要消耗一些cpu的资源

rdbchecksum yes	# 报错rdb文件时候进行错误的检查校验

dir ./ # rdb文件保存的目录，默认为当前目录
123456789101112131415
</code></pre>
<h2 id="replication-复制主从复制">REPLICATION 复制，主从复制</h2>
<h2 id="security">SECURITY</h2>
<p>可以在这里设置redis的密码，默认是没有密码的</p>
<pre><code class="language-bash">127.0.0.1:6379&gt; config get requirepass	# 获取redis的密码
1) &quot;requirepass&quot;
2) &quot;&quot;
127.0.0.1:6379&gt; config set requirepass &quot;123456&quot;	# 设置redis密码
OK
auth 123456 # 输入密码后才可以登录
123456
</code></pre>
<h2 id="限制clients">限制CLIENTS</h2>
<pre><code class="language-bash">maxclients 10000	# 设置能连接上的最大客户端的数量

maxmemory &lt;bytes&gt;	# redis最大的内存容量

maxmemory-policy noeviction	# 内存达到上限的处理策略
	# 移除一些过期的key
	# 报错
	# 。。。
noeviction: 不删除策略, 达到最大内存限制时, 如果需要更多内存, 直接返回错误信息。（默认值）
allkeys-lru: 所有key通用; 优先删除最近最少使用(less recently used ,LRU) 的 key。
volatile-lru: 只限于设置了 expire 的部分; 优先删除最近最少使用(less recently used ,LRU) 的 key。
allkeys-random: 所有key通用; 随机删除一部分 key。
volatile-random: 只限于设置了 expire 的部分; 随机删除一部分 key。
volatile-ttl: 只限于设置了 expire 的部分; 优先删除剩余时间(time to live,TTL) 短的key。

redis中并不会准确的删除所有键中最近最少使用的键，而是随机抽取maxmeory-samples个键，删除这三个键中最近最少使用的键。
12345678910111213141516
</code></pre>
<h2 id="append-only-模式-aof配置">APPEND ONLY 模式 aof配置</h2>
<pre><code class="language-bash">appendonly no	# 默认不开启aof模式，默认使用rdb方式持久化，几乎在所有情况下rdb够用

appendfilename &quot;appendonly.aof&quot; # 持久化文件的名字

# appendfsync always	# 每次修改都会同步，消耗性能
appendfsync everysec	# 每秒都同步一次 sync，可能会丢失这1s数据
# appendfsync no		# 不执行同步，这时候操作系统自己同步数据，速度最快
1234567
</code></pre>
<h1 id="redis持久化">Redis持久化</h1>
<h2 id="rdbredis-database">RDB（Redis Database）</h2>
<p>在主从复制中，rdb就是备用的！在从机上面</p>
<p>面试和工作，持久化必须是重点</p>
<p>Redis是内存数据库，如果不将内存中的数据库状态保存到磁盘，那么一旦服务器进程退出，服务器中的数据库状态也会消失。所以redis提供了持久化功能</p>
<p>在指定时间间隔内将内存中的数据集写入磁盘，也就是Snapshot快照，它恢复时是将文件直接读到内存里</p>
<p>Redis会单独创建（fork）一个子进程来进行持久化，会先将书局写入到一个临时文件中，待持久化过程都结束了。再用这个临时文件替换上次持久化好的文件。整个过程中，主进程是不进行任何IO操作的，这就确保了极高的性能。如果需要大规模数据的恢复，且对于数据恢复的完整性不是非常敏感，那RDB方式要比AOF更加高效，RDB的缺点是最后一次持久化后的数据可能丢失。我们默认的就是RDB，一般情况下不需要修改这个配置。</p>
<p>RDB保存的文件是dump.rdb都是在我们配置文件中的快照中进行配置的</p>
<figure data-type="image" tabindex="20"><img src="https://img-blog.csdnimg.cn/img_convert/490e34c578bff1f061a953356ab55865.png" alt="image-20201104175635326" loading="lazy"></figure>
<p>自己测试一次，60s内修改5次，就会触发rdb操作</p>
<figure data-type="image" tabindex="21"><img src="https://img-blog.csdnimg.cn/img_convert/1b9128e01b714fbfdf56c11a5bd29699.png" alt="image-20201104175830756" loading="lazy"></figure>
<p>flushall默认产生一个dump.rdb文件</p>
<h3 id="触发机制">触发机制</h3>
<ol>
<li>save规则满足的情况下，会自动触发rdb规则</li>
<li>执行flushall命令也会触发rdb规则</li>
<li>退出redis，也会产生rdb</li>
</ol>
<p>备份就自动生成dump.rdb<img src="https://img-blog.csdnimg.cn/img_convert/c6c0d341d5904b5708753742517786f9.png" alt="image-20201104181156646" loading="lazy"></p>
<h3 id="如何恢复rdb文件">如何恢复rdb文件</h3>
<ol>
<li>
<p>只需要将rdb文件放在redis的启动目录下就可以了，redis启动的时候会自动检查dump.rdb文件恢复</p>
</li>
<li>
<p>查看需要存在的位置</p>
<pre><code class="language-bash">127.0.0.1:6379&gt; config get dir
1) &quot;dir&quot;
2) &quot;/usr/local/redis-6.0.6/bin&quot;	#如果在这个目录下存在dump.rdb文件，启动就会自动恢复其中的数据
123
</code></pre>
<p>几乎他自己的默认配置就够用了，但是我们还是学习</p>
<p>有时候在生产环境，我们会将这个文件进行备份</p>
</li>
</ol>
<p><strong>优点</strong>：</p>
<ol>
<li>适合大规模的数据恢复！dump.rdb</li>
<li>如果对数据完整性不高！</li>
</ol>
<p><strong>缺点</strong>：</p>
<ol>
<li>需要一定的时间间隔进行操作，如果redis意外宕机了，那么最后一次修改的数据就没有了</li>
<li>fork进程的时候会占用一定的内存空间</li>
</ol>
<h2 id="aofappend-only-file">AOF（Append Only File）</h2>
<figure data-type="image" tabindex="22"><img src="https://img-blog.csdnimg.cn/img_convert/3eb0e5a9501dfa3228e646cb18d34e44.png" alt="img" loading="lazy"></figure>
<p>将我们的所有命令都记录下来，history，恢复的时候把这个文件全部再执行一遍</p>
<p>以日志的形式来记录每个写的操作，将Redis执行过的所有指令记录下来（读操作不记录），只许追加文件，但是不可以改写文件，redis启动之初会读取该文件重新构建数据，换言之，redis重启的话就根据日志文件的内容将写指令从签到后执行一次以完成数据的恢复工作</p>
<p>AOF保存的是appendonly.aof文件</p>
<p>默认是不开启的，我们需要手动进行配置，我们只需要将appendonly改为yes即可</p>
<p>重启redis就可以生效了</p>
<p>如果这个aof文件有错位，这时候redis是启动不起来的，我们需要修复这个aof文件</p>
<p>redis给我们提供了<code>redis-check-aof --fix appendonly.aof</code>来进行appendonly.aof的修复</p>
<p>如果文件正常，重启就可以直接恢复了<img src="https://img-blog.csdnimg.cn/img_convert/0d004c953c67eb5e72b536d4229866ff.png" alt="image-20201105102513952" loading="lazy"></p>
<h3 id="优点和缺点">优点和缺点</h3>
<p><strong>优点：</strong></p>
<ol>
<li>每一次修改都同步，文件完整性更加好</li>
<li>每同步一次，可能会丢失一秒的数据</li>
<li>从不同步</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li>相对于数据文件来说，aof远远大于rdb，修复速度也比rdb慢</li>
<li>AOF运行效率也要比rdb慢，redis默认的配置就是rdb持久化</li>
</ol>
<h3 id="重写规则说明">重写规则说明</h3>
<p>如果aof文件大于64MB，太大了，！fork一个新的进程来将我们文件进行重写</p>
<p>aof默认就是文件的无限追加，文件会越来越大</p>
<h2 id="扩展">扩展：</h2>
<h3 id="redis-持久化">Redis 持久化</h3>
<p>Redis 提供了不同级别的持久化方式:</p>
<ul>
<li>RDB持久化方式能够在指定的时间间隔能对你的数据进行快照存储.</li>
<li>AOF持久化方式记录每次对服务器写的操作,当服务器重启的时候会重新执行这些命令来恢复原始的数据,AOF命令以redis协议追加保存每次写的操作到文件末尾.Redis还能对AOF文件进行后台重写,使得AOF文件的体积不至于过大.</li>
<li>如果你只希望你的数据在服务器运行的时候存在,你也可以不使用任何持久化方式.</li>
<li>你也可以同时开启两种持久化方式, 在这种情况下, 当redis重启的时候会优先载入AOF文件来恢复原始的数据,因为在通常情况下AOF文件保存的数据集要比RDB文件保存的数据集要完整.</li>
<li>最重要的事情是了解RDB和AOF持久化方式的不同,让我们以RDB持久化方式开始:</li>
</ul>
<h4 id="rdb的优点"><strong>RDB的优点</strong></h4>
<ul>
<li>
<p>RDB是一个非常紧凑的文件,它保存了某个时间点得数据集,非常适用于数据集的备份,比如你可以在每个小时报保存一下过去24小时内的数据,同时每天保存过去30天的数据,这样即使出了问题你也可以根据需求恢复到不同版本的数据集.</p>
</li>
<li>
<p>RDB是一个紧凑的单一文件,很方便传送到另一个远端数据中心或者亚马逊的S3（可能加密），非常适用于灾难恢复.</p>
</li>
<li>
<p>RDB在保存RDB文件时父进程唯一需要做的就是fork出一个子进程,接下来的工作全部由子进程来做，父进程不需要再做其他IO操作，所以RDB持久化方式可以最大化redis的性能.</p>
</li>
<li>
<p>与AOF相比,在恢复大的数据集的时候，RDB方式会更快一些.</p>
</li>
<li>
<h4 id="rdb的缺点"><strong>RDB的缺点</strong></h4>
</li>
<li>
<p>如果你希望在redis意外停止工作（例如电源中断）的情况下丢失的数据最少的话，那么RDB不适合你.虽然你可以配置不同的save时间点(例如每隔5分钟并且对数据集有100个写的操作),是Redis要完整的保存整个数据集是一个比较繁重的工作,你通常会每隔5分钟或者更久做一次完整的保存,万一在Redis意外宕机,你可能会丢失几分钟的数据.</p>
</li>
<li>
<p>RDB 需要经常fork子进程来保存数据集到硬盘上,当数据集比较大的时候,fork的过程是非常耗时的,可能会导致Redis在一些毫秒级内不能响应客户端的请求.如果数据集巨大并且CPU性能不是很好的情况下,这种情况会持续1秒,AOF也需要fork,但是你可以调节重写日志文件的频率来提高数据集的耐久度.</p>
</li>
<li>
<h4 id="aof-优点"><strong>AOF 优点</strong></h4>
</li>
<li>
<p>使用AOF 会让你的Redis更加耐久: 你可以使用不同的fsync策略：无fsync,每秒fsync,每次写的时候fsync.使用默认的每秒fsync策略,Redis的性能依然很好(fsync是由后台线程进行处理的,主线程会尽力处理客户端请求),一旦出现故障，你最多丢失1秒的数据.</p>
</li>
<li>
<p>AOF文件是一个只进行追加的日志文件,所以不需要写入seek,即使由于某些原因(磁盘空间已满，写的过程中宕机等等)未执行完整的写入命令,你也也可使用redis-check-aof工具修复这些问题.</p>
</li>
<li>
<p>Redis 可以在 AOF 文件体积变得过大时，自动地在后台对 AOF 进行重写： 重写后的新 AOF 文件包含了恢复当前数据集所需的最小命令集合。 整个重写操作是绝对安全的，因为 Redis 在创建新 AOF 文件的过程中，会继续将命令追加到现有的 AOF 文件里面，即使重写过程中发生停机，现有的 AOF 文件也不会丢失。 而一旦新 AOF 文件创建完毕，Redis 就会从旧 AOF 文件切换到新 AOF 文件，并开始对新 AOF 文件进行追加操作。</p>
</li>
<li>
<p>AOF 文件有序地保存了对数据库执行的所有写入操作， 这些写入操作以 Redis 协议的格式保存， 因此 AOF 文件的内容非常容易被人读懂， 对文件进行分析（parse）也很轻松。 导出（export） AOF 文件也非常简单： 举个例子， 如果你不小心执行了 FLUSHALL 命令， 但只要 AOF 文件未被重写， 那么只要停止服务器， 移除 AOF 文件末尾的 FLUSHALL 命令， 并重启 Redis ， 就可以将数据集恢复到 FLUSHALL 执行之前的状态。</p>
</li>
<li>
<h4 id="aof-缺点"><strong>AOF 缺点</strong></h4>
</li>
<li>
<p>对于相同的数据集来说，AOF 文件的体积通常要大于 RDB 文件的体积。</p>
</li>
<li>
<p>根据所使用的 fsync 策略，AOF 的速度可能会慢于 RDB 。 在一般情况下， 每秒 fsync 的性能依然非常高， 而关闭 fsync 可以让 AOF 的速度和 RDB 一样快， 即使在高负荷之下也是如此。 不过在处理巨大的写入载入时，RDB 可以提供更有保证的最大延迟时间（latency）。</p>
</li>
</ul>
<h1 id="redis发布订阅">Redis发布订阅</h1>
<p>消息队列</p>
<p>通信，队列 发送者 订阅者</p>
<p>Redis 发布订阅（pub/sub）是一种消息通信模式：发送者（pub）发送消息，订阅者（sub）接收消息。微信，微博</p>
<p>Redis客户端可以订阅任意数量的频道</p>
<p>订阅/发布消息图：</p>
<p>第一个：消息发送者，第二个：频道 第三个：消息订阅者！</p>
<figure data-type="image" tabindex="23"><img src="https://img-blog.csdnimg.cn/img_convert/dd6b8ef093cf55e0aa316e0a4f94a200.png" alt="image-20201105104248223" loading="lazy"></figure>
<h2 id="命令">命令：</h2>
<p><code>psubcribe pattern [pattern]</code> 订阅一个或多个符合给定模式的频道</p>
<p><code>pubsub subcommand [argument [argument]]</code> 查看订阅与发布系统状态</p>
<p><code>publish channel message</code> 将信息发送到指定的频道</p>
<p><code>punsubscribe [pattern [pattern]]</code> 退订所有给定模式的频道</p>
<p><code>subscribe channel [channel</code> 订阅一个或多个频道的信息</p>
<p><code>unsubscribe [channel [channel]]</code> 退订给定的频道</p>
<h2 id="测试">测试</h2>
<h3 id="订阅端">订阅端</h3>
<pre><code class="language-bash">127.0.0.1:6379&gt; SUBSCRIBE yangjianChannel 	# 订阅一个频道
Reading messages... (press Ctrl-C to quit)
1) &quot;subscribe&quot;
2) &quot;yangjianChannel&quot;
3) (integer) 1
# 等待读取推送的信息
1) &quot;message&quot;	#消息
2) &quot;yangjianChannel&quot;	# 哪个频道的消息
3) &quot;hello,yangjianChannel&quot;	# 具体内容
1) &quot;message&quot;
2) &quot;yangjianChannel&quot;
3) &quot;hello,Redis&quot;
123456789101112
</code></pre>
<h3 id="发送端">发送端</h3>
<pre><code class="language-bash">127.0.0.1:6379&gt; PUBLISH yangjianChannel &quot;hello,yangjianChannel&quot;	# 发布者发布消息到指定的频道
(integer) 1
127.0.0.1:6379&gt; PUBLISH yangjianChannel &quot;hello,Redis&quot;
(integer) 1
1234
</code></pre>
<h2 id="原理">原理</h2>
<p>Redis是使用C实现的，通过分析Redis源码里的pubsub.c文件，了解发布和订阅机制的底层实现，籍此加深对Redis 的理解。Redis通过PUBLISH 、SUBSCRIBE和PSUBSCRIBE等命令实现发布和订阅功能。<br>
微信:<br>
通过SUBSCRIBE命令订阅某频道后，redis-server里维护了一个字典，字典的键就是一个个频道!，而字典的值则是一个链表，链表中保存了所有订阅这个channel的客户端。SUBSCRIBE命令的关键，就是将客户端添加到给定channel的订阅链表中。</p>
<p>通过PUBLSH命令向订阅者发送消息，redis-server会使用给定的频道作为键，在它所维护的channel字典中查找记录了订阅这个频道的所有客户端的链表，遍历这个链表，将消息发布给所有订阅者。</p>
<p>Pub/Sub从字面上理解就是发布(Publish)与订阅( Subscribe )，在Redis中，你可以设定对某一个key值进行消息发布及消息订阅，当一个key值上进行了消息发布后，所有订阅它的客户端都会收到相应的消息。这一功能最明显的用法就是用作实时消息系统，比如普通的即时聊天，群聊等功能。</p>
<h2 id="使用场景">使用场景：</h2>
<ol>
<li>实时消息系统</li>
<li>实时聊天（频道当做聊天室）</li>
<li>订阅关注系统</li>
</ol>
<p>稍微复杂的场景，消息中间件MQ（）</p>
<h1 id="redis主从复制">Redis主从复制</h1>
<h2 id="概念">概念</h2>
<p>主从复制，是指将一台Redis服务器的数据，复制到其他的Redis服务器。前者称为主节点(masterleader)，后者称为从节点(slave/follower);数据的复制是单向的，只能由主节点到从节点。Master以写为主，Slave以读为主。</p>
<p><strong>主从复制的作用主要包括:</strong><br>
1、数据冗余∶主从复制实现了数据的热备份，是持久化之外的一种数据冗余方式。<br>
2、故障恢复∶当主节点出现问题时，可以由从节点提供服务，实现快速的故障恢复;实际上是一种服务的冗余。<br>
3、负载均衡︰在主从复制的基础上，配合读写分离，可以由主节点提供写服务，由从节点提供读服务（即写Redis数据时应用连接主节点，读Redis数据时应用连接从节点），分担服务器负载﹔尤其是在写少读多的场景下，通过多个从节点分担读负载，可以大大提高Redis服务器的并发量。<br>
4、高可用（集群）基石︰除了上述作用以外，主从复制还是哨兵和集群能够实施的基础，因此说主从复制是Redis高可用的基础。</p>
<p>一般来说，要将Redis运用于工程项目中，只使用一台Redis是万万不能的(宕机，最少3个），原因如下:<br>
1、从结构上，单个Redis服务器会发生单点故障，并且一台服务器需要处理所有的请求负载，压力较大;<br>
2、从容量上，单个Redis服务器内存容量有限，就算一台Redis服务器内存容量为256G，也不能将所有内存用作Redis存储内存，一般来说，单台Redis最大使用内存不应该超过20G。<br>
电商网站上的商品，一般都是一次上传，无数次浏览的，说专业点也就是&quot;多读少写&quot;。对于这种场景，我们可以使如下这种架构︰</p>
<figure data-type="image" tabindex="24"><img src="https://img-blog.csdnimg.cn/img_convert/d8f97ed2519b086a298024d5c7675343.png" alt="image-20201105111148557" loading="lazy"></figure>
<p>主从复制，读写分离！80%情况下都是在进行读操作！减缓服务器压力，架构经常使用！一主二从！</p>
<p>只要在公司中，主从复制就是必须要使用的，因为在真实的项目中，不可能单机使用redis！</p>
<h2 id="环境配置">环境配置</h2>
<p>只配置从库，不用配置主库</p>
<h3 id="主从复制信息">主从复制信息</h3>
<pre><code class="language-bash">info replication # 查看当前库的信息

127.0.0.1:6379&gt; info replication
# Replication
role:master		#	角色：master
connected_slaves:0	# 没有从机
master_replid:85e0dc3072e2b2d876b7ff4a0cb53f961d7fd5e6
master_replid2:0000000000000000000000000000000000000000
master_repl_offset:0
second_repl_offset:-1
repl_backlog_active:0
repl_backlog_size:1048576
repl_backlog_first_byte_offset:0
repl_backlog_histlen:0
1234567891011121314
</code></pre>
<p>复制三个配置文件，修改对应的信息</p>
<ol>
<li>端口号</li>
<li>pid</li>
<li>日志名字</li>
<li>备份文件名字 dump.rdb</li>
</ol>
<p>修改完毕之后启动三个redis服务<img src="https://img-blog.csdnimg.cn/img_convert/f39ce1210976bd51f427e88aee4ec562.png" alt="img" loading="lazy"></p>
<h2 id="一主二从">一主二从</h2>
<p>默认情况下，每台Redis服务器都是主节点，我们一般情况下只用配置从机就好了</p>
<p>认老大，一主（79）二从（80,81）</p>
<pre><code class="language-bash">slaveof host port # 设置这个主机的主人

127.0.0.1:6380&gt; SLAVEOF 127.0.0.1 6379	# 认定这个主机下的6379端口为主
OK
127.0.0.1:6380&gt; info replication
# Replication
role:slave
master_host:127.0.0.1	# 可以看到主机的信息
master_port:6379
master_link_status:up
master_last_io_seconds_ago:8
master_sync_in_progress:0
slave_repl_offset:14
slave_priority:100
slave_read_only:1
connected_slaves:0
master_replid:acec4d2172706ac4d75dc58669cda8a2d7a8bfbd
master_replid2:0000000000000000000000000000000000000000
master_repl_offset:14
second_repl_offset:-1
repl_backlog_active:1
repl_backlog_size:1048576
repl_backlog_first_byte_offset:1
repl_backlog_histlen:14


# 在主机中查看
[root@iZbp17e1mp4a0cgbw1358xZ bin]# redis-cli -p 6379
127.0.0.1:6379&gt; info replication
# Replication
role:master
connected_slaves:1
slave0:ip=127.0.0.1,port=6380,state=online,offset=126,lag=1
master_replid:acec4d2172706ac4d75dc58669cda8a2d7a8bfbd
master_replid2:0000000000000000000000000000000000000000
master_repl_offset:126
second_repl_offset:-1
repl_backlog_active:1
repl_backlog_size:1048576
repl_backlog_first_byte_offset:1
repl_backlog_histlen:126
1234567891011121314151617181920212223242526272829303132333435363738394041
</code></pre>
<p>如果两个都配置完了，就会有两个从机的</p>
<p>真实的主从配置应该在配置文件中配置，这样的话是永久的，使用命令是暂时的</p>
<h2 id="细节">细节</h2>
<p>主机可以写，从机不能写只能读！主机中的所有信息和数据，都会自动被从机保存</p>
<p>测试：主机断开连接，从机依旧连接到主机的，但是没有写操作，这个时候，如果主机回来，从机依旧可以直接获取到主机写的信息</p>
<p>如果是使用命令行，来配置的主从，这个时候如果重启了，就会变回主机!只要变为从机，立马就会从主机中获取值</p>
<h2 id="复制原理">复制原理</h2>
<p>Slave启动成功连接到master后会发送一个sync命令<br>
Master接到命令，启动后台的存盘进程，同时收集所有接收到的用于修改数据集命令，在后台进程执行完毕之后，master将传送整个数据文件到slave，并完成一次完全同步。</p>
<p>全量复制︰而slave服务在接收到数据库文件数据后，将其存盘并加载到内存中。</p>
<p>增量复制:Master继续将新的所有收集到的修改命令依次传给slave，完成同步。</p>
<p>但是只要是重新连接master，一次完全同步(全量复制）将被自动执行。我们的数据一定可以在从机中看到</p>
<h2 id="层层链路">层层链路</h2>
<p>上一个M连接下一个S，这时候也可以完成我们的主从复制</p>
<figure data-type="image" tabindex="25"><img src="https://img-blog.csdnimg.cn/img_convert/1d90bc1c207338cae0e414051f4bfe8e.png" alt="image-20201105122218386" loading="lazy"></figure>
<h2 id="如果没有老大能不能选择一个老大出来呢手动">如果没有老大，能不能选择一个老大出来呢，手动</h2>
<p>谋朝篡位</p>
<p><code>salveof no one</code>,如果主机断开了连接，可以使用这个命令来让自己成为主节点，其他的结点就可以手动连接到最新的这个主节点</p>
<p>如果这个时候老大修复了，那就只能重新连接</p>
<h1 id="redis哨兵模式自动选取老大的模式">Redis哨兵模式（自动选取老大的模式）</h1>
<h2 id="概述">概述：</h2>
<p>主从切换技术的方法是：当主服务器宕机后，需要手动把一台服务器切换为主服务器，这就需要人工干预，费时费力，还会造成一段时间内服务器不可用。这不是一种推荐方式，更多时候，我们优先考虑哨兵模式，Redis从2.8开始正式提供了Sentinel（哨兵）架构来解决这个问题。</p>
<p>谋朝篡位的自动版，能够后台监控主机是否故障，如果故障了根据投票数自动将库转换为主库。</p>
<p>哨兵模式是一种特殊的模式，首先Redis提供了哨兵的命令，哨兵是一个独立的进程，作为进程，它会独立运行。其原理是<strong>哨兵通过发送命令，等待Redis服务器响应，从而监控运行的多个Redis实例</strong><img src="https://img-blog.csdnimg.cn/img_convert/c1ba98cbc27441ca87553f823873fac2.png" alt="image-20201105125043998" loading="lazy"></p>
<p><strong>哨兵有两个作用</strong></p>
<ol>
<li>通过发送命令，让Redis服务器返回监控其运行状态，包括主服务器和从服务器</li>
<li>当哨兵监测到master宕机，会自动将slave切换成master，然后通过发布订阅模式通知其他的从服务器，修改配置文件，让他们切换主机</li>
</ol>
<p>然而一个哨兵进程对Redis服务器进行监控，可能会出现问题，为此，我们可以使用多个哨兵进行监控。各个哨兵之间还会进行监控，这样就形成了多哨兵模式。<img src="https://img-blog.csdnimg.cn/img_convert/3cad6f6728ec2c403ae6d074bbc51823.png" alt="image-20201105125638471" loading="lazy"></p>
<p>假设主服务器宕机，哨兵1先检测到这个结果，系统并不会马上进行failover过程，仅仅是哨兵1主观的认为主服务器不可用，这个现象成为<strong>主观下线</strong>。当后面的哨兵也检测到主服务器不可用，并且数量达到一定值时，那么哨兵之间就会进行一次投票，投票的结果由一个哨兵发起，进行failover[故障转移]操作。切换成功后，就会通过发布订阅模式，让各个哨兵把自己监控的从服务器实现切换主机，这个过程称为客观下线。</p>
<h2 id="测试-2">测试</h2>
<p>我们目前的状态是一主二从</p>
<ol>
<li>配置哨兵配置文件<code>sentinel.conf</code></li>
</ol>
<pre><code class="language-bash"># sentinel monitor 被监控的名称 host port 1
sentinel monitor myredis 127.0.0.1 6379 1
# 1是有多少哨兵认为挂了，master才是真的挂了
123
</code></pre>
<p>后面的这个数字1代表主机挂了，slave投票看让谁来接替成为主机，票数最多的就会成为主机</p>
<ol>
<li>启动哨兵</li>
</ol>
<pre><code class="language-bash">redis-sentinel ../etc/sentinal.conf

[root@iZbp17e1mp4a0cgbw1358xZ bin]# redis-sentinel ../etc/sentinal.conf 
20615:X 05 Nov 2020 13:05:15.181 # oO0OoO0OoO0Oo Redis is starting oO0OoO0OoO0Oo
20615:X 05 Nov 2020 13:05:15.181 # Redis version=6.0.6, bits=64, commit=00000000, modified=0, pid=20615, just started
20615:X 05 Nov 2020 13:05:15.181 # Configuration loaded
                _._                                                  
           _.-``__ ''-._                                             
      _.-``    `.  `_.  ''-._           Redis 6.0.6 (00000000/0) 64 bit
  .-`` .-```.  ```\/    _.,_ ''-._                                   
 (    '      ,       .-`  | `,    )     Running in sentinel mode
 |`-._`-...-` __...-.``-._|'` _.-'|     Port: 26379
 |    `-._   `._    /     _.-'    |     PID: 20615
  `-._    `-._  `-./  _.-'    _.-'                                   
 |`-._`-._    `-.__.-'    _.-'_.-'|                                  
 |    `-._`-._        _.-'_.-'    |           http://redis.io        
  `-._    `-._`-.__.-'_.-'    _.-'                                   
 |`-._`-._    `-.__.-'    _.-'_.-'|                                  
 |    `-._`-._        _.-'_.-'    |                                  
  `-._    `-._`-.__.-'_.-'    _.-'                                   
      `-._    `-.__.-'    _.-'                                       
          `-._        _.-'                                           
              `-.__.-'                                               

20615:X 05 Nov 2020 13:05:15.182 # WARNING: The TCP backlog setting of 511 cannot be enforced because /proc/sys/net/core/somaxconn is set to the lower value of 128.
20615:X 05 Nov 2020 13:05:15.185 # Sentinel ID is ee900e749cf585634b3bc2860471d28dcd1ec109
20615:X 05 Nov 2020 13:05:15.185 # +monitor master myredis 127.0.0.1 6379 quorum 1
20615:X 05 Nov 2020 13:05:15.186 * +slave slave 127.0.0.1:6380 127.0.0.1 6380 @ myredis 127.0.0.1 6379
20615:X 05 Nov 2020 13:05:15.188 * +slave slave 127.0.0.1:6381 127.0.0.1 6381 @ myredis 127.0.0.1 6379

123456789101112131415161718192021222324252627282930
</code></pre>
<p>如果master结点断开了，这个时候就会从从机中随机选择一个服务器,这里面有一个投票算法</p>
<p><strong>哨兵日志</strong></p>
<figure data-type="image" tabindex="26"><img src="https://img-blog.csdnimg.cn/img_convert/09a06ab2d386b69dc6e093379c3dfbbe.png" alt="image-20201105130939012" loading="lazy"></figure>
<h2 id="哨兵模式">哨兵模式</h2>
<p>如果主机此时回来了，只能归并到新的主机下，当做从机，这就是哨兵模式的规则！</p>
<p><strong>优点：</strong></p>
<ol>
<li>哨兵模式，基于主从复制模式，所有主从配置的优点它都有</li>
<li>主从可以切换，故障可以转移，系统的可用性就会更好</li>
<li>哨兵模式就是主从模式的升级，手动到自动，更加健壮</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li>Redis不好在线扩容，集群容量一旦达到上限，在线扩容就十分麻烦</li>
<li>实现哨兵模式的配置是非常麻烦的，里面有很多选择</li>
</ol>
<h2 id="哨兵模式全部配置">哨兵模式全部配置</h2>
<pre><code class="language-bash"># Example sentinel.conf
 
# 哨兵sentinel实例运行的端口 默认26379
port 26379
 
# 哨兵sentinel的工作目录
dir /tmp
 
# 哨兵sentinel监控的redis主节点的 ip port 
# master-name  可以自己命名的主节点名字 只能由字母A-z、数字0-9 、这三个字符&quot;.-_&quot;组成。
# quorum 当这些quorum个数sentinel哨兵认为master主节点失联 那么这时 客观上认为主节点失联了
# sentinel monitor &lt;master-name&gt; &lt;ip&gt; &lt;redis-port&gt; &lt;quorum&gt;
sentinel monitor mymaster 127.0.0.1 6379 1
 
# 当在Redis实例中开启了requirepass foobared 授权密码 这样所有连接Redis实例的客户端都要提供密码
# 设置哨兵sentinel 连接主从的密码 注意必须为主从设置一样的验证密码
# sentinel auth-pass &lt;master-name&gt; &lt;password&gt;
sentinel auth-pass mymaster MySUPER--secret-0123passw0rd
 
 
# 指定多少毫秒之后 主节点没有应答哨兵sentinel 此时 哨兵主观上认为主节点下线 默认30秒
# sentinel down-after-milliseconds &lt;master-name&gt; &lt;milliseconds&gt;
sentinel down-after-milliseconds mymaster 30000
 
# 这个配置项指定了在发生failover主备切换时最多可以有多少个slave同时对新的master进行 同步，
这个数字越小，完成failover所需的时间就越长，
但是如果这个数字越大，就意味着越 多的slave因为replication而不可用。
可以通过将这个值设为 1 来保证每次只有一个slave 处于不能处理命令请求的状态。
# sentinel parallel-syncs &lt;master-name&gt; &lt;numslaves&gt;
sentinel parallel-syncs mymaster 1
 
 
 
# 故障转移的超时时间 failover-timeout 可以用在以下这些方面： 
#1. 同一个sentinel对同一个master两次failover之间的间隔时间。
#2. 当一个slave从一个错误的master那里同步数据开始计算时间。直到slave被纠正为向正确的master那里同步数据时。
#3.当想要取消一个正在进行的failover所需要的时间。  
#4.当进行failover时，配置所有slaves指向新的master所需的最大时间。不过，即使过了这个超时，slaves依然会被正确配置为指向master，但是就不按parallel-syncs所配置的规则来了
# 默认三分钟
# sentinel failover-timeout &lt;master-name&gt; &lt;milliseconds&gt;
sentinel failover-timeout mymaster 180000
 
# SCRIPTS EXECUTION
 
#配置当某一事件发生时所需要执行的脚本，可以通过脚本来通知管理员，例如当系统运行不正常时发邮件通知相关人员。
#对于脚本的运行结果有以下规则：
#若脚本执行后返回1，那么该脚本稍后将会被再次执行，重复次数目前默认为10
#若脚本执行后返回2，或者比2更高的一个返回值，脚本将不会重复执行。
#如果脚本在执行过程中由于收到系统中断信号被终止了，则同返回值为1时的行为相同。
#一个脚本的最大执行时间为60s，如果超过这个时间，脚本将会被一个SIGKILL信号终止，之后重新执行。
 
#通知型脚本:当sentinel有任何警告级别的事件发生时（比如说redis实例的主观失效和客观失效等等），将会去调用这个脚本，
#这时这个脚本应该通过邮件，SMS等方式去通知系统管理员关于系统不正常运行的信息。调用该脚本时，将传给脚本两个参数，
#一个是事件的类型，
#一个是事件的描述。
#如果sentinel.conf配置文件中配置了这个脚本路径，那么必须保证这个脚本存在于这个路径，并且是可执行的，否则sentinel无法正常启动成功。
#通知脚本
# sentinel notification-script &lt;master-name&gt; &lt;script-path&gt;
  sentinel notification-script mymaster /var/redis/notify.sh
 
# 客户端重新配置主节点参数脚本
# 当一个master由于failover而发生改变时，这个脚本将会被调用，通知相关的客户端关于master地址已经发生改变的信息。
# 以下参数将会在调用脚本时传给脚本:
# &lt;master-name&gt; &lt;role&gt; &lt;state&gt; &lt;from-ip&gt; &lt;from-port&gt; &lt;to-ip&gt; &lt;to-port&gt;
# 目前&lt;state&gt;总是“failover”,
# &lt;role&gt;是“leader”或者“observer”中的一个。 
# 参数 from-ip, from-port, to-ip, to-port是用来和旧的master和新的master(即旧的slave)通信的
# 这个脚本应该是通用的，能被多次调用，不是针对性的。
# sentinel client-reconfig-script &lt;master-name&gt; &lt;script-path&gt;
sentinel client-reconfig-script mymaster /var/redis/reconfig.sh


123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172
</code></pre>
<h1 id="redis缓存穿透与雪崩面试高频工作常用">Redis缓存穿透与雪崩（面试高频，工作常用）</h1>
<p>服务的高可用问题</p>
<p>不会详细分析解决方案的底层（专题）</p>
<figure data-type="image" tabindex="27"><img src="https://img-blog.csdnimg.cn/img_convert/0a4034ed3249cfd5dcf1b4801c603ac3.png" alt="image-20201105132544362" loading="lazy"></figure>
<h2 id="缓存穿透查不到">缓存穿透（查不到）</h2>
<h3 id="概念-2">概念</h3>
<p>缓存穿透的概念很简单，用户想要查询一个数据，发现redis内存数据库没有，也就是缓存没有命中，于是向持久层数据库查询。发现也没有，于是本次查询失败。当用户很多的时候，缓存都没有命中（秒杀!)，于是都去请求了持久层数据库。这会给持久层数据库造成很大的压力，这时候就相当于出现了缓存穿透。</p>
<h3 id="解决方案">解决方案</h3>
<h4 id="布隆过滤器">布隆过滤器</h4>
<p>布隆过滤器是一种数据结构，对所有可能查询的参数以hash形式存储，在控制层先进行校验，不符合则丢弃，从而避免了对底层存储系统的查询压力﹔</p>
<figure data-type="image" tabindex="28"><img src="https://img-blog.csdnimg.cn/img_convert/711f719d7387385ef19084f732aa266e.png" alt="image-20201105132801112" loading="lazy"></figure>
<h4 id="缓存空对象">缓存空对象</h4>
<p>当存储层不命中后，即使返回的空对象也将其缓存起来，同时会设置一个过期时间，之后再访问这个数据将会从缓存中获取，保护了后端数据源;</p>
<figure data-type="image" tabindex="29"><img src="https://img-blog.csdnimg.cn/img_convert/14c9e14245bae4a3e49226c416376c8c.png" alt="image-20201105132900180" loading="lazy"></figure>
<p>但是这种方法会存在两个问题:<br>
1、如果空值能够被缓存起来，这就意味着缓存需要更多的空间存储更多的键，因为这当中可能会有很多的空值的键;<br>
2、即使对空值设置了过期时间，还是会存在缓存层和存储层的数据会有一段时间窗口的不一致，这对于需要保持一致性的业务会有影响。</p>
<h2 id="缓存击穿查的太多量大缓存过期">缓存击穿（查的太多，量大，缓存过期）</h2>
<p>微博服务器宕机（60 60.1 0.1）</p>
<h3 id="概述-2">概述：</h3>
<p>这里需要注意和缓存击穿的区别，缓存击穿，是指一个key非常热点，在不停的扛着大并发，大并发集中对这一个点进行访问，当这个key在失效的瞬间，持续的大并发就穿破缓存，直接请求数据库，就像在一个屏障上凿开了一个洞。<br>
当某个key在过期的瞬间，有大量的请求并发访问，这类数据一般是热点数据，由于缓存过期，会同时访问数据库来查询最新数据，并且回写缓存，会导使数据库瞬间压力过大。</p>
<h3 id="解决方案-2">解决方案</h3>
<h4 id="设置热点数据永不过期">设置热点数据永不过期</h4>
<p>从缓存层面来看，没有设置过期时间，所以不会出现热点key过期后产生的问题</p>
<h4 id="加互斥锁">加互斥锁</h4>
<p>分布式锁：使用分布式锁，保证对于每个key同时只有一个线程去查询后端服务，其他线程没有获得分布式锁的权限，因此只需要等待即可。这种方式将高并发的压力转移到了分布式锁，因此对分布式锁考验很大</p>
<h2 id="缓存雪崩">缓存雪崩</h2>
<h3 id="概念-3">概念</h3>
<p>缓存雪崩，是指在某一个时间段，缓存集中过期失效。Redis宕机<br>
产生雪崩的原因之一，比如在写本文的时候，马上就要到双十二零点，很快就会迎来一波抢购，这波商品时间比较集中的放入了缓存，假设缓存一个小时。那么到了凌晨一点钟的时候，这批商品的缓存就都过期了。而对这批商品的访问查询，都落到了数据库上，对于数据库而言，就会产生周期性的压力波峰。于是所有的请求都会达到存储层，存储层的调用量会暴增，造成存储层也会挂掉的情况。</p>
<h3 id="解决方案-3">解决方案</h3>
<h4 id="redis高可用">Redis高可用</h4>
<p>这个思想的含义是，既然redis有可能挂掉，那我多增设几台redis，这样一台挂掉之后其他的还可以继续工作，其实就是搭建的集群。(异地多活）</p>
<h4 id="限流降级在springcloud中有">限流降级（在SpringCloud中有）</h4>
<p>这个解决方案的思想是，在缓存失效后，通过加锁或者队列来控制读数据库写缓存的线程数量。比如对某个key只允许一个线程查询数据和写缓存，其他线程等待。</p>
<h4 id="数据预热">数据预热</h4>
<p>数据加热的含义就是在正式部署之前，我先把可能的数据先预先访问一遍，这样部分可能大量访问的数据就会加载到缓存中。在即将发生大并发访问前手动触发加载缓存不同的key，设置不同的过期时间，让缓存失效的时间点尽量均匀。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等77. 组合]]></title>
        <id>https://ganhan999.github.io/post/77、 组合/</id>
        <link href="https://ganhan999.github.io/post/77、 组合/">
        </link>
        <updated>2021-01-22T12:29:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定两个整数 n 和 k，返回 1 ... n 中所有可能的 k 个数的组合。</p>
<p>示例:<br>
输入: n = 4, k = 2<br>
输出:<br>
[<br>
[2,4],<br>
[3,4],<br>
[2,3],<br>
[1,2],<br>
[1,3],<br>
[1,4],<br>
]</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>回溯算法</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def combine(self, n: int, k: int) -&gt; list:
        ans = list()

        def backtrack(tmp: list, index: int) -&gt; None:
            if len(tmp) == k:
                ans.append(tmp[:])
                return
            for i in range(index, n + 1):
                tmp.append(i)
                backtrack(tmp, i + 1)
                tmp.pop()
        backtrack([], 1)
        return ans
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>用Python的itertools来写组合组合</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution:
    def combine(self, n: int, k: int) -&gt; List[List[int]]:
         return list(itertools.combinations(range(1,n+1),k))
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等75. 颜色分类]]></title>
        <id>https://ganhan999.github.io/post/75、颜色分类/</id>
        <link href="https://ganhan999.github.io/post/75、颜色分类/">
        </link>
        <updated>2021-01-21T13:06:40.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定一个包含红色、白色和蓝色，一共 n 个元素的数组，原地对它们进行排序，使得相同颜色的元素相邻，并按照红色、白色、蓝色顺序排列。<br>
此题中，我们使用整数 0、 1 和 2 分别表示红色、白色和蓝色。</p>
<p>示例 1：<br>
输入：nums = [2,0,2,1,1,0]<br>
输出：[0,0,1,1,2,2]</p>
<p>示例 2：<br>
输入：nums = [2,0,1]<br>
输出：[0,1,2]</p>
<p>示例 3：<br>
输入：nums = [0]<br>
输出：[0]</p>
<p>示例 4：<br>
输入：nums = [1]<br>
输出：[1]</p>
<p>提示：<br>
n == nums.length<br>
1 &lt;= n &lt;= 300<br>
nums[i] 为 0、1 或 2</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>单指针<br>
我们可以考虑对数组进行两次遍历。在第一次遍历中，我们将数组中所有的 0 交换到数组的头部。<br>
在第二次遍历中，我们将数组中所有的 11 交换到头部的 0 之后。此时，所有的 2 都出现在数组的尾部，这样我们就完成了排序。</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def sortColors(self, nums: List[int]) -&gt; None:
        n = len(nums)
        ptr = 0
        for i in range(n):
            if nums[i] == 0:
                nums[i], nums[ptr] = nums[ptr], nums[i]
                ptr += 1
        for i in range(ptr, n):
            if nums[i] == 1:
                nums[i], nums[ptr] = nums[ptr], nums[i]
                ptr += 1

</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>双指针，一个指向换0的位置，一个指向换1的位置。</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution:
    def sortColors(self, nums: List[int]) -&gt; None:
        n = len(nums)
        p0 = p1 = 0
        for i in range(n):
            if nums[i] == 1:
                nums[i], nums[p1] = nums[p1], nums[i]
                p1 += 1
            elif nums[i] == 0:
                nums[i], nums[p0] = nums[p0], nums[i]
                if p0 &lt; p1:#如果p0 &lt; p1，要把nums[i], nums[p1]互换，当 p_0 &lt; p_1时，我们已经将一些1连续地放在头部，此
                    # 时一定会把一个 11 交换出去，导致答案错误
                    nums[i], nums[p1] = nums[p1], nums[i]
                p0 += 1
                p1 += 1

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java MyBatisPlus]]></title>
        <id>https://ganhan999.github.io/post/Java MyBatisPlus/</id>
        <link href="https://ganhan999.github.io/post/Java MyBatisPlus/">
        </link>
        <updated>2021-01-20T11:18:25.000Z</updated>
        <content type="html"><![CDATA[<h1 id="mybatisplusb站狂神说">MyBatisPlusB站狂神说</h1>
<p>学习MyBatis-Plus之前要先学MyBatis–&gt;Spring—&gt;SpringMVC</p>
<p>为什么要学它?MyBatisPlus可以节省我们大量的时间,所有CRUD代码都可以自动完成</p>
<p>JPA, tk-mapper ,MyBatisPlus</p>
<p>偷懒用的!</p>
<blockquote>
<h2 id="1简介">1.简介</h2>
</blockquote>
<p>是什么?</p>
<p>官网:https://baomidou.com/</p>
<figure data-type="image" tabindex="1"><img src="https://img-blog.csdnimg.cn/20200930150243845.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="https://img-blog.csdnimg.cn/20200930150327276.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<blockquote>
<h2 id="2特性">2.特性</h2>
</blockquote>
<ul>
<li><strong>无侵入</strong>：只做增强不做改变，引入它不会对现有工程产生影响，如丝般顺滑</li>
<li><strong>损耗小</strong>：启动即会自动注入基本 CURD，性能基本无损耗，直接面向对象操作</li>
<li><strong>强大的 CRUD 操作</strong>：内置通用 Mapper、通用 Service，仅仅通过少量配置即可实现单表大部分 CRUD 操作，更有强大的条件构造器，满足各类使用需求</li>
<li><strong>支持 Lambda 形式调用</strong>：通过 Lambda 表达式，方便的编写各类查询条件，无需再担心字段写错</li>
<li><strong>支持主键自动生成</strong>：支持多达 4 种主键策略（内含分布式唯一 ID 生成器 - Sequence），可自由配置，完美解决主键问题</li>
<li><strong>支持 ActiveRecord 模式</strong>：支持 ActiveRecord 形式调用，实体类只需继承 Model 类即可进行强大的 CRUD 操作</li>
<li><strong>支持自定义全局通用操作</strong>：支持全局通用方法注入（ Write once, use anywhere ）</li>
<li><strong>内置代码生成器</strong>：采用代码或者 Maven 插件可快速生成 Mapper 、 Model 、 Service 、 Controller 层代码，支持模板引擎，更有超多自定义配置等您来使用</li>
<li><strong>内置分页插件</strong>：基于 MyBatis 物理分页，开发者无需关心具体操作，配置好插件之后，写分页等同于普通 List 查询</li>
<li><strong>分页插件支持多种数据库</strong>：支持 MySQL、MariaDB、Oracle、DB2、H2、HSQL、SQLite、Postgre、SQLServer 等多种数据库</li>
<li><strong>内置性能分析插件</strong>：可输出 Sql 语句以及其执行时间，建议开发测试时启用该功能，能快速揪出慢查询</li>
<li><strong>内置全局拦截插件</strong>：提供全表 delete 、 update 操作智能分析阻断，也可自定义拦截规则，预防误操作</li>
</ul>
<h2 id="1快速入门">1.快速入门</h2>
<p>地址:https://baomidou.com/guide/quick-start.html#%E5%88%9D%E5%A7%8B%E5%8C%96%E5%B7%A5%E7%A8%8B</p>
<p>使用第三方插件:</p>
<ol>
<li>导入对应的依赖</li>
<li>研究依赖如何配置</li>
<li>代码如何编写</li>
<li>提高扩展技术能力</li>
</ol>
<blockquote>
<p>步骤</p>
</blockquote>
<p>1.创建数据库 <code>mybatis_plus</code></p>
<p>2.创建数据库</p>
<p>创建表</p>
<pre><code class="language-sql">DROP TABLE IF EXISTS user;

CREATE TABLE user
(
	id BIGINT(20) NOT NULL COMMENT '主键ID',
	name VARCHAR(30) NULL DEFAULT NULL COMMENT '姓名',
	age INT(11) NULL DEFAULT NULL COMMENT '年龄',
	email VARCHAR(50) NULL DEFAULT NULL COMMENT '邮箱',
	PRIMARY KEY (id)
);
--真实开发环境中,version(乐观锁),deleted(逻辑删除),gmt_create,gmt_modified
</code></pre>
<p>插入数据</p>
<pre><code class="language-sql">DELETE FROM user;

INSERT INTO user (id, name, age, email) VALUES
(1, 'Jone', 18, 'test1@baomidou.com'),
(2, 'Jack', 20, 'test2@baomidou.com'),
(3, 'Tom', 28, 'test3@baomidou.com'),
(4, 'Sandy', 21, 'test4@baomidou.com'),
(5, 'Billie', 24, 'test5@baomidou.com');
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://img-blog.csdnimg.cn/20200930150344676.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>3.编写项目,初始化项目! 使用SpringBoot初始化!</p>
<p>4.导入依赖</p>
<pre><code class="language-xml">&lt;!--mysql--&gt;
&lt;dependency&gt;
    &lt;groupId&gt;mysql&lt;/groupId&gt;
    &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;!--lombok--&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.projectlombok&lt;/groupId&gt;
    &lt;artifactId&gt;lombok&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;!--mybatis-plus--&gt;
&lt;dependency&gt;
    &lt;groupId&gt;com.baomidou&lt;/groupId&gt;
    &lt;artifactId&gt;mybatis-plus-boot-starter&lt;/artifactId&gt;
    &lt;version&gt;3.0.5&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>说明:我们使用mybatis-plus 可以节省我们大量的代码,尽量不要同时导入mybatis和mybatis-plus因为版本有差异!</p>
<p>5.连接数据库!这一步和mybatis相同!</p>
<pre><code class="language-properties"># mysql 5 驱动不同  com.mysql.jdbc.Driver

# mysql 8 驱动不同 com.mysql.cj.jdbc.Driver . 需要增加时区的配置 serverTimezone=GMT%2B8
spring.datasource.username=codeyuaiiao
spring.datasource.password=3615yuhaijiao
spring.datasource.url=jdbc:mysql://localhost:3306/mybatis_plus?useSSL=false&amp;useUnicode=true&amp;characterEncoding=utf-8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
1234567
</code></pre>
<p>6.传统的方式pojo-dao(连接mybatis,配置mapper.xml文件)-service-controller</p>
<p>6.使用了mybatis-plus之后</p>
<ul>
<li>
<p>pojo</p>
<pre><code class="language-java">@Data
@AllArgsConstructor
@NoArgsConstructor
public class User {
    private Long id;
    private String name;
    private Integer age;
    private String email;
</code></pre>
</li>
<li>
<p>mapper接口</p>
<pre><code class="language-java">import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.codeyuaiiao.pojo.User;
import org.springframework.stereotype.Repository;

//在对应的mapper上面继承基本的类 BaseMapper
@Repository//代表持久层
public interface UserMapper extends BaseMapper&lt;User&gt; {
    //所有CRUD操作都已经编写完成了
    //你不需要向以前一样配置一大堆文件了!
}

</code></pre>
<p>注意点:需要在主启动类<code>MybatisPlusApplication</code>上扫描我们Mapper包下的所有接口</p>
<p><code>@MapperScan(&quot;com.codeyuaiiao.mapper&quot;)</code></p>
</li>
<li>
<p>测试类中测试</p>
<pre><code class="language-java">@SpringBootTest
class MybatisPlusApplicationTests {

    //继承了BaseMapper, 所有的方法都来自己父类
    //我们也可以编写自己的扩展方法
    @Autowired
    private UserMapper userMapper;

    @Test
    void contextLoads() {
        //参数是一个Wrapper , 条件构造器,这里我们先不用 --null
        //查询全部用户
        List&lt;User&gt; users = userMapper.selectList(null);
        users.forEach(System.out::println);
    }
}
</code></pre>
</li>
<li>
<p>查询结果:</p>
</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://img-blog.csdnimg.cn/20200930150413201.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<blockquote>
<p>思考问题</p>
<ol>
<li>sql谁帮我们写的?—mybatis-plus</li>
<li>方法谁帮我们写的?—mybatis-pluss</li>
</ol>
</blockquote>
<h2 id="2配置日志">2.配置日志</h2>
<p>我们所有的sql是不可见的,我们希望知道他是怎么执行的,所以我们必须看日志!</p>
<pre><code class="language-properties"># 配置日志  (默认控制台输出)
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://img-blog.csdnimg.cn/20200930150425185.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>配置完日志之后你会喜欢上mybatis-plus!</p>
<h2 id="3crud扩展">3.CRUD扩展</h2>
<h3 id="1插入数据">1.插入数据</h3>
<pre><code class="language-java">@Test
public void testInsert(){
    User user = new User();
    user.setName(&quot;codeyuaiiao&quot;);
    user.setAge(3);
    user.setEmail(&quot;747557612@qq.com&quot;);
    int result = userMapper.insert(user);
    System.out.println(result);
    System.out.println(user);

}
</code></pre>
<figure data-type="image" tabindex="6"><img src="https://img-blog.csdnimg.cn/20200930150437175.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<blockquote>
<p>注意点:数据库插入的id默认值为:全局的唯一id</p>
</blockquote>
<h3 id="2主键生成策略">2.主键生成策略</h3>
<blockquote>
<p>默认 ID_WORKER 全局唯一id</p>
<p>对应数据库中的主键(uuid.自增id.雪花算法.redis.zookeeper)</p>
</blockquote>
<p>分布式系统唯一id生成:https://www.cnblogs.com/haoxinyue/p/5208136.html</p>
<p><strong>雪花算法</strong>😦<strong>Twitter的snowflake算法</strong>)</p>
<p>snowflake是Twitter开源的分布式ID生成算法，结果是一个long型的ID。其核心思想是：使用41bit作为毫秒数，10bit作为机器的ID（5个bit是数据中心，5个bit的机器ID），12bit作为毫秒内的流水号（意味着每个节点在每毫秒可以产生 4096 个 ID），最后还有一个符号位，永远是0.可以保证几乎全球唯一</p>
<blockquote>
<p>主键自增</p>
</blockquote>
<p>我们需要配置主键自增:</p>
<ol>
<li>
<p>实体类字段上<code>@TableId(type = IdType.AUTO)</code></p>
</li>
<li>
<p>数据库字段一定要是<strong>自增</strong>!</p>
<figure data-type="image" tabindex="7"><img src="https://img-blog.csdnimg.cn/20200930150449191.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<figure data-type="image" tabindex="8"><img src="https://img-blog.csdnimg.cn/20200930150459128.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
</li>
</ol>
<figure data-type="image" tabindex="9"><img src="https://img-blog.csdnimg.cn/20200930150508407.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<blockquote>
<p>其余源码解释</p>
</blockquote>
<pre><code class="language-java">public enum IdType {
     
    AUTO(0),//数据库ID自增  
    NONE(1),//该类型为未设置主键类型      
    INPUT(2),//用户输入ID
      		 //该类型可以通过自己注册自动填充插件进行填充  
    
//以下3种类型、只有当插入对象ID 为空，才自动填充。     
    ID_WORKER(3),//全局唯一ID (idWorker)      
    UUID(4),//全局唯一ID (UUID)          
    ID_WORKER_STR(5);//字符串全局唯一ID (idWorker 的字符串表示)    
</code></pre>
<h3 id="3更新数据">3.更新数据</h3>
<blockquote>
<p>动态sql</p>
</blockquote>
<p>注意:updateById()参数是 一个对象!</p>
<pre><code class="language-java">//测试更新
@Test
public void testUpdate(){
    User user = new User();
    user.setId(2L);
    user.setName(&quot;阿峧不是山交&quot;);
    //        注意:updateById()参数是 一个对象!
    int i = userMapper.updateById(user);
    System.out.println(i);

}
</code></pre>
<figure data-type="image" tabindex="10"><img src="https://img-blog.csdnimg.cn/20200930150523190.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<figure data-type="image" tabindex="11"><img src="https://img-blog.csdnimg.cn/20200930150543666.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>所有的sql都是动态帮你配置的</p>
<h3 id="4自动填充">4.自动填充</h3>
<p>创建时间 . 修改时间! 这些个操作都是自动化完成的,我们不希望手动更新!</p>
<p>阿里巴巴开发手册:所有的数据库表:gmt_create .gmt_modified几乎所有的表都要配置上!而且需要自动化!</p>
<blockquote>
<p>方式一:数据库级别</p>
</blockquote>
<p>1.在表中新增字段 create_time , update_time</p>
<figure data-type="image" tabindex="12"><img src="https://img-blog.csdnimg.cn/20200930150615744.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>2.再次测试插入方法,我们需要先把实体类同步</p>
<pre><code class="language-java">private Data creatTime;
private Data updateTime;
</code></pre>
<p>再次更新查看结果即可</p>
<figure data-type="image" tabindex="13"><img src="https://img-blog.csdnimg.cn/20200930150634402.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<blockquote>
<p>方式二:代码级别</p>
</blockquote>
<p>1.删除数据库默认值</p>
<figure data-type="image" tabindex="14"><img src="https://img-blog.csdnimg.cn/20200930150644351.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>2.实体类字段属性上添加注解</p>
<pre><code class="language-java">//记住用util包下的Date!!
//字段添加填充内容
@TableField(fill = FieldFill.INSERT)
private Data creatTime;

@TableField(fill = FieldFill.INSERT_UPDATE)
private Data updateTime;
</code></pre>
<p>3.编写处理器来处理这个注解</p>
<pre><code class="language-java">package com.codeyuaiiao.handler;

import com.baomidou.mybatisplus.core.handlers.MetaObjectHandler;
import lombok.extern.slf4j.Slf4j;
import org.apache.ibatis.reflection.MetaObject;
import org.springframework.stereotype.Component;

import java.util.Date;

@Slf4j
@Component //把处理器加到IOC容器中
public class MyMetaObjectHandler implements MetaObjectHandler {

    //插入时的填充策略
    @Override
    public void insertFill(MetaObject metaObject) {
        log.info(&quot;Start insert fill.... &quot;);
        this.setFieldValByName(&quot;createTime&quot;,new Date(),metaObject);
        this.setFieldValByName(&quot;updateTime&quot;,new Date(),metaObject);
    }

    //更新时的填充策略
    @Override
    public void updateFill(MetaObject metaObject) {
        log.info(&quot;Start update fill.... &quot;);
        this.setFieldValByName(&quot;updateTime&quot;,new Date(),metaObject);
    }
}
</code></pre>
<p>3.编写处理器来处理这个注解即可!</p>
<pre><code class="language-java">package com.codeyuaiiao.handler;

import com.baomidou.mybatisplus.core.handlers.MetaObjectHandler;
import lombok.extern.slf4j.Slf4j;
import org.apache.ibatis.reflection.MetaObject;
import org.springframework.stereotype.Component;

import java.util.Date;

@Slf4j
@Component //把处理器加到IOC容器中
public class MyMetaObjectHandler implements MetaObjectHandler {

    //插入时的填充策略
    @Override
    public void insertFill(MetaObject metaObject) {
        log.info(&quot;Start insert fill.... &quot;);
        this.setFieldValByName(&quot;createTime&quot;,new Date(),metaObject);
        this.setFieldValByName(&quot;updateTime&quot;,new Date(),metaObject);
    }

    //更新时的填充策略
    @Override
    public void updateFill(MetaObject metaObject) {
        log.info(&quot;Start update fill.... &quot;);
        this.setFieldValByName(&quot;updateTime&quot;,new Date(),metaObject);
    }
}
</code></pre>
<p>4.测试插入</p>
<p>5测试更新,观察时间即可!</p>
<h3 id="5乐观锁悲观锁">5.乐观锁&amp;悲观锁</h3>
<blockquote>
<p>乐观锁: 顾名思义十分乐观,他总是认为不会出现问题,无论干什么都不去上锁!如果出现了问题,再次更新值测试</p>
<p>悲观锁;顾名思义十分悲观,他总是认为出现问题,无论干什么都会上锁!再去操作!</p>
</blockquote>
<p>我们这里主要讲解 乐观锁机制!</p>
<p>乐观锁实现方式:</p>
<ul>
<li>取出记录时,获取当前version</li>
<li>更新时,带上这个version</li>
<li>执行更新时,set version = newVersion where version = oldVersion</li>
<li>如果version不对,就更新失败</li>
</ul>
<h3 id="6查询操作">6.查询操作</h3>
<pre><code class="language-java">//测试查询
@Test
public void testSelectById(){
    User user = userMapper.selectById(1L);
    System.out.println(user);
}

//测试批量查询
public void testSelectBatchId(){
    List&lt;User&gt; users = userMapper.selectBatchIds(Arrays.asList(1, 2, 3));
    users.forEach(System.out::println);
}

//按条件查询之--使用Map操作
@Test
public void testSelectBatchIds(){
    HashMap&lt;String, Object&gt; map = new HashMap&lt;&gt;();
    map.put(&quot;name&quot;,&quot;阿峧说java&quot;);
    map.put(&quot;age&quot;,&quot;18&quot;);

    List&lt;User&gt; users = userMapper.selectByMap(map);
    users.forEach(System.out::println);
}
</code></pre>
<p>我们在工作中会遇见逻辑删除</p>
<h3 id="7逻辑删除">7.逻辑删除</h3>
<blockquote>
<p>物理删除:从数据库中直接移除</p>
<p>逻辑删除: 在数据库中没有被移除,而是通过一个变量来让他失效! deleted=0=&gt;deleted=1</p>
</blockquote>
<p>管理员可以查看被删除的记录!防止数据的丢失,类似于回收站!</p>
<p>测试:</p>
<p>1.在数据表中增加一个deleted字段</p>
<figure data-type="image" tabindex="15"><img src="https://img-blog.csdnimg.cn/20200930150703144.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>2.实体类中增加属性</p>
<pre><code class="language-java">//逻辑删除
@TableLogic
private Integer deleted;
</code></pre>
<p>3.配置</p>
<pre><code class="language-java">//注册逻辑删除
@Bean
public ISqlInjector sqlInjector(){
    return new LogicSqlInjector();
}
</code></pre>
<p>配置</p>
<pre><code class="language-properties"># 配置逻辑删除
mybatis-plus.global-config.db-config.logic-delete-value=1
mybatis-plus.global-config.db-config.logic-not-delete-value=0
</code></pre>
<blockquote>
<p>走的是更新操作,不是删除操作</p>
<figure data-type="image" tabindex="16"><img src="https://img-blog.csdnimg.cn/2020093015071649.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
</blockquote>
<blockquote>
<p>查询的时候会自动过滤删除的数据</p>
<figure data-type="image" tabindex="17"><img src="https://img-blog.csdnimg.cn/20200930150728942.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
</blockquote>
<p>以上的所有CRUD操作及其扩展,我们都必须精通掌握!会大大提好你的工作和写项目的效率</p>
<h2 id="4性能分析插件">4.性能分析插件</h2>
<p>我们在平时的开发中,会遇到一些慢sql.</p>
<p>MP也提供了性能分析插件,如果超过这个时间就停止运行!</p>
<p>1.导入插件</p>
<pre><code class="language-java">@Bean
    @Profile({&quot;dev&quot;,&quot;test&quot;}) //设置dev 和 test环境开启
    public PerformanceInterceptor performanceInterceptor(){
        PerformanceInterceptor performanceInterceptor = new PerformanceInterceptor();
        performanceInterceptor.setMaxTime(1);
        performanceInterceptor.setFormat(true);
        return performanceInterceptor;
    }
</code></pre>
<p>[外链图片转存失败,源站可能有防盗链机制,建议将图片保存下来直接上传(img-7kbdevy0-1601449207282)(MyBatisPlusB站狂神说.assets/image-20200930133715010.png)]</p>
<p>记住在SpringBoot中配置环境为 dev或者test环境</p>
<p>application.properties中添加设置开发环境</p>
<pre><code class="language-properties">#设置开发环境
spring.profiles.active=dev
</code></pre>
<p>2.测试查询</p>
<pre><code class="language-java">@Test
    public void testSelectBatchId(){
        List&lt;User&gt; users = userMapper.selectBatchIds(Arrays.asList(1, 2, 3));
        users.forEach(System.out::println);
    }
</code></pre>
<figure data-type="image" tabindex="18"><img src="https://img-blog.csdnimg.cn/20200930150745143.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQzNjQ5MjIz,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<h2 id="5条件构造器">5.条件构造器</h2>
<pre><code class="language-java">package com.kuang;

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import com.kuang.mapper.UserMapper;
import com.kuang.pojo.User;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import java.util.List;
import java.util.Map;

@SpringBootTest
public class WrapperTest {

    @Autowired
    private UserMapper userMapper;

    @Test
    void contextLoads() {
        // 查询name不为空的用户，并且邮箱不为空的用户，年龄大于等于12
        QueryWrapper&lt;User&gt; wrapper = new QueryWrapper&lt;&gt;();
        wrapper
                .isNotNull(&quot;name&quot;)
                .isNotNull(&quot;email&quot;)
                .ge(&quot;age&quot;,12);
        userMapper.selectList(wrapper).forEach(System.out::println); // 和我们刚才学习的map对比一下
    }

    @Test
    void test2(){
        // 查询名字狂神说
        QueryWrapper&lt;User&gt; wrapper = new QueryWrapper&lt;&gt;();
        wrapper.eq(&quot;name&quot;,&quot;狂神说&quot;);
        User user = userMapper.selectOne(wrapper); // 查询一个数据，出现多个结果使用List 或者 Map
        System.out.println(user);
    }

    @Test
    void test3(){
        // 查询年龄在 20 ~ 30 岁之间的用户
        QueryWrapper&lt;User&gt; wrapper = new QueryWrapper&lt;&gt;();
        wrapper.between(&quot;age&quot;,20,30); // 区间
        Integer count = userMapper.selectCount(wrapper);// 查询结果数
        System.out.println(count);
    }

    // 模糊查询
    @Test
    void test4(){
        // 查询年龄在 20 ~ 30 岁之间的用户
        QueryWrapper&lt;User&gt; wrapper = new QueryWrapper&lt;&gt;();
        // 左和右  t%
        wrapper
                .notLike(&quot;name&quot;,&quot;e&quot;)
                .likeRight(&quot;email&quot;,&quot;t&quot;);

        List&lt;Map&lt;String, Object&gt;&gt; maps = userMapper.selectMaps(wrapper);
        maps.forEach(System.out::println);
    }

    // 模糊查询
    @Test
    void test5(){

        QueryWrapper&lt;User&gt; wrapper = new QueryWrapper&lt;&gt;();
        // id 在子查询中查出来
        wrapper.inSql(&quot;id&quot;,&quot;select id from user where id&lt;3&quot;);

        List&lt;Object&gt; objects = userMapper.selectObjs(wrapper);
        objects.forEach(System.out::println);
    }

    //测试六
    @Test
    void test6(){
        QueryWrapper&lt;User&gt; wrapper = new QueryWrapper&lt;&gt;();
        // 通过id进行排序
        wrapper.orderByAsc(&quot;id&quot;);

        List&lt;User&gt; users = userMapper.selectList(wrapper);
        users.forEach(System.out::println);
    }

}
</code></pre>
<h2 id="6代码自动生成非常nb">6.代码自动生成**非常NB</h2>
<p>dao、pojo、service、controller都给我自己去编写完成！</p>
<p>AutoGenerator 是 MyBatis-Plus 的代码生成器，通过 AutoGenerator 可以快速生成 Entity、</p>
<p>Mapper、Mapper XML、Service、Controller 等各个模块的代码，极大的提升了开发效率。</p>
<p>只需要改实体类名字 和包名 还有 数据库配置即可</p>
<p>测试:</p>
<pre><code class="language-java">import com.baomidou.mybatisplus.annotation.DbType; import com.baomidou.mybatisplus.annotation.FieldFill; import com.baomidou.mybatisplus.annotation.IdType; import com.baomidou.mybatisplus.annotation.TableField; import com.baomidou.mybatisplus.generator.AutoGenerator; import com.baomidou.mybatisplus.generator.config.DataSourceConfig; import com.baomidou.mybatisplus.generator.config.GlobalConfig; import com.baomidou.mybatisplus.generator.config.PackageConfig; import com.baomidou.mybatisplus.generator.config.StrategyConfig; import com.baomidou.mybatisplus.generator.config.po.TableFill; import com.baomidou.mybatisplus.generator.config.rules.DateType; import com.baomidou.mybatisplus.generator.config.rules.NamingStrategy; import java.util.ArrayList; 

// 代码自动生成器 
public class KuangCode {
public static void main(String[] args) {
    // 需要构建一个 代码自动生成器 对象 
    AutoGenerator mpg = new AutoGenerator(); 
    
    // 配置策略 
    // 1、全局配置 
    GlobalConfig gc = new GlobalConfig();
    String projectPath = System.getProperty(&quot;user.dir&quot;); 
    gc.setOutputDir(projectPath+&quot;/src/main/java&quot;);
    gc.setAuthor(&quot;狂神说&quot;); gc.setOpen(false);
    gc.setFileOverride(false);
    
    // 是否覆盖
    gc.setServiceName(&quot;%sService&quot;);
    
    // 去Service的I前缀
    gc.setIdType(IdType.ID_WORKER);
    gc.setDateType(DateType.ONLY_DATE);
    gc.setSwagger2(true);
    mpg.setGlobalConfig(gc);
    
    //2、设置数据源
    DataSourceConfig dsc = new DataSourceConfig();
    dsc.setUrl(&quot;jdbc:mysql://localhost:3306/kuang_community? useSSL=false&amp;useUnicode=true&amp;characterEncoding=utf-8&amp;serverTimezone=GMT%2B8&quot;);
    dsc.setDriverName(&quot;com.mysql.cj.jdbc.Driver&quot;);
    dsc.setUsername(&quot;root&quot;);
    dsc.setPassword(&quot;123456&quot;);
    dsc.setDbType(DbType.MYSQL); mpg.setDataSource(dsc);
    
    //3、包的配置
    PackageConfig pc = new PackageConfig();
    //只需要改实体类名字 和包名 还有 数据库配置即可
    pc.setModuleName(&quot;blog&quot;); pc.setParent(&quot;com.kuang&quot;);
    pc.setEntity(&quot;entity&quot;); pc.setMapper(&quot;mapper&quot;);
    pc.setService(&quot;service&quot;); pc.setController(&quot;controller&quot;);
    mpg.setPackageInfo(pc);
    
    //4、策略配置
    StrategyConfig strategy = new StrategyConfig();
  strategy.setInclude(&quot;blog_tags&quot;,&quot;course&quot;,&quot;links&quot;,&quot;sys_settings&quot;,&quot;user_record&quot;,&quot; user_say&quot;);
    
    // 设置要映射的表名
    strategy.setNaming(NamingStrategy.underline_to_camel);
    strategy.setColumnNaming(NamingStrategy.underline_to_camel);
    strategy.setEntityLombokModel(true);
    
    // 自动lombok；
    strategy.setLogicDeleteFieldName(&quot;deleted&quot;); 
    
    // 自动填充配置
    TableFill gmtCreate = new TableFill(&quot;gmt_create&quot;, FieldFill.INSERT);
    TableFill gmtModified = new TableFill(&quot;gmt_modified&quot;, FieldFill.INSERT_UPDATE);
    ArrayList&lt;TableFill&gt; tableFills = new ArrayList&lt;&gt;();
    tableFills.add(gmtCreate); tableFills.add(gmtModified);
    strategy.setTableFillList(tableFills);
    
    // 乐观锁
    strategy.setVersionFieldName(&quot;version&quot;);
    strategy.setRestControllerStyle(true);
    strategy.setControllerMappingHyphenStyle(true);
    
    // localhost:8080/hello_id_2 
    mpg.setStrategy(strategy);
    mpg.execute(); //执行 
	}
}
</code></pre>
<p>大家可以给B站狂神说点一波关注!</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等56. 合并区间]]></title>
        <id>https://ganhan999.github.io/post/57、插入区间/</id>
        <link href="https://ganhan999.github.io/post/57、插入区间/">
        </link>
        <updated>2021-01-20T11:10:49.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给出一个无重叠的 ，按照区间起始端点排序的区间列表。<br>
在列表中插入一个新的区间，你需要确保列表中的区间仍然有序且不重叠（如果有必要的话，可以合并区间）。</p>
<p>示例 1：<br>
输入：intervals = [[1,3],[6,9]], newInterval = [2,5]<br>
输出：[[1,5],[6,9]]</p>
<p>示例 2：<br>
输入：intervals = [[1,2],[3,5],[6,7],[8,10],[12,16]], newInterval = [4,8]<br>
输出：[[1,2],[3,10],[12,16]]<br>
解释：这是因为新的区间 [4,8] 与 [3,5],[6,7],[8,10] 重叠。</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>依次合并区间，直到它完全在某两个区间之内，添加到区间内</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def insert(self, intervals: List[List[int]], newInterval: List[int]) -&gt; List[List[int]]:
        left, right = newInterval
        placed = False
        ans = list()
        for li, ri in intervals:
            if li &gt; right:
                # 在插入区间的右侧且无交集
                if not placed:#第一次在区间内就加入，之后就不进行语句内操作
                    ans.append([left, right])
                    placed = True
                ans.append([li, ri])
            elif ri &lt; left:
                # 在插入区间的左侧且无交集
                ans.append([li, ri])
            else:
                # 与插入区间有交集，计算它们的并集
                left = min(left, li)
                right = max(right, ri)

        if not placed:# 没找到就填到最后
            ans.append([left, right])
        return ans
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Git]]></title>
        <id>https://ganhan999.github.io/post/Git/</id>
        <link href="https://ganhan999.github.io/post/Git/">
        </link>
        <updated>2021-01-18T15:18:25.000Z</updated>
        <content type="html"><![CDATA[<p>狂神聊Git</p>
<p>学习git之前，我们需要先明白一个概念</p>
<p><strong>版本控制！</strong></p>
<p>版本控制</p>
<h2 id="什么是版本控制">什么是版本控制</h2>
<p>版本控制（Revision control）是一种在开发的过程中用于管理我们对文件、目录或工程等内容的修改历史，方便查看更改历史记录，备份以便恢复以前的版本的软件工程技术。</p>
<ul>
<li>实现跨区域多人协同开发</li>
<li>追踪和记载一个或者多个文件的历史记录</li>
<li>组织和保护你的源代码和文档</li>
<li>统计工作量</li>
<li>并行开发、提高开发效率</li>
<li>跟踪记录整个软件的开发过程</li>
<li>减轻开发人员的负担，节省时间，同时降低人为错误</li>
</ul>
<p>简单说就是用于管理多人协同开发项目的技术。</p>
<p>没有进行版本控制或者版本控制本身缺乏正确的流程管理，在软件开发过程中将会引入很多问题，如软件代码的一致性、软件内容的冗余、软件过程的事物性、软件开发过程中的并发性、软件源代码的安全性，以及软件的整合等问题。</p>
<p>无论是工作还是学习，或者是自己做笔记，都经历过这样一个阶段！我们就迫切需要一个版本控制工具！</p>
<figure data-type="image" tabindex="1"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEM0cWVMeE1nb1RnOUIxNTRpYmFoc1VhaWJpYVY3RGdIOUdURlFaajNLeWhmNWZ4cmo2RzJVNUhGZy82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>多人开发就必须要使用版本控制！</p>
<h2 id="常见的版本控制工具">常见的版本控制工具</h2>
<p>我们学习的东西，一定是当下最流行的！</p>
<p>主流的版本控制器有如下这些：</p>
<ul>
<li><strong>Git</strong></li>
<li><strong>SVN</strong>（Subversion）</li>
<li><strong>CVS</strong>（Concurrent Versions System）</li>
<li><strong>VSS</strong>（Micorosoft Visual SourceSafe）</li>
<li><strong>TFS</strong>（Team Foundation Server）</li>
<li>Visual Studio Online</li>
</ul>
<p>版本控制产品非常的多（Perforce、Rational ClearCase、RCS（GNU Revision Control System）、Serena Dimention、SVK、BitKeeper、Monotone、Bazaar、Mercurial、SourceGear Vault），现在影响力最大且使用最广泛的是Git与SVN</p>
<h2 id="版本控制分类">版本控制分类</h2>
<p><strong>1、本地版本控制</strong></p>
<p>记录文件每次的更新，可以对每个版本做一个快照，或是记录补丁文件，适合个人用，如RCS。</p>
<figure data-type="image" tabindex="2"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMERnM2ZIcmJQcWJORU9NTzlHVGpGaFZhdWtNWld4NTRpY1M3ZVMyeDhBN0JFdTBWQjlpYndFaHpRLzY0MA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p><strong>2、集中版本控制  SVN</strong></p>
<p>所有的版本数据都保存在服务器上，协同开发者从服务器上同步更新或上传自己的修改</p>
<figure data-type="image" tabindex="3"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMDBWNHVMYWlieHRaSTlSTHBxN3RrU2RsV2lhRjkyQVZlWjBpYjlEaWNxQmtTMnBvbzV1OHNFVTJtQ1EvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>所有的版本数据都存在服务器上，用户的本地只有自己以前所同步的版本，如果不连网的话，用户就看不到历史版本，也无法切换版本验证问题，或在不同分支工作。而且，所有数据都保存在单一的服务器上，有很大的风险这个服务器会损坏，这样就会丢失所有的数据，当然可以定期备份。代表产品：SVN、CVS、VSS</p>
<p><strong>3、分布式版本控制 Git</strong></p>
<p>每个人都拥有全部的代码！安全隐患！</p>
<p>所有版本信息仓库全部同步到本地的每个用户，这样就可以在本地查看所有版本历史，可以离线在本地提交，只需在连网时push到相应的服务器或其他用户那里。由于每个用户那里保存的都是所有的版本数据，只要有一个用户的设备没有问题就可以恢复所有的数据，但这增加了本地存储空间的占用。</p>
<p>不会因为服务器损坏或者网络问题，造成不能工作的情况！</p>
<figure data-type="image" tabindex="4"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMGV2OFE3cVhqc1RmZVN3RmV4ZEE0dEdqRkFpYVZFS1F6QUhkR2NJTlhJTEtmbEkyY2ZrOUJpYXdRLzY0MA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<h2 id="git与svn的主要区别">Git与SVN的主要区别</h2>
<p>SVN是集中式版本控制系统，版本库是集中放在中央服务器的，而工作的时候，用的都是自己的电脑，所以首先要从中央服务器得到最新的版本，然后工作，完成工作后，需要把自己做完的活推送到中央服务器。集中式版本控制系统是必须联网才能工作，对网络带宽要求较高。</p>
<figure data-type="image" tabindex="5"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMHF0RUlEcjJORzZyT0dnMVVnRHU1YzNmZlJSOFA3RkQ1RDhCUExVRVhwMGhRb0w3cWZwM0k2Zy82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>Git是分布式版本控制系统，没有中央服务器，每个人的电脑就是一个完整的版本库，工作的时候不需要联网了，因为版本都在自己电脑上。协同的方法是这样的：比如说自己在电脑上改了文件A，其他人也在电脑上改了文件A，这时，你们两之间只需把各自的修改推送给对方，就可以互相看到对方的修改了。Git可以直接看到更新了哪些代码和文件！</p>
<p><strong>Git是目前世界上最先进的分布式版本控制系统。</strong></p>
<p>聊聊Git的历史</p>
<p>同生活中的许多伟大事物一样，Git 诞生于一个极富纷争大举创新的年代。</p>
<p>Linux 内核开源项目有着为数众广的参与者。绝大多数的 Linux 内核维护工作都花在了提交补丁和保存归档的繁琐事务上(1991－2002年间)。到 2002 年，整个项目组开始启用一个专有的分布式版本控制系统 BitKeeper 来管理和维护代码。</p>
<p>Linux社区中存在很多的大佬！破解研究 BitKeeper ！</p>
<p>到了 2005 年，开发 BitKeeper 的商业公司同 Linux 内核开源社区的合作关系结束，他们收回了 Linux 内核社区免费使用 BitKeeper 的权力。这就迫使 Linux 开源社区(特别是 Linux 的缔造者 Linus Torvalds)基于使用 BitKeeper 时的经验教训，开发出自己的版本系统。（2周左右！） 也就是后来的 Git！</p>
<p><strong>Git是目前世界上最先进的分布式版本控制系统。</strong></p>
<p>Git是免费、开源的，最初Git是为辅助 Linux 内核开发的，来替代 BitKeeper！</p>
<figure data-type="image" tabindex="6"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMFhHTGJ5RkRVaWNjQ3NpYjRMOVZrZzduZUpWV3VwZlNjYnJqZDd6bTdhcEM4ZVlUemdRenROQW5BLzY0MA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>Linux和Git之父李纳斯·托沃兹（Linus Benedic Torvalds）1969、芬兰</p>
<p>Git环境配置</p>
<h2 id="软件下载">软件下载</h2>
<p>打开 [git官网] https://git-scm.com/，下载git对应操作系统的版本。</p>
<p>所有东西下载慢的话就可以去找镜像！</p>
<p>官网下载太慢，我们可以使用淘宝镜像下载：http://npm.taobao.org/mirrors/git-for-windows/</p>
<figure data-type="image" tabindex="7"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMElYa3RzZUJSN2xwdlZGNGJpYkZ3aWFpYm5HeGtEbTB3WWljUElpYVp4Y1VlMkt1aWJBSGo4M01pYVdGU1EvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>下载对应的版本即可安装！</p>
<p>安装：无脑下一步即可！安装完毕就可以使用了！</p>
<h2 id="启动git">启动Git</h2>
<p>安装成功后在开始菜单中会有Git项，菜单下有3个程序：任意文件夹下右键也可以看到对应的程序！</p>
<figure data-type="image" tabindex="8"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMGphaWFBZnIycEFmV3RGWDU3a0dZcVIzU2xOeERsQVpEa0NVNklPQjFZQWljS3hIaWI1eUdidjl6US82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>**Git Bash：**Unix与Linux风格的命令行，使用最多，推荐最多</p>
<p>**Git CMD：**Windows风格的命令行</p>
<p><strong>Git GUI</strong>：图形界面的Git，不建议初学者使用，尽量先熟悉常用命令</p>
<h2 id="常用的linux命令">常用的Linux命令</h2>
<p>平时一定要多使用这些基础的命令！</p>
<p>1）、cd : 改变目录。</p>
<p>2）、cd . . 回退到上一个目录，直接cd进入默认目录</p>
<p>3）、pwd : 显示当前所在的目录路径。</p>
<p>4）、ls(ll):  都是列出当前目录中的所有文件，只不过ll(两个ll)列出的内容更为详细。</p>
<p>5）、touch : 新建一个文件 如 touch index.js 就会在当前目录下新建一个index.js文件。</p>
<p>6）、rm:  删除一个文件, rm index.js 就会把index.js文件删除。</p>
<p>7）、mkdir:  新建一个目录,就是新建一个文件夹。</p>
<p>8）、rm -r :  删除一个文件夹, rm -r src 删除src目录</p>
<pre><code class="language-php">rm -rf /  切勿在Linux中尝试！删除电脑中全部文件！
</code></pre>
<p>9）、mv 移动文件, mv index.html src index.html 是我们要移动的文件, src 是目标文件夹,当然, 这样写,必须保证文件和目标文件夹在同一目录下。</p>
<p>10）、reset 重新初始化终端/清屏。</p>
<p>11）、clear 清屏。</p>
<p>12）、history 查看命令历史。</p>
<p>13）、help 帮助。</p>
<p>14）、exit 退出。</p>
<p>15）、#表示注释</p>
<h2 id="git配置">Git配置</h2>
<p>所有的配置文件，其实都保存在本地！</p>
<p>查看配置 git config -l</p>
<figure data-type="image" tabindex="9"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEdKQU5pYnM4NkR3WXFvQURkZ1p5U0dpYm1hZlI4cDFYQnE2WkczdDBKMndTZzlpY3JJVlZRbzZkUS82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>查看不同级别的配置文件：</p>
<pre><code class="language-php">#查看系统config



git config --system --list



　　



#查看当前用户（global）配置



git config --global  --list
</code></pre>
<p><strong>Git相关的配置文件：</strong></p>
<p>1）、Git\etc\gitconfig  ：Git 安装目录下的 gitconfig   --system 系统级</p>
<p>2）、C:\Users\Administrator\ .gitconfig   只适用于当前登录用户的配置  --global 全局</p>
<figure data-type="image" tabindex="10"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMGhjSlMwcnhqM3FvQ1Z2ZkRLaDNXeHdRSmxTVjNQMTVFSVp1ZWpyYU93WExkaWM2TkNCOFg4b1EvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>这里可以直接编辑配置文件，通过命令设置后会响应到这里。</p>
<h2 id="设置用户名与邮箱用户标识必要">设置用户名与邮箱（用户标识，必要）</h2>
<p>当你安装Git后首先要做的事情是设置你的用户名称和e-mail地址。这是非常重要的，因为每次Git提交都会使用该信息。它被永远的嵌入到了你的提交中：</p>
<pre><code class="language-cs">git config --global user.name &quot;kuangshen&quot;  #名称



git config --global user.email 24736743@qq.com   #邮箱
</code></pre>
<p>只需要做一次这个设置，如果你传递了--global 选项，因为Git将总是会使用该信息来处理你在系统中所做的一切操作。如果你希望在一个特定的项目中使用不同的名称或e-mail地址，你可以在该项目中运行该命令而不要--global选项。总之--global为全局配置，不加为某个项目的特定配置。</p>
<figure data-type="image" tabindex="11"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMHpRWlkzN3ExaWFHMG43NDQ1WDhZZ1BWdlpINUFxeUd2VDRSZ21veUljWmxKV2lhTGN4eURnU2RRLzY0MA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>Git基本理论（重要）</p>
<h2 id="三个区域">三个区域</h2>
<p>Git本地有三个工作区域：工作目录（Working Directory）、暂存区(Stage/Index)、资源库(Repository或Git Directory)。如果在加上远程的git仓库(Remote Directory)就可以分为四个工作区域。文件在这四个区域之间的转换关系如下：</p>
<figure data-type="image" tabindex="12"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwME5KNEw5T1BJOWlhMU1taWJwdkRkNmNTZGRCZHZybGJkRXR5RU9yaDRDS25XVmlieWZDSGEzbHpYdy82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<ul>
<li>Workspace：工作区，就是你平时存放项目代码的地方</li>
<li>Index / Stage：暂存区，用于临时存放你的改动，事实上它只是一个文件，保存即将提交到文件列表信息</li>
<li>Repository：仓库区（或本地仓库），就是安全存放数据的位置，这里面有你提交到所有版本的数据。其中HEAD指向最新放入仓库的版本</li>
<li>Remote：远程仓库，托管代码的服务器，可以简单的认为是你项目组中的一台电脑用于远程数据交换</li>
</ul>
<p>本地的三个区域确切的说应该是git仓库中HEAD指向的版本：</p>
<figure data-type="image" tabindex="13"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMGljejZYMmFpYklnVVd6SHh0d1g4a2ljUENLcERyc2lhUHpaazA0T2xJMmJ6bHlkemljQnVYVEp2TEVRLzY0MA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<ul>
<li>Directory：使用Git管理的一个目录，也就是一个仓库，包含我们的工作空间和Git的管理空间。</li>
<li>WorkSpace：需要通过Git进行版本控制的目录和文件，这些目录和文件组成了工作空间。</li>
<li>.git：存放Git管理信息的目录，初始化仓库的时候自动创建。</li>
<li>Index/Stage：暂存区，或者叫待提交更新区，在提交进入repo之前，我们可以把所有的更新放在暂存区。</li>
<li>Local Repo：本地仓库，一个存放在本地的版本库；HEAD会只是当前的开发分支（branch）。</li>
<li>Stash：隐藏，是一个工作状态保存栈，用于保存/恢复WorkSpace中的临时状态。</li>
</ul>
<h2 id="工作流程">工作流程</h2>
<p>git的工作流程一般是这样的：</p>
<p>１、在工作目录中添加、修改文件；</p>
<p>２、将需要进行版本管理的文件放入暂存区域；</p>
<p>３、将暂存区域的文件提交到git仓库。</p>
<p>因此，git管理的文件有三种状态：已修改（modified）,已暂存（staged）,已提交(committed)</p>
<figure data-type="image" tabindex="14"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMDlpYU9obDBkQUNmTHJNd05iRHp1Y0dRMzBzM0huc2lhY3pmY1I2ZEM5T2VoaWN1d2liS3VIalJsemcvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>Git项目搭建</p>
<h2 id="创建工作目录与常用指令">创建工作目录与常用指令</h2>
<p>工作目录（WorkSpace)一般就是你希望Git帮助你管理的文件夹，可以是你项目的目录，也可以是一个空目录，建议不要有中文。</p>
<p>日常使用只要记住下图6个命令：</p>
<figure data-type="image" tabindex="15"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEFJSTZZVm9vVXppYnBpYnpKbm9PSEhYVXNMM2Y5RHFBNGhvclVpYmZjcEVaODhPeWYyZ1FRTlI2dy82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<h2 id="本地仓库搭建">本地仓库搭建</h2>
<p>创建本地仓库的方法有两种：一种是创建全新的仓库，另一种是克隆远程仓库。</p>
<p>1、创建全新的仓库，需要用GIT管理的项目的根目录执行：</p>
<pre><code class="language-shell"># 在当前目录新建一个Git代码库



$ git init
</code></pre>
<p>2、执行后可以看到，仅仅在项目目录多出了一个.git目录，关于版本等的所有信息都在这个目录里面。</p>
<h2 id="克隆远程仓库">克隆远程仓库</h2>
<p>1、另一种方式是克隆远程目录，由于是将远程服务器上的仓库完全镜像一份至本地！</p>
<pre><code class="language-shell"># 克隆一个项目和它的整个代码历史(版本信息)



$ git clone [url]  # https://gitee.com/kuangstudy/openclass.git
</code></pre>
<p>2、去 gitee 或者 github 上克隆一个测试！</p>
<p>Git文件操作</p>
<h2 id="文件的四种状态">文件的四种状态</h2>
<p>版本控制就是对文件的版本控制，要对文件进行修改、提交等操作，首先要知道文件当前在什么状态，不然可能会提交了现在还不想提交的文件，或者要提交的文件没提交上。</p>
<ul>
<li>Untracked: 未跟踪, 此文件在文件夹中, 但并没有加入到git库, 不参与版本控制. 通过git add 状态变为Staged.</li>
<li>Unmodify: 文件已经入库, 未修改, 即版本库中的文件快照内容与文件夹中完全一致. 这种类型的文件有两种去处, 如果它被修改, 而变为Modified. 如果使用git rm移出版本库, 则成为Untracked文件</li>
<li>Modified: 文件已修改, 仅仅是修改, 并没有进行其他的操作. 这个文件也有两个去处, 通过git add可进入暂存staged状态, 使用git checkout 则丢弃修改过, 返回到unmodify状态, 这个git checkout即从库中取出文件, 覆盖当前修改 !</li>
<li>Staged: 暂存状态. 执行git commit则将修改同步到库中, 这时库中的文件和本地文件又变为一致, 文件为Unmodify状态. 执行git reset HEAD filename取消暂存, 文件状态为Modified</li>
</ul>
<h2 id="查看文件状态">查看文件状态</h2>
<p>上面说文件有4种状态，通过如下命令可以查看到文件的状态：</p>
<pre><code class="language-properties">#查看指定文件状态



git status [filename]







 



#查看所有文件状态



git status







 



# git add .                  添加所有文件到暂存区



# git commit -m &quot;消息内容&quot;    提交暂存区中的内容到本地仓库 -m 提交信息
</code></pre>
<h2 id="忽略文件">忽略文件</h2>
<p>有些时候我们不想把某些文件纳入版本控制中，比如数据库文件，临时文件，设计文件等</p>
<p>在主目录下建立&quot;.gitignore&quot;文件，此文件有如下规则：</p>
<ol>
<li>忽略文件中的空行或以井号（#）开始的行将会被忽略。</li>
<li>可以使用Linux通配符。例如：星号（*）代表任意多个字符，问号（？）代表一个字符，方括号（[abc]）代表可选字符范围，大括号（{string1,string2,...}）代表可选的字符串等。</li>
<li>如果名称的最前面有一个感叹号（!），表示例外规则，将不被忽略。</li>
<li>如果名称的最前面是一个路径分隔符（/），表示要忽略的文件在此目录下，而子目录中的文件不忽略。</li>
<li>如果名称的最后面是一个路径分隔符（/），表示要忽略的是此目录下该名称的子目录，而非文件（默认文件或目录都忽略）。</li>
</ol>
<pre><code class="language-bash">#为注释



*.txt        #忽略所有 .txt结尾的文件,这样的话上传就不会被选中！



!lib.txt     #但lib.txt除外



/temp        #仅忽略项目根目录下的TODO文件,不包括其它目录temp



build/       #忽略build/目录下的所有文件



doc/*.txt    #会忽略 doc/notes.txt 但不包括 doc/server/arch.txt
</code></pre>
<p>使用码云</p>
<h2 id="github-是有墙的比较慢在国内的话我们一般使用-gitee-公司中有时候会搭建自己的gitlab服务器">github 是有墙的，比较慢，在国内的话，我们一般使用 gitee ，公司中有时候会搭建自己的gitlab服务器</h2>
<p>这个其实可以作为大家未来找工作的一个重要信息！</p>
<p>1、注册登录码云，完善个人信息</p>
<figure data-type="image" tabindex="16"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMGViQXFodGVza0c0R0h3RDAxYlg0bFhZbXhsbU1HbjhQUnFuNGFDWGZhUWRwM1NuYkJIZGlidFEvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>2、设置本机绑定SSH公钥，实现免密码登录！（免密码登录，这一步挺重要的，码云是远程仓库，我们是平时工作在本地仓库！)</p>
<pre><code class="language-apache"># 进入 C:\Users\Administrator\.ssh 目录



# 生成公钥



ssh-keygen
</code></pre>
<figure data-type="image" tabindex="17"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMFlsSzRpYlEwRU1zM0xtUm1kaWFocG1hOHNzVFFlZGtoeVNoTmtpYlR5RkJ2YVpXaWNpY1RmTmljV1FJQS82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>3、将公钥信息public key 添加到码云账户中即可！</p>
<figure data-type="image" tabindex="18"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEk1Wmljcm00UEVibm94OUw1d2pDUHRQeWJDeXJLSTFKT2tSV0NZSVk1enNYNEZ2STc3TFhYbVEvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>4、使用码云创建一个自己的仓库！</p>
<figure data-type="image" tabindex="19"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEZTSXdKYjlnNlFicDk5alk2MDV4ZFBmaDNONGwyckdwRDQ0ZDZOQ2NkaWJhbmtCVUw2MHVPRGcvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>许可证：开源是否可以随意转载，开源但是不能商业使用，不能转载，...  限制！</p>
<figure data-type="image" tabindex="20"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMFM5NlhmR29nTldWcVJBZXhleWJUN0RYZHlRaGZjWUoxb0VBZ2FIMVJpYlJVMFdaRTBlY3pkeHcvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>克隆到本地！</p>
<figure data-type="image" tabindex="21"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMFB5S2ZHRmQ4VUhTR0lSYlZMa1hINWljUXNneGg2SzJSUGliWWVVRVI1NFV6dU5WQVlzZ3hYY2ZBLzY0MA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>IDEA中集成Git</p>
<p>1、新建项目，绑定git。</p>
<figure data-type="image" tabindex="22"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEQ4TFBHdTJTTktYRDAxSU1xRGFTa0JlUDhpYnR2bmFzQllpYVJleXVaV0FsMEVqRWliOElZZjdjUS82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>注意观察idea中的变化</p>
<figure data-type="image" tabindex="23"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMENzOTNCaWFPaWExU2RrOGljZEg3dlF6UGZ6SWp1b1ROWXF1S3pZdHJFZTVta2xoZzJiN0tPWXNvdy82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>2、修改文件，使用IDEA操作git。</p>
<ul>
<li>添加到暂存区</li>
<li>commit 提交</li>
<li>push到远程仓库</li>
</ul>
<p>3、提交测试</p>
<figure data-type="image" tabindex="24"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMHRFUklzemRnTFZsVVdhbXlSYXBmTjc0YVI4WGVHRlYyT1lXaWFlUjlDa1lsZm9CZWZSaDJBSUEvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>这些都是单个人的操作！</p>
<p>学习的方式最重要！学会学习！我上课的更多时候都是在教大家去学习一种理念和思想（学习方式）</p>
<p>有道无术、术尚可求。有术无道、止于术！</p>
<p>真正的教学，授人以渔！</p>
<h2 id="说明git分支">说明：GIT分支</h2>
<p>分支在GIT中相对较难，分支就是科幻电影里面的平行宇宙，如果两个平行宇宙互不干扰，那对现在的你也没啥影响。不过，在某个时间点，两个平行宇宙合并了，我们就需要处理一些问题了！</p>
<figure data-type="image" tabindex="25"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEJPR3phRzRRVGM0SlhPMGhTbHdjTnR1ak56QXZ4ZWliU3JhakxZTENUNm90Tm5IRFY5eFlXd0EvNjQw?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<figure data-type="image" tabindex="26"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMEF5bjg3d294ZmVwT2hTbFVqNEZRVEZVc2lhNGljMGo2YVF5NFR6MzJQUnVKMEhTVmVHZVV6VVJBLzY0MA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>git分支中常用指令：</p>
<pre><code class="language-shell"># 列出所有本地分支



git branch







 



# 列出所有远程分支



git branch -r







 



# 新建一个分支，但依然停留在当前分支



git branch [branch-name]







 



# 新建一个分支，并切换到该分支



git checkout -b [branch]







 



# 合并指定分支到当前分支



$ git merge [branch]







 



# 删除分支



$ git branch -d [branch-name]







 



# 删除远程分支



$ git push origin --delete [branch-name]



$ git branch -dr [remote/branch]
</code></pre>
<p>IDEA中操作</p>
<figure data-type="image" tabindex="27"><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9tbWJpei5xcGljLmNuL21tYml6X3BuZy91SkRBVUtyR0M3S3N1OFVsSVR3TWxiWDNrTUd0WjlwMHdITklZZVRIQzhhSEdBU29EeVpPNjRRaWNzbHFpYU1iMU9KMVoxTFBvaWMzTEJHeURJWUJhN1hYdy82NDA?x-oss-process=image/format,png" alt="img" loading="lazy"></figure>
<p>如果同一个文件在合并分支时都被修改了则会引起冲突：解决的办法是我们可以修改冲突文件后重新提交！选择要保留他的代码还是你的代码！</p>
<p>master主分支应该非常稳定，用来发布新版本，一般情况下不允许在上面工作，工作一般情况下在新建的dev分支上工作，工作完后，比如上要发布，或者说dev分支代码稳定后可以合并到主分支master上来。</p>
<p>作业练习：找一个小伙伴，一起搭建一个远程仓库，来练习Git！</p>
<p>1、不要把Git想的很难，工作中多练习使用就自然而然的会了！</p>
<p>2、Git的学习也十分多，看完我的Git教程之后，可以多去思考，总结到自己博客！</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等74. 搜索二维矩阵]]></title>
        <id>https://ganhan999.github.io/post/74、搜索二维矩阵/</id>
        <link href="https://ganhan999.github.io/post/74、搜索二维矩阵/">
        </link>
        <updated>2021-01-18T13:06:40.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>编写一个高效的算法来判断 m x n 矩阵中，是否存在一个目标值。该矩阵具有如下特性：<br>
每行中的整数从左到右按升序排列。<br>
每行的第一个整数大于前一行的最后一个整数。</p>
<p>示例 1：<br>
输入：matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3<br>
输出：true</p>
<p>示例 2：<br>
输入：matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 13<br>
输出：false</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>二分法</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -&gt; bool:
        m = len(matrix)
        if m == 0:
            return False
        n = len(matrix[0])

        # 二分查找
        left, right = 0, m * n - 1
        while left &lt;= right:
            pivot_idx = (left + right) // 2
            pivot_element = matrix[pivot_idx // n][pivot_idx % n]
            if target == pivot_element:
                return True
            else:
                if target &lt; pivot_element:
                    right = pivot_idx - 1
                else:
                    left = pivot_idx + 1
        return False

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java Linux]]></title>
        <id>https://ganhan999.github.io/post/Java Linux/</id>
        <link href="https://ganhan999.github.io/post/Java Linux/">
        </link>
        <updated>2021-01-18T12:18:25.000Z</updated>
        <content type="html"><![CDATA[<p>为什么要学Linux</p>
<p>在服务器端，在开发领域Linux是越来越受欢迎，很多程序员都觉得不懂点Linux都觉得不好意思，Linux在开源社区的地位依然岿然不动。<br>
 尤其是作为一个后端程序员，是必须要掌握Linux的，因为这都成为了你找工作的基础门槛了，所以不得不学习！</p>
<h1 id="linux简介">Linux简介</h1>
<p>Linux内核最初只是由芬兰人林纳斯.托瓦兹( Linus Torvalds )在赫尔辛基大学上学时出于个人爱好而编写的。<br>
 Linux是一套免费使用和自由传播的<strong>类Unix操作系统</strong>,是一个基于POSIX (可移植操作系统接口)和UNIX的<strong>多用户</strong>、<strong>多任务</strong>、支持<strong>多线程</strong>和<strong>多CPU</strong>的操作系统。<br>
 Linux<strong>能运行主要的UNIX工具软件、应用程序和网络协议</strong>。它支持32位和64位硬件。Linux 继承了Unix以<strong>网络为核心</strong>的设计思想，是一个性能稳定的多用户网络操作系统。</p>
<h2 id="linux发行版">Linux发行版</h2>
<p>发行版就是将Linux内核与应用软件做一个打包<br>
<img src="https://img-blog.csdnimg.cn/20200630083506541.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 版本图解</p>
<h2 id="linux应用领域">Linux应用领域</h2>
<p>今天各种场合都有使用各种Linux发行版,从嵌入式设备到超级计算机,并且在服务器领域确定了地位,通常服务器使用LAMP(Linux + Apache + MySQL + PHP)或LNMP(Linux + Nginx + MySQL + PHP)组合。<br>
 目前Linux不仅在家庭与企业中使用,并且在政府中也很受欢迎。<br>
 许多国家都在使用Linux系统</p>
<h1 id="环境搭建">环境搭建</h1>
<p>跳过<br>
环境：虚拟机VMware Workstation Pro 15.2+CentOS 7</p>
<h1 id="走近linux系统">走近Linux系统</h1>
<h2 id="开机">开机</h2>
<p>会启动许多程序。它们在Windows叫做’服务&quot; ( service ) , <strong>在Linux就叫做&quot;守护进程&quot; ( daemon)</strong>。<br>
一般来说，用户的登录方式有三种:<br>
 ●命令行登录<br>
 ●SSH登录(远程)<br>
 ●图形界面登录<br>
 最高权限账户为root,可以操作一切!</p>
<h2 id="关机">关机</h2>
<p>在linux领域内大多用在服务器上,很少遇到关机的操作。毕竟服务器上跑一个服务是永无止境的,除非特殊情况下,不得已才会关机。<br>
 关机指令为shutdown</p>
<pre><code class="language-bash">sync #将数据由内存同步到硬盘中。
shutdown #关机指令，你可以man shutdown 来看一下 帮助文档。例如你可以运行如下命令关机:
shutdown -h 10 #这个命令告诉大家，计算机将在10分钟后关机
shutdown -h now#立马关机
shutdown -h 20:25 #系统会在今天20:25关机
shutdown -h +10 #十分钟后关机
shutdown -r now #系统立马重启
shutdown -r +10 #系统十分钟后重启
reboot #就是重启，等同于shutdown -r now
halt #关闭系统，等同于shutdown -h now和poweroff
</code></pre>
<p>最后总结：不管是重启还是关闭系统，首先要运行sync命令，把内存中的数据写到磁盘中，执行命令之后没有返回消息则表明运行成功</p>
<h2 id="系统目录">系统目录</h2>
<p>1、一切皆文件<br>
2、根目录/ ,所有的文件都挂载在这个节点下</p>
<pre><code class="language-bash">ls / #查看当前下的目录
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://img-blog.csdnimg.cn/20200630084025462.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 树状目录结构</p>
<h2 id="目录解释">目录解释</h2>
<p>/bin：bin是Binary的缩写, 这个目录存放着最经常使用的命令。<br>
/boot： 这里存放的是启动Linux时使用的一些核心文件，包括一些连接文件以及镜像文件。<br>
/dev ： dev是Device(设备)的缩写, 存放的是Linux的外部设备，在Linux中访问设备的方式和访问文件的方式是相同的。<br>
/etc： 这个目录用来存放所有的系统管理所需要的配置文件和子目录。<br>
/home：用户的主目录，在Linux中，每个用户都有一个自己的目录，一般该目录名是以用户的账号命名的。<br>
/lib：这个目录里存放着系统最基本的动态连接共享库，其作用类似于Windows里的DLL文件。<br>
/lost+found：这个目录一般情况下是空的，当系统非法关机后，这里就存放了一些文件。<br>
/media：linux系统会自动识别一些设备，例如U盘、光驱等等，当识别后，linux会把识别的设备挂载到这个目录下。<br>
/mnt：系统提供该目录是为了让用户临时挂载别的文件系统的，我们可以将光驱挂载在/mnt/上，然后进入该目录就可以查看光驱里的内容了。<br>
/opt：这是给主机额外安装软件所摆放的目录。比如你安装一个ORACLE数据库则就可以放到这个目录下。默认是空的。<br>
/proc：这个目录是一个虚拟的目录，它是系统内存的映射，我们可以通过直接访问这个目录来获取系统信息。<br>
/root：该目录为系统管理员，也称作超级权限者的用户主目录。<br>
/sbin：s就是Super User的意思，这里存放的是系统管理员使用的系统管理程序。<br>
/srv：该目录存放一些服务启动之后需要提取的数据。<br>
/sys：这是linux2.6内核的一个很大的变化。该目录下安装了2.6内核中新出现的一个文件系统 sysfs 。<br>
/tmp：这个目录是用来存放一些临时文件的。 用完即丢的文件可以放在这个目录下<br>
/usr：这是一个非常重要的目录，用户的很多应用程序和文件都放在这个目录下，类似于windows下的program files目录。<br>
/usr/bin： 系统用户使用的应用程序。<br>
/usr/sbin： 超级用户使用的比较高级的管理程序和系统守护程序。<br>
/usr/src： 内核源代码默认的放置目录。<br>
/var：这个目录中存放着在不断扩充着的东西，我们习惯将那些经常被修改的目录放在这个目录下。包括各种日志文件。<br>
/run：是一个临时文件系统，存储系统启动以来的信息。当系统重启时，这个目录下的文件应该被删掉或清除。<br>
/www：存放服务器网站相关的资源，环境，网站的项目</p>
<p>2020/6/29</p>
<h1 id="常用的基本命令">常用的基本命令</h1>
<h2 id="cd切换目录">cd:切换目录</h2>
<pre><code class="language-bash">cd:#切换目录命令!
./:#当前目录
/:#绝对路径
cd..:#返回上一级目录
cd ~:#回到当前的用户目录
pwd :#显示当前用户所在的目录
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://img-blog.csdnimg.cn/20200630084914110.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 pwd展示</p>
<h2 id="ls列出目录">ls(列出目录！)</h2>
<p>在Linux中Is可能是最常常被使用的!</p>
<pre><code class="language-bash">-a:#参数: all ,查看全部的文件,包括隐藏文件
-l:#参数列出所有的文件,包含文件的属性和权限,没有隐藏文件
</code></pre>
<p>所有Linux可以组合使用!</p>
<pre><code class="language-bash">ls -al 查看全部的文件包括隐藏文件的属性和权限
</code></pre>
<h2 id="mkdir-创建一个目录">mkdir 创建一个目录</h2>
<pre><code class="language-bash">mkdir -p test2/test3/test4 # 用-p递归创建层级目录
</code></pre>
<h2 id="rmdir-删除目录">rmdir 删除目录</h2>
<pre><code class="language-bash">rmdir -p test2/test3/test4 #递归删除文件
</code></pre>
<p>rmdir仅能删除空的目录,如果下面存在文件,需要先删除文件,递归删除多个目录-p参数即可</p>
<h2 id="cp复制文件或者目录">cp(复制文件或者目录)</h2>
<pre><code class="language-bash">cp install.sh cqhstudy #将当前目录下的install.sh 复制到cqhstudy文件夹中
</code></pre>
<h2 id="rm-移除文件或者目录">rm 移除文件或者目录</h2>
<pre><code class="language-bash">-f #忽略不存在的文件,不会出现警告,强制删除!
-r #递归删除目录!
-i #互动,删除询问是否删除
rm -rf install.sh/#删除系统中的install.sh
</code></pre>
<h2 id="mv-移动文件或者目录重命名文件">mv 移动文件或者目录|重命名文件</h2>
<pre><code class="language-bash">-f #强制
-u #只替换已经更新过的文件
12
mv install.sh cqhstudy #移动文件
mv cqhstudy cqhstudy2 #重命名文件夹名
</code></pre>
<h1 id="基本属性">基本属性</h1>
<p>Linux系统是一种典型的多用户系统,不同的用户处于不同的地位,拥有不同的权限。为了保护系统的安全性, Linux系统对不同的用户访问同一文件(包括目录文件)的权限做了不同的规定。<br>
 在Linux中我们可以使用ll或者ls -l命令来显示一个文件的属性以及文件所属的用户和组,如:<br>
 实例中, boot文件的第一个属性用&quot;d&quot;表示。 &quot;d&quot;在Linux中代表该文件是一 个目录文件。<br>
 在Linux中第一个字符代表这个文件是目录、 文件或链接文件等等:<br>
 ●当为[d]则是目录<br>
 ●当为[-]则是文件;<br>
 ●若是[l]则表示为链接文档( link file);<br>
 ●若是[b]则表示为装置文件里面的可供储存的接口设备(可随机存取装置) ;<br>
 ●若是[c]则表示为装置文件里面的串行端口设备,例如键盘、鼠标( 一次性读取装置)。<br>
 接下来的字符中,以三个为一组,且均为[rwx] 的三个参数的组合。<br>
 其中，[r]代表可读(read)、[w]代表可写(write)、[x]代表可执行(execute)。<br>
 要注意的是,这三个权限的位置不会改变,如果没有权限,就会出现减号[-]而已。<br>
 每个文件的属性由左边第一部分的10个字符来确定 (如下图) :<br>
<img src="https://img-blog.csdnimg.cn/20200630091157920.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 属性图解</p>
<p>从左至右用0-9这些数字来表示。<br>
第0位确定文件类型,第1-3位确定属主(该文件的所有者)拥有该文件的权限。第4-6位确定属组 (所有者的同组用户)拥有该文件的权限,第7-9位确定其他用户拥有该文件的权限。<br>
其中:<br>
第1、4、7位表示读权限,如果用&quot;r&quot;字符表示,则有读权限,如果用&quot;-“字符表示,则没有读权限;<br>
第2、5、8位表示写权限,如果用&quot;W&quot;字符表示,则有写权限,如果用”-“字符表示没有写权限;<br>
第3、6、9位表示可执行权限,如果用&quot;x&quot;字符表示,则有执行权限,如果用”-&quot;字符表示,则没有执行权限。<br>
对于文件来说,它都有一个特定的所有者,也就是对该文件具有所有权的用户。<br>
同时,在Linux系统中,用户是按组分类的, - -个用户属于一个或多个组。<br>
文件所有者以外的用户又可以分为文件所有者的同组用户和其他用户。<br>
<strong>因此, Linux系统按文件所有者、文件所有者同组用户和其他用户来规定了不同的文件访问权限。</strong><br>
在以上实例中, boot文件是一个目录文件,属主和属组都为root。<br>
<img src="https://img-blog.csdnimg.cn/20200630091910216.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 图解</p>
<h2 id="修改文件属性">修改文件属性</h2>
<p>1、chgrp :更改文件属组</p>
<pre><code class="language-bash">chgrp [-R] 属组名文件名
1
</code></pre>
<p>-R :递归更改文件属组,就是在更改某个目录文件的属组时,如果加上R的参数,那么该目录下的所有文件的属组都会更改。<br>
2、chown :更改文件属主,也可以同时更改文件属组</p>
<pre><code class="language-bash">chown [-R] 属主名文件名
chown [-R] 属主名:属组名文件名
</code></pre>
<p>3、chmod :更改文件9个属性</p>
<pre><code class="language-bash">chmod [-R] xyz 文件或目录
</code></pre>
<p>Linux文件属性有两种设置方法,一种是数字（常用的是数字）, 一种是符号。<br>
Linux文件的基本权限就有九个,分别是owner/group/others三种身份各有自己的read/write/execute权限。<br>
先复习一下刚刚上面提到的数据:文件的权限字符为: [-rwxrwxrwx]，这九个权限是三个三个一组的!其中,我们可以使用字来代表各个权限,各权限的分数对照表如下:<br>
r:4 w:2 x:1<br>
可读可写不可执行 rw- 6<br>
可读可写可执行 rwx 7<br>
chomd 777 文件赋予所有用户可读可执行!<br>
<img src="https://img-blog.csdnimg.cn/20200630092111387.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 示例</p>
<h2 id="文件内容查看">文件内容查看</h2>
<p>Linux系统中使用以下命令来查看文件的内容:<br>
●cat由第一行开始显示文件内容<br>
●tac从最后一行开始显示，可以看出tac是cat的倒着写!<br>
<img src="https://img-blog.csdnimg.cn/20200630092256742.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 cat和tac图解</p>
<p>●nl显示的时候,顺道输出行号!<br>
<img src="https://img-blog.csdnimg.cn/2020063009232293.png#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 nl命令图解</p>
<p>●more一页一页的显示文件内容（空格表示翻页，enter代表向下看下一行）<br>
<img src="https://img-blog.csdnimg.cn/20200630092344102.png#pic_center" alt="在这里插入图片描述" loading="lazy"><br>
<img src="https://img-blog.csdnimg.cn/20200630092351429.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 more命令图解</p>
<p><strong>●less与more类似,但是比more更好的是,他可以往前翻页!（空格翻页，上下键代表上下翻动页面，退出q命令，查找字符串/要查询的字符向下查询，向上查询使用?要查询的字符串,用n继续搜寻下一个,用N向上寻找）</strong><br>
●head 只看头几行 通过-n参数来控制显示几行<br>
●tail只看尾巴几行 通过-n参数来控制显示几行<br>
你可以使用man 命令来查看各个命令的使用文档,如: man [命令]。</p>
<h2 id="网络配置目录">网络配置目录:</h2>
<pre><code class="language-bash">cd /etc/ sysconfig/network-scripts
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://img-blog.csdnimg.cn/20200630092521193.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 网络基础配置</p>
<pre><code class="language-bash">ping #用来查看网络是否连通
ifconfig #查看网络 和Windows的ipconfig是一样的
</code></pre>
<p>2020/6/30</p>
<h1 id="硬链接和软链接">硬链接和软链接</h1>
<p>Linux链接分为两种：硬链接、软链接<br>
<strong>硬链接：A—B,假设B是A的硬链接，那么他们两个指向了同一个文件!允许一个文件拥有多个路径，用户可以通过这种机制硬链接到一个重要文件上，防止误删</strong><br>
<strong>软链接：类似Windows下的快捷方式，删除源文件，快捷方式也就访问不</strong>了<br>
操作步骤：<br>
创建链接 ln命令！<br>
touch命令创建文件！<br>
echo输入字符串<br>
<img src="https://img-blog.csdnimg.cn/20200701085219991.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 图解</p>
<p>删除f1之后，查看f2和f3的区别<br>
<img src="https://img-blog.csdnimg.cn/20200701085258696.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 删除f1之后 f2和f3的区别</p>
<h1 id="vim编辑器">Vim编辑器</h1>
<h2 id="什么是vim编辑器">什么是Vim编辑器</h2>
<p>Vim通过一些插件可以实现和IDE的功能!<br>
 Vim是从Vi发展出来的一个文本编辑器。代码补完、编译及错误跳转等方便编程的功能特别丰富,在程序员中被广泛使用。尤其是Linux中,必须要会使用Vim (查看内容, 编辑内容,保存内容! )<br>
 简单的来说，Vi是老式的字处理器,不过功能已经很齐全了,但是还是有可以进步的地方。<br>
 Vim则可以说是程序开发者的一项很好用的工具。<br>
三种使用方式:<br>
基本上Vi/Vim共分为三种模式,分别是<br>
<strong>命令模式( Command mode) ,<br>
输入模式( Insert mode )和<br>
底线命令模式( Lastline mode )。</strong><br>
这三种模式的作用分别是:</p>
<h2 id="命令模式">命令模式</h2>
<p>用户刚刚启动Vi/Vim ,便进入了命令模式。<br>
此状态下敲击键盘动作会被Vim识别为命令,而非输入字符。比如我们此时按下i ,并不会输入一个字符, i被当作了一个命令。<br>
以下是常用的几个命令:<br>
●i切换到输入模式,以输入字符。<br>
●x删除当前光标所在处的字符。<br>
●:切换到底线命令模式,以在最底一行输入命令。<br>
若想要编辑文本:启动Vim,进入了命令模式,按下i,切换到输入模式。<br>
命令模式只有一些最基本的命令,因此仍要依靠底线命令模式输入更多命令。</p>
<h2 id="输入模式">输入模式</h2>
<p>在命令模式下按下i就进入了输入模式。<br>
在输入模式中，可以使用以下按键：<br>
 字符按键以及Shift组合，输入字符<br>
 ENTER，回车键，换行<br>
 BACK SPACE，退格键，删除光标前一个字符<br>
 DEL，删除键，删除光标后一个字符<br>
 方向键，在文本中移动光标<br>
 HOME/END，移动光标到行首/行尾<br>
 Page Up/Page Down，上/下翻页<br>
 Insert，切换光标为输入/替换模式，光标将变成竖线/下划线<br>
 ESC，退出输入模式，切换到命令模式</p>
<h2 id="底线命令模式">底线命令模式</h2>
<p>在命令模式下按下:（英文冒号）就进入了底线命令模式。<br>
底线命令模式可以输入单个或多个字符的命令，可用的命令非常多。<br>
在底线命令模式中，基本的命令有（已经省略了冒号）：<br>
q 退出程序<br>
w 保存文件<br>
按ESC键可随时退出底线命令模式。<br>
2020/7/1</p>
<h1 id="账号管理">账号管理</h1>
<p>Linux系统是一个多用户多任务的分时操作系统,任何一个要使用系统资源的用户,都必须首先向系统管理员申请一个账号,然后以这个账号的身份进入系统。<br>
 用户的账号一方面可以帮助系统管理员对使用系统的用户进行跟踪,并控制他们对系统资源的访问;另一方面也可以帮助用户组织文件,并为用户提供安全性保护。<br>
 每个用户账号都拥有一个唯一的用户名和各自的口令。<br>
 用户在登录时键入正确的用户名和口令后,就能够进入系统和自己的主目录。<br>
 实现用户账号的管理,要完成的工作主要有如下几个方面:<br>
 ●用户账号的添加、删除与修改。<br>
 ●用户口令的管理。<br>
 ●用户组的管理。</p>
<h1 id="用户账号的管理">用户账号的管理</h1>
<p>用户账号的管理工作主要涉及到用户账号的添加、修改和删除。<br>
 添加用户账号就是在系统中创建一个新账号,然后为新账号分配用户号、用户组、主目录和登录ShelI等资源。</p>
<h2 id="useradd-命令-添加用户">useradd 命令 添加用户</h2>
<p>useradd -选项 用户名<br>
-m:自动创建这个用户的主目录/home/cqh<br>
-G:给用户分配组</p>
<pre><code class="language-bash">[root@cqh home]useradd -m cqh  #创建一个用户
[root@cqh home]ls
install.sh cqhstudy cqh www  
123
</code></pre>
<p>理解一下本质: Linux中一切皆文件,这里的添加用户说白了就是往某-个文件中写入用户的信息了! /etc/passwd</p>
<pre><code class="language-bash">[root@cqh home]cat /etc/passwd  #创建一个用户
cqh:x:1002:1002::/home/cqh:/bin/bash
12
</code></pre>
<h2 id="删除用户-userdel">删除用户 userdel</h2>
<pre><code class="language-bash">userdel -r cqh #删除用户的时候把它的目录也删除掉
</code></pre>
<h2 id="修改用户-usermod">修改用户 usermod</h2>
<p>对应修改的内容 修改那个用户</p>
<pre><code class="language-bash">[root@cqh home]usermode -d /home/233 cqh
</code></pre>
<p>修改完毕后查看配置文件 将cqh用户下的目录修改为233</p>
<h2 id="切换用户">切换用户</h2>
<figure data-type="image" tabindex="4"><img src="https://img-blog.csdnimg.cn/20200702085032114.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 细节说明</p>
<p>1.切换用户的命令为: su username [username是你的用户名]<br>
<img src="https://img-blog.csdnimg.cn/20200702085314796.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 切换后的细节</p>
<p>2.从普通用户切换到root用户,还可以使用命令: sudo su<br>
3.在终端输入exit或logout或使用快捷方式ctrl+d ,可以退回到原来用户,其实ctrl+d也是执行的exit命令<br>
4.在切换用户时,如果想在切换用户之后使用新用户的工作环境,可以在su和username之间加,例如: [su- root]<br>
$表示普通用户，#表示超级用户,也就是root用户</p>
<h2 id="用户的密码设置问题">用户的密码设置问题</h2>
<p>我们一般通过root创建用户的时候!要配置密码!<br>
如果是超级用户</p>
<pre><code class="language-bash">passwd username 
new password #新密码
re password #修改密码
</code></pre>
<p>普通用户</p>
<pre><code class="language-bash">passwd
(current)UNIX password:
new password: #密码不能过于简单
re password:
</code></pre>
<p>锁定账户<br>
root ,比如张三辞职了!冻结这个账号, 一旦冻结,这个人就登录不上系统了!</p>
<pre><code class="language-bash">passwd -l cqh #锁定之后这个用户就不能再登录了
passwd -d cqh #把密码清空 这样也能防止用户登录
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://img-blog.csdnimg.cn/20200702092430346.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>效果图 2020/7/2</p>
<h1 id="用户组管理">用户组管理</h1>
<p>属主、属组<br>
 每个用户都有一个用户组,系统可以对一个用户组中的所有用户进行集中管理(开发、测试、运维)。不同Linux 系统对用户组的规定有所不同,如Linux下的用户属于与它同名的用户组,这个用户组在创建用户时同时创建。<br>
 用户组的管理涉及用户组的添加、删除和修改。组的增加、删除和修改实际上就是对/etc/group文件的更新。</p>
<h2 id="创建一个用户组-groupadd">创建一个用户组 groupadd</h2>
<pre><code class="language-bash">[root@cqh ~]group add cqh
[root@cqh ~]cat /etc/group
</code></pre>
<p>创建完用户组后可以得到一一个组的id ,这个id是可以指定的! -g 520， 若果不指定就是自增1</p>
<h2 id="删除用户组-groupdel">删除用户组 groupdel</h2>
<pre><code class="language-bash">[root@cqh ~]groupdel cqh
[root@cqh ~]cat /etc/group
</code></pre>
<h2 id="修改用户组的权限和名字-groupmod-g-n">修改用户组的权限和名字 groupmod -g-n</h2>
<pre><code class="language-bash">cqh:x:1003
[root@cqh ~]group -g 666 -n newcqh cqh #修改cqh用户组id为666 重命名为newcqh 
</code></pre>
<h2 id="用户切换用户组">用户切换用户组</h2>
<pre><code class="language-bash">#当前登录用户 cqh
$ newgrp root #切换为root
</code></pre>
<h2 id="拓展-用户账户文件的查看了解即可-etcpasswd">拓展 用户账户文件的查看(了解即可) /etc/passwd</h2>
<p>用户名:口令(登录密码，我们不可见) :用户标识号:组标识号:注释性描述:主目录:登录She11<br>
这个文件中的每一行都代表这一用户,我们可以从这里看出这个用户的主目录在那里,可以看到属于哪一个组!<br>
 登录口令:把真正的加密后的用户口令字存放到/etc/shadow文件中,保证我们密码的安全性!<br>
用户组的所有信息都存放在/etc/group文件中。</p>
<h1 id="磁盘管理">磁盘管理</h1>
<p>df(列出文件系统整体的磁盘使用量) du(检查磁盘空间使用量)<br>
<img src="https://img-blog.csdnimg.cn/20200703084335994.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 df命令</p>
<figure data-type="image" tabindex="6"><img src="https://img-blog.csdnimg.cn/20200703084403486.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 du命令</p>
<p>du -sm /*<br>
<img src="https://img-blog.csdnimg.cn/20200703084435661.png#pic_center" alt="在这里插入图片描述" loading="lazy"></p>
<p>图 查看系统目录</p>
<p>Mac或者想使用Linux挂载我们的一些本地磁盘或者文件!</p>
<h2 id="挂载mount">挂载：mount</h2>
<figure data-type="image" tabindex="7"><img src="https://img-blog.csdnimg.cn/20200703084513927.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 挂载图</p>
<p>卸载：umount-f [挂载位置] 强制卸载<br>
除了这个之外，以后安装了JDK，可以使用Java命令查看信息</p>
<h1 id="进程管理">进程管理</h1>
<h2 id="什么是进程">什么是进程</h2>
<p>1、在Linux中 ,每一个程序都是有自己的一个进程,每一个进程都有一个id号!<br>
2、每一个进程呢,都会有一个父进程!<br>
3、进程可以有两种存在方式:<strong>前台</strong>!<strong>后台</strong>运行!<br>
4、一般的话<strong>服务都是后台运行</strong>的，基本的程序都是<strong>前台运行</strong>的<br>
命令<br>
ps 查看当前系统中正在执行的各种进程的信息！<br>
ps- xx：<br>
  -a 显示当前终端所有的进程信息<br>
  -u 以用户的信息显示进程<br>
  -x 显示后台运行进程的参数</p>
<pre><code class="language-bash">#ps -aux  查看所有进程
ps-aux|grep mysql  查看mysql相关进程
# | 在Linux中这个叫管道符   A|B
# grep 查找文件中符合条件的字符串
</code></pre>
<p>对于我们来说,这里目前只需要记住一个命令即可ps -xx|grep进程名字!过滤进程信息!<br>
ps-ef ：可以查看到父进程的信息</p>
<pre><code class="language-bash">ps-ef|grep mysql 看父进程我们一般可以通过目录树结构来查看
#进程树
#pstree -pu
#-p 显示父id
#-u 显示用户组
</code></pre>
<figure data-type="image" tabindex="8"><img src="https://img-blog.csdnimg.cn/20200706084738377.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>进程树效果图</p>
<p>结束进程：杀掉进程 等价于Windows结束任务</p>
<pre><code class="language-bash">kill -9 pid
</code></pre>
<p>表示强制结束该进程</p>
<h1 id="环境安装">环境安装</h1>
<p>安装软件一般有三种方式<br>
 rpm（在线发布一个SpringBoot项目）<br>
 解压缩<br>
 yum在线安装</p>
<h2 id="jdk安装">JDK安装</h2>
<p>1、下载IDK rpm。 去oralce官网下载即可!<br>
2、安装java环境</p>
<pre><code class="language-bash">java -version 检测当前系统是否存在Java环境 和windows命令一样
#如果有的话就需要卸载
rpm -qa|grep jdk 查看JDK版本信息
rpm -e --nodeps jdk_ 卸载
#卸载完毕后可安装JDK
rpm -ivk rpm包 
</code></pre>
<figure data-type="image" tabindex="9"><img src="https://img-blog.csdnimg.cn/20200706085220973.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 java-version命令</p>
<figure data-type="image" tabindex="10"><img src="https://img-blog.csdnimg.cn/20200706085250364.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 安装中</p>
<p>配置环境变量: /etc/profile 在文件的最后面增加java的配置和window安装环境变量一样!</p>
<pre><code class="language-bash">vim /etc/profile
</code></pre>
<p>这个环境变量有问题 狂神老师敲的是有问题的</p>
<pre><code class="language-bash">JAVA_HOME=/usr/java/jdk-14.0.1
JRE_HOME=$JAVA_HOME/jre
PATH=$PATH:$JAVA_HOME/bin:$JRE_HOME/bin
CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar:$JRE_HOME/lib
export JAVA_HOME JRE_HOME PATH CLASSPATH
</code></pre>
<p>让这个配置文件生效!</p>
<pre><code class="language-bash">source /etc/profile
</code></pre>
<p>狂神老师用的是阿里云，自带网络了，此处给用虚拟机的同学使用。虚拟机联网教程：<a href="https://blog.csdn.net/u012049667/article/details/81171003">虚拟机和主机联网教程</a><br>
 配置完后在Linux防火墙中开启相应端口 使用java -jar发布 如果你的项目在云服务器上，就可以在公网上进行发布查看了</p>
<pre><code class="language-bash">#开启防火墙端口
firewall-cmd --zone=public --add-port=9000/tcp --permanent
#重启防火墙
systemctl restart firewalld.service
#查看所有开启的端口，如果是阿里云 需要配置安全组规则
firewall-cmd --list-ports
</code></pre>
<figure data-type="image" tabindex="11"><img src="https://img-blog.csdnimg.cn/20200707090238431.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L29rRm9ycmVzdDI3,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>命令运行效果</p>
<figure data-type="image" tabindex="12"><img src="https://img-blog.csdnimg.cn/20200707090302192.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>查出centos7中的ip 外机进行访问</p>
<h2 id="tomcat安装">Tomcat安装</h2>
<p>ssm war 就需要放在tomcat中运行<br>
1.下载tomcat 官网下载即可<br>
2.解压tar -zxvf apache-tomcat-9.0.36.tar.gz<br>
3.启动tomcat</p>
<pre><code class="language-bash">#执行 
./startup.sh
#停止
./shutdown.sh
</code></pre>
<figure data-type="image" tabindex="13"><img src="https://img-blog.csdnimg.cn/20200707090700195.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>启动效果</p>
<pre><code class="language-bash">#开启端口
firewall-cmd --zone=public --add-port=8080/tcp –permanent
</code></pre>
<p>命令含义:<br>
 --zone 作用域<br>
 --add-port-80/tcp 添加端口， 格式为:端口/通讯协议<br>
 --permanent 永久生效，没有此参数重启后失效</p>
<p>如果是阿里云 上传完毕的项目直接购买自己的域名,备案解析过去即可!<br>
 域名解析后,如果端口是80 - http或者443-https 可以直接访问,如果是9000 8080 ,就需要通过Apcahe或者Nginx做一下反向代理即可（配置文件）</p>
<h2 id="dockeryum安装">Docker（yum安装）</h2>
<p>1.检测CentOS 7</p>
<pre><code class="language-bash">[root@localhost ~] cat /etc/redhat-release 
CentOS Linux release 7.3.1611 (Core)
</code></pre>
<p>2.安装我们的准备环境</p>
<pre><code class="language-bash">yum -y install 包名
#yum install 安装命令 -y  所有的提示都为y
yum -y install gcc
yum -y install gcc-c++
</code></pre>
<p>3.卸载以前的docker</p>
<pre><code class="language-bash">yum remove docker \
                  docker-client \
                  docker-client-latest \
                  docker-common \
                  docker-latest \
                  docker-latest-logrotate \
                  docker-logrotate \
                  docker-engine
</code></pre>
<p>4.下载环境</p>
<pre><code class="language-bash">yum install -y yum-utils \device-mapper-persistent-data \lvm2
</code></pre>
<p>5.使用国内阿里云镜像</p>
<pre><code class="language-bash">yum-config-manager --add-repo http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo
</code></pre>
<p>6.更新yum软件包安装</p>
<pre><code class="language-bash">yum makecache fast
</code></pre>
<p>7.安装docker ce</p>
<pre><code class="language-bash">yum -y install docker-ce docker-ce-Cli containerd.io
</code></pre>
<p>8.启动docker</p>
<pre><code class="language-bash">systemctl start docker
</code></pre>
<p>9.测试</p>
<pre><code class="language-bash">docker version
docker run hello-world
docker images
</code></pre>
<figure data-type="image" tabindex="14"><img src="https://img-blog.csdnimg.cn/20200707105511664.png#pic_center" alt="在这里插入图片描述" loading="lazy"></figure>
<p>图 运行效果</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[中等73. 矩阵置零]]></title>
        <id>https://ganhan999.github.io/post/73、矩阵置零/</id>
        <link href="https://ganhan999.github.io/post/73、矩阵置零/">
        </link>
        <updated>2021-01-17T12:06:40.000Z</updated>
        <content type="html"><![CDATA[<h1 id="题目">题目</h1>
<p>给定一个 m x n 的矩阵，如果一个元素为 0，则将其所在行和列的所有元素都设为 0。请使用原地算法。</p>
<p>示例 1:<br>
输入:<br>
[<br>
[1,1,1],<br>
[1,0,1],<br>
[1,1,1]<br>
]<br>
输出:<br>
[<br>
[1,0,1],<br>
[0,0,0],<br>
[1,0,1]<br>
]</p>
<p>示例 2:<br>
输入:<br>
[<br>
[0,1,2,0],<br>
[3,4,5,2],<br>
[1,3,1,5]<br>
]<br>
输出:<br>
[<br>
[0,0,0,0],<br>
[0,4,5,0],<br>
[0,3,1,0]<br>
]<br>
进阶:<br>
一个直接的解决方案是使用  O(mn) 的额外空间，但这并不是一个好的解决方案。<br>
一个简单的改进方案是使用 O(m + n) 的额外空间，但这仍然不是最好的解决方案。<br>
你能想出一个常数空间的解决方案吗？</p>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<h1 id="本体思路">本体思路</h1>
<p>我们扫描一遍原始矩阵，找到所有为零的元素。<br>
如果我们找到 [i, j] 的元素值为零，我们需要记录下行号 i 和列号 j。<br>
用两个 sets ，一个记录行信息一个记录列信息。<br>
最后，我们迭代原始矩阵，对于每个格子检查行 r 和列 c 是否被标记过，如果是就将矩阵格子的值设为 0。</p>
<p>时间复杂度：O(M×N)，其中 M 和 N 分别对应行数和列数。<br>
空间复杂度：O(M+N)。</p>
<h1 id="大神做法1">大神做法1：</h1>
<pre><code class="language-python">class Solution(object):
    def setZeroes(self, matrix):
        &quot;&quot;&quot;
        :type matrix: List[List[int]]
        :rtype: void Do not return anything, modify matrix in-place instead.
        &quot;&quot;&quot;
        R = len(matrix)
        C = len(matrix[0])
        rows, cols = set(), set()

        # Essentially, we mark the rows and columns that are to be made zero
        for i in range(R):
            for j in range(C):
                if matrix[i][j] == 0:
                    rows.add(i)
                    cols.add(j)

        # Iterate over the array once again and using the rows and cols sets, update the elements
        for i in range(R):
            for j in range(C):
                if i in rows or j in cols:
                    matrix[i][j] = 0

</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>遍历原始矩阵，如果发现如果某个元素 cell[i][j] 为 0，我们将第 i 行和第 j 列的所有非零元素设成很大的负虚拟值（比如说 -1000000）。注意，正确的虚拟值取值依赖于问题的约束，任何允许值范围外的数字都可以作为虚拟之。<br>
最后，我们便利整个矩阵将所有等于虚拟值（常量在代码中初始化为 MODIFIED）的元素设为 0。</p>
<h1 id="大神做法2">大神做法2：</h1>
<pre><code class="language-python">class Solution(object):
    def setZeroes(self, matrix):
        &quot;&quot;&quot;
        :type matrix: List[List[int]]
        :rtype: void Do not return anything, modify matrix in-place instead.
        &quot;&quot;&quot;
        MODIFIED = -1000000
        R = len(matrix)
        C = len(matrix[0])
        for r in range(R):
            for c in range(C):
                if matrix[r][c] == 0:
                    # We modify the elements in place. Note, we only change the non zeros to MODIFIED
                    for k in range(C):
                        matrix[r][k] = MODIFIED if matrix[r][k] != 0 else 0
                    for k in range(R):
                        matrix[k][c] = MODIFIED if matrix[k][c] != 0 else 0
        for r in range(R):
            for c in range(C):
                # Make a second pass and change all MODIFIED elements to 0 &quot;&quot;&quot;
                if matrix[r][c] == MODIFIED:
                    matrix[r][c] = 0
</code></pre>
<p>&quot;&quot;&quot;&quot;&quot;&quot;</p>
<p>遍历整个矩阵，如果 cell[i][j] == 0 就将第 i 行和第 j 列的第一个元素标记。<br>
第一行和第一列的标记是相同的，都是 cell[0][0]，所以需要一个额外的变量告知第一列是否被标记，<br>
同时用 cell[0][0] 继续表示第一行的标记。<br>
然后，从第二行第二列的元素开始遍历，如果第 r 行或者第 c 列被标记了，那么就将 cell[r][c] 设为 0。<br>
这里第一行和第一列的作用就相当于方法一中的 row_set 和 column_set 。<br>
然后我们检查是否 cell[0][0] == 0 ，如果是则赋值第一行的元素为零。<br>
然后检查第一列是否被标记，如果是则赋值第一列的元素为零。</p>
<h1 id="大神做法3">大神做法3：</h1>
<pre><code class="language-python">class Solution:
    def setZeroes(self, matrix: List[List[int]]) -&gt; None:
        row = len(matrix)
        col = len(matrix[0])
        row0_flag = False
        col0_flag = False
        # 找第一行是否有0
        for j in range(col):
            if matrix[0][j] == 0:
                row0_flag = True
                break
        # 第一列是否有0
        for i in range(row):
            if matrix[i][0] == 0:
                col0_flag = True
                break

        # 把第一行或者第一列作为 标志位
        for i in range(1, row):
            for j in range(1, col):
                if matrix[i][j] == 0:
                    matrix[i][0] = matrix[0][j] = 0
        #print(matrix)
        # 置0
        for i in range(1, row):
            for j in range(1, col):
                if matrix[i][0] == 0 or matrix[0][j] == 0:
                    matrix[i][j] = 0

        if row0_flag:
            for j in range(col):
                matrix[0][j] = 0
        if col0_flag:
            for i in range(row):
                matrix[i][0] = 0

</code></pre>
]]></content>
    </entry>
</feed>